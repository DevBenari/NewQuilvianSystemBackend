# Traceability Roadmap — Modul Laboratorium

| Field | Value |
|---|---|
| `blueprint_id` | `LAB-BP-001` |
| Roadmap revision | `79` |
| Status | `DRAFT` |
| Tanggal | 2026-09-02; **`EPIC-LAB-11` ditambahkan 2026-09-14** |
| Manifest | `blueprint-manifest.md` revision `68` — disegarkan 2026-09-18 sore sesudah `r25`/`r8`/rev 7 disetujui; nilai lama `67` berlaku beberapa jam, dan `38` tertinggal sejak revision 23 |
| Backend SHA | `e2152709`. Nilai lama `466a7127` (diverifikasi tidak berubah pada `9067fa73`) tertinggal sejak revision 15; disegarkan revision 23 |
| Frontend SHA | `9cd4cd03f` |
| Contract version | `LAB-API-v1` **r24** approved + **r25 usulan**, `LAB-STATE-v1` **r3**, `LAB-VAL-v1` **r7**, `LAB-INT-v1` r3, `LAB-PERM-v1` **rev 6** — seluruhnya `approved`; amandemen `MVP-5a` disetujui 2026-09-14, `MVP-5b` disetujui 2026-09-15, dan `r11` mencabut ruas `clinicalNote` pada 2026-09-15 |
| Approval | Yoga Aji Pratama (`yogaaji452@gmail.com`), pemilik modul, 2026-09-02 |
| Masukan | Decisions rev `48`; capability map rev `3`; gerbang requirement `LAB-RCG-001-r7` |
| Input hash | `sha256:6504b18a327b9966526bd1df8f3cb878d7f6d6519dacc1f7df16b1066729ae82` (decisions), dihitung 2026-09-02 |

Dokumen ini menjawab satu pertanyaan: **untuk setiap kebutuhan, siapa yang mengerjakannya dan
apa buktinya kalau sudah benar.** Baris yang tidak punya bukti muncul sebagai *coverage gap* di
bagian 4, bukan disembunyikan.

---

## 1. Traceability per Epic

### `EPIC-LAB-01` — Penandaan cito dan batas waktunya

| FR | Keputusan | Desain | Kontrak | Task BE | Task FE | Bukti | Status |
|---|---|---|---|---|---|---|---|
| `FR-01.1` | `LAB-DEC-013`, `LAB-DEC-026` | `02-backend-architecture.md` §4.3 | `LAB-API-v1` r3 | `BE-LAB-10` | `FE-LAB-06` | `AC-18` dan `AC-39` **terbukti** — [`BE-LAB-10.md`](../task/report/backend/BE-LAB-10.md); satu pesanan memuat Kalium cito dan Kolesterol biasa sekaligus. Sisi layar **terbukti 2026-09-04** — [`FE-LAB-06.md`](../task/report/frontend/FE-LAB-06.md). | **`SELESAI`** |
| `FR-01.2` | `LAB-DEC-026` | `contracts/validation-matrix.md` `VAL-03` | `LAB-VAL-v1` r3 | `BE-LAB-10` | `FE-LAB-06` | `AC-18` jalur gagal **terbukti** — `VAL-03` menjawab `403` tanpa mengubah satu ruas pun, `VAL-04` menjawab `409`. Sisi layar **terbukti 2026-09-04** — [`FE-LAB-06.md`](../task/report/frontend/FE-LAB-06.md). | **`SELESAI`** |
| `FR-01.3` | `LAB-DEC-013` | `CAP-04`, `CAP-15` | `LAB-STATE-v1` r2 | `BE-LAB-10` | — | `AC-18` **terbukti** — setiap penandaan menghasilkan satu baris `LabTransitionHistory` berlingkup `LabExamination` yang menunjuk pemeriksaannya | **`SELESAI`** |
| `FR-01.4` | `LAB-DEC-013` | `02-backend-architecture.md` §4.4 | `LAB-API-v1` r3 | `BE-LAB-02` | `FE-LAB-02` | Kolom penyimpanannya ada — `LabValueBound.CitoTurnaroundMinutes`, [`task/report/backend/BE-LAB-02.md`](../task/report/backend/BE-LAB-02.md) bagian 3.2 dan 5.1. `AC-17` **terbukti 2026-09-04** lewat [`BE-LAB-14.md`](../task/report/backend/BE-LAB-14.md): batas waktu itu kini benar-benar dipakai menghitung keterlambatan | **`SELESAI`** |

### `EPIC-LAB-02` — Pemisahan wadah fisik dan pemeriksaan terpesan

| FR | Keputusan | Desain | Kontrak | Task BE | Task FE | Bukti | Status |
|---|---|---|---|---|---|---|---|
| `FR-02.1` | `LAB-DEC-024` | `02-backend-architecture.md` §4.3 | `LAB-API-v1` r3 | `BE-LAB-09`, `BE-LAB-16` | `FE-LAB-07` | `AC-35` **terbukti ujung-ke-ujung** — struktur pada [`BE-LAB-09.md`](../task/report/backend/BE-LAB-09.md) bagian 6.1, dan endpointnya pada [`BE-LAB-16.md`](../task/report/backend/BE-LAB-16.md) bagian 6.1: dua pemeriksaan ditambahkan lewat `POST /by-order` lalu keduanya terbaca lewat `GET /by-specimen` dengan satu barcode yang sama. `VAL-17` .. `VAL-20` masing-masing punya ujinya | **`SELESAI`** |
| `FR-02.2` | `LAB-DEC-024` | `contracts/state-transition-matrix.md` | `LAB-STATE-v1` r2 | `BE-LAB-12`, `BE-LAB-16` | `FE-LAB-07` | `AC-36` **terbukti** — [`BE-LAB-12.md`](../task/report/backend/BE-LAB-12.md) bagian 6.1; menolak wadah menggugurkan kedua pemeriksaan yang ditopangnya. `AC-37` terbukti pada tingkat status; penerbitan faktanya menunggu `BE-LAB-13` | **`SELESAI SEBAGIAN`** — status selesai `BE-LAB-12`; fakta per pemeriksaan menunggu `BE-LAB-13` |
| `FR-02.3` | `LAB-DEC-024` | `VAL-13` | `LAB-VAL-v1` r3 | `BE-LAB-12` | `FE-LAB-07` | `AC-36` **terbukti** — `VAL-13` ditegakkan secara struktural: tidak ada satu pun jalur pengubah yang menolak sebagian pemeriksaan | **`SELESAI`** |
| `FR-02.4` | `LAB-DEC-024` | `erd/data-dictionary.md` | — | `BE-LAB-11` | — | `AC-35` — keenam kolom lepas dari `LabSpecimen` dan utuh pada `LabExamination`, dibuktikan `LabSpecimenColumnSplitTests` 4/4; [`BE-LAB-11.md`](../task/report/backend/BE-LAB-11.md) | **`SELESAI`** |
| `FR-02.5` | `LAB-DEC-024` | `VAL-14` | `LAB-VAL-v1` r3 | `BE-LAB-12` | `FE-LAB-07` | `VAL-14` dan `VAL-15` **terbukti** — [`BE-LAB-12.md`](../task/report/backend/BE-LAB-12.md) bagian 5 | **`SELESAI`** |
| `FR-02.6` | `LAB-DEC-024` | `02-backend-architecture.md` §6 | — | `BE-LAB-11` | — | `AC-35`, `AC-38` — migration `SplitLabSpecimenIntoExamination` terbukti **dua arah** terhadap `QuilvianNewDevYoga`; [`BE-LAB-11.md`](../task/report/backend/BE-LAB-11.md) bagian 5.3 | **`SELESAI`** |

### `EPIC-LAB-03` — Batas nilai dan persetujuan klinis

| FR | Keputusan | Desain | Kontrak | Task BE | Task FE | Bukti | Status |
|---|---|---|---|---|---|---|---|
| `FR-03.1` | `LAB-DEC-006`, `LAB-DEC-018` | `02-backend-architecture.md` §4.4 | `LAB-API-v1` r3 | `BE-LAB-02`, `BE-LAB-04` | `FE-LAB-02` | `AC-24` **terbukti** — [`BE-LAB-02.md`](../task/report/backend/BE-LAB-02.md) bagian 6.1 untuk penyimpanannya, [`BE-LAB-04.md`](../task/report/backend/BE-LAB-04.md) bagian 6.1 untuk endpointnya; tiga baris Hemoglobin dibuat lewat `POST` dan terbaca lewat `GET /` | **`SELESAI`** |
| `FR-03.2` | `LAB-DEC-021` | `02-backend-architecture.md` §4.5 | `LAB-VAL-v1` r3 `VAL-22` .. `VAL-24` | `BE-LAB-02`, `BE-LAB-04` | `FE-LAB-02` | `AC-28` **terbukti** — [`BE-LAB-04.md`](../task/report/backend/BE-LAB-04.md) bagian 5; `VAL-22`, `VAL-23`, dan `VAL-24` masing-masing punya ujinya | **`SELESAI`** |
| `FR-03.3` | `LAB-DEC-023` | — | `LAB-API-v1` r3 | `BE-LAB-04` | `FE-LAB-02` | `AC-33` bagian "batas normal langsung berlaku" **terbukti** — [`BE-LAB-04.md`](../task/report/backend/BE-LAB-04.md) bagian 6.1 | **`SELESAI`** |
| `FR-03.4` | `LAB-DEC-023` | `02-backend-architecture.md` §4.6 | `LAB-API-v1` r3, `VAL-28`, `VAL-32`, `VAL-33` | `BE-LAB-03`, `BE-LAB-05` | `FE-LAB-02` | `AC-33` **terbukti seluruh jalur** — [`BE-LAB-05.md`](../task/report/backend/BE-LAB-05.md) bagian 6.1; `VAL-31` .. `VAL-35` masing-masing punya ujinya, dan `VAL-33` dibuktikan dua arah (menyetujui maupun menolak pengajuan sendiri) | **`SELESAI`** — dibangun; belum dapat dipakai sampai peran `LabCriticalBound : Approve` ditetapkan manajemen |
| `FR-03.5` | `LAB-DEC-023` | `02-backend-architecture.md` §4.7 | `LAB-STATE-v1` r2 | `BE-LAB-03`, `BE-LAB-04` | `FE-LAB-02` | `AC-34` **terbukti** — riwayat tersimpan ([`BE-LAB-03.md`](../task/report/backend/BE-LAB-03.md)) dan diterbitkan serta dibaca lewat endpoint ([`BE-LAB-04.md`](../task/report/backend/BE-LAB-04.md) bagian 6.1) | **`SELESAI`** |
| `FR-03.6` | `LAB-DEC-006` | `erd/data-dictionary.md` | — | `BE-LAB-02` | — | `AC-25` **terbukti** — [`task/report/backend/BE-LAB-02.md`](../task/report/backend/BE-LAB-02.md) bagian 6.1; nol kolom operasional laboratorium pada model EF, dan jumlah kolom `MstProcedure` tetap 35 pada keempat titik pengukuran migration, bagian 5.1 | **`SELESAI`** |

### `EPIC-LAB-04` — Daftar kerja dan pemantauan keterlambatan

| FR | Keputusan | Desain | Kontrak | Task BE | Task FE | Bukti | Status |
|---|---|---|---|---|---|---|---|
| `FR-04.1` | `LAB-DEC-013` | `LAB-FE-006` | `LAB-API-v1` r3 | `BE-LAB-14` | `FE-LAB-08` | `AC-10` dan `AC-39` **terbukti** — [`BE-LAB-14.md`](../task/report/backend/BE-LAB-14.md); satu cito pukul 10.05 di atas empat belas pesanan biasa pukul 10.00 | **`SELESAI`** |
| `FR-04.2` | `LAB-DEC-013` | — | `LAB-API-v1` r3 | `BE-LAB-14` | `FE-LAB-08` | `AC-17` **terbukti** pada kedua jalurnya, termasuk `VAL-39` | **`SELESAI`** |
| `FR-04.3` | `LAB-DEC-013` | `contracts/state-transition-matrix.md` | `LAB-STATE-v1` r2 | `BE-LAB-14` | `FE-LAB-08` | `AC-17` **terbukti** — keterlambatan dihitung sejak wadah dinyatakan layak; wadah yang belum diputuskan tidak pernah terhitung terlambat | **`SELESAI`** |
| `FR-04.4` | `LAB-DEC-013` | `02-backend-architecture.md` | — | `BE-LAB-14` | — | Tinjauan struktur **terbukti** — nol entity ber-nama `Worklist` dan nol jalur tulis pada grupnya | **`SELESAI`** |

### `EPIC-LAB-05` — Fakta kelayakan tagih per pemeriksaan

| FR | Keputusan | Desain | Kontrak | Task BE | Task FE | Bukti | Status |
|---|---|---|---|---|---|---|---|
| `FR-05.1` | `LAB-INH-013` | `CAP-11` | `LAB-INT-v1` r3 `INT-01` | `BE-LAB-13` | — | `AC-37` **terbukti** — [`BE-LAB-13.md`](../task/report/backend/BE-LAB-13.md) bagian 5: wadah dua pemeriksaan menerbitkan dua fakta dengan tarif masing-masing | **`SELESAI`** |
| `FR-05.2` | `LAB-INH-013` | `CAP-11` | `LAB-INT-v1` r3 | `BE-LAB-13` | — | `AC-37` — satuan `SourceItemId` berpindah ke `LabExamination.Id`; producer, enum, dan jalur dispatch tidak disentuh | **`SELESAI`** |
| `FR-05.3` | `LAB-INH-013` | `CAP-11` `Ready to reuse` | `LAB-INT-v1` r3 | `BE-LAB-13` | — | `AC-12` **terbukti** — menekan layak dua kali tetap menghasilkan dua fakta dengan identitas yang sama persis | **`SELESAI`** |
| `FR-05.4` | `LAB-DEC-011` | `CAP-12` | — | `BE-LAB-13` | — | `AC-13` **terbukti 2026-09-04** — `LaboratoryAuthorityTests` 18/18, nol properti dan nol method finansial pada Laboratorium | **`SELESAI`** |

### `EPIC-LAB-06` — Pengelolaan alasan penolakan

| FR | Keputusan | Desain | Kontrak | Task BE | Task FE | Bukti | Status |
|---|---|---|---|---|---|---|---|
| `FR-06.1` | `LAB-DEC-019` | `CAP-05` | `LAB-API-v1` r3 | `BE-LAB-06` | `FE-LAB-03` | `AC-26` **terbukti** — [`BE-LAB-06.md`](../task/report/backend/BE-LAB-06.md) bagian 6.1; alasan "Sampel tidak diberi label" ditambahkan lewat `POST /` lalu langsung terbaca petugas, dan penonaktifannya terbukti lewat `PUT /{id}/activation` | **`SELESAI`** |
| `FR-06.2` | `LAB-DEC-019` | `LAB-FE-012`, `VAL-37` | `LAB-PERM-v1` r3 | `BE-LAB-06` | `FE-LAB-03` | `AC-26` jalur gagal **terbukti** — [`BE-LAB-06.md`](../task/report/backend/BE-LAB-06.md) bagian 5; `VAL-37` diuji tiga arah: penanda kesalahan internal, penanda wajib catatan, dan sifat menyeluruh penolakannya. Hak akses `LabRejectionReason : SystemFlag` berdiri terpisah dari `: Update` | **`SELESAI`** — dibangun; penanda biaya belum dapat disetel sampai pemegang `SystemFlag` ditetapkan manajemen |
| `FR-06.3` | `LAB-DEC-019` | `CAP-05` | — | `BE-LAB-06` | — | Data awal terisi **terbukti** — [`BE-LAB-06.md`](../task/report/backend/BE-LAB-06.md) bagian 5; `LabRejectionReasonSeeder` mengisi sepuluh alasan baseline, tidak menimpa keputusan pengguna, dan memakai identitas yang sama dengan migration `20260824091610` | **`SELESAI`** |

### `EPIC-LAB-08` — Pendaftaran pasien datang langsung dan rujukan luar

| FR | Keputusan | Desain | Kontrak | Task BE | Task FE | Bukti | Status |
|---|---|---|---|---|---|---|---|
| `FR-08.1` | `LAB-DEC-032` | `CAP-09` | `LAB-API-v1` r3 | `BE-LAB-08` | `FE-LAB-05` | `AC-44` **terbukti** — pendaftaran datang langsung membentuk kunjungan ber-`IsWalkIn` benar dan bersumber `WalkIn`; [`BE-LAB-08.md`](../task/report/backend/BE-LAB-08.md) | **`SELESAI`** — sisi layar terbukti 2026-09-07, [`FE-LAB-05.md`](../task/report/frontend/FE-LAB-05.md) |
| `FR-08.2` | `LAB-DEC-032` | `LAB-INT-v1` r3 `INT-05` | `LAB-INT-v1` r3 | `BE-LAB-08`, `BE-EXT-03` | `FE-LAB-05` | `AC-45` **terbukti dua kali** — telusur source modul Laboratorium nol penulisan, dan uji dua penyimpanan terpisah; [`BE-LAB-08.md`](../task/report/backend/BE-LAB-08.md) | **`SELESAI`** — sisi layar terbukti 2026-09-07, [`FE-LAB-05.md`](../task/report/frontend/FE-LAB-05.md) |
| `FR-08.3` | `LAB-DEC-035` | `erd/data-dictionary.md` §9b | — | `BE-EXT-02`, `BE-EXT-03`, `BE-LAB-08` | `FE-LAB-05` | `AC-46` dan `AC-50` **terbukti** — rujukan luar menyimpan kedua penunjuk beserta nomor surat, dan permintaannya tidak punya ruas nama perujuk sama sekali; [`BE-LAB-08.md`](../task/report/backend/BE-LAB-08.md) | **`SELESAI`** — sisi layar terbukti 2026-09-07, [`FE-LAB-05.md`](../task/report/frontend/FE-LAB-05.md) |
| `FR-08.4` | `LAB-DEC-032` | `INT-05` idempotensi | `LAB-INT-v1` r3 | `BE-EXT-03`, `BE-LAB-08` | — | `AC-44` **terbukti** — kunci sama dikirim dua kali menghasilkan satu kunjungan; ditegakkan unique index tersaring, bukan kode aplikasi; [`BE-LAB-08.md`](../task/report/backend/BE-LAB-08.md) | **Selesai** |
| `FR-08.5` | `LAB-DEC-032` | `INT-05` perilaku tolak | `LAB-INT-v1` r3 | `BE-LAB-08` | `FE-LAB-05` | `AC-45` **terbukti** — penolakan Registrasi diteruskan apa adanya dan nol baris tersisa, termasuk sumber pembayaran; [`BE-LAB-08.md`](../task/report/backend/BE-LAB-08.md) | **`SELESAI`** — sisi layar terbukti 2026-09-07, [`FE-LAB-05.md`](../task/report/frontend/FE-LAB-05.md) |

### `EPIC-LAB-09` — Katalog, harga, dan cakupan penjamin

| FR | Keputusan | Desain | Kontrak | Task BE | Task FE | Bukti | Status |
|---|---|---|---|---|---|---|---|
| `FR-09.1` | `LAB-DEC-036` | `CAP-06` | `LAB-API-v1` r3 | `BE-LAB-07`, `BE-EXT-01` | `FE-LAB-04` | `AC-43` **terbukti** — tiga pemeriksaan menampilkan harga satuan dan total tanpa satu baris tagihan pun terbentuk; [`BE-LAB-07.md`](../task/report/backend/BE-LAB-07.md). Sisi layar **terbukti 2026-09-04** — [`FE-LAB-04.md`](../task/report/frontend/FE-LAB-04.md). | **`SELESAI`** |
| `FR-09.2` | `LAB-DEC-033` | `CAP-10` | `LAB-API-v1` r3 | `BE-LAB-07` | `FE-LAB-04` | `AC-43` **terbukti** — harga berasal dari `MstTariff` dengan aturan berlaku yang sama persis dengan jalur pemesanan; [`BE-LAB-07.md`](../task/report/backend/BE-LAB-07.md). Sisi layar **terbukti 2026-09-04** — [`FE-LAB-04.md`](../task/report/frontend/FE-LAB-04.md). | **`SELESAI`** |
| `FR-09.3` | `LAB-DEC-033` | `MstInsuranceTariff` | `LAB-INT-v1` r3 `INT-06` | `BE-LAB-07` | `FE-LAB-04` | `AC-43` **terbukti** — harga kontrak penjamin tampil bila ada, dan ketiadaannya ditandai tidak tercakup, bukan gratis; [`BE-LAB-07.md`](../task/report/backend/BE-LAB-07.md). Sisi layar **terbukti 2026-09-04** — [`FE-LAB-04.md`](../task/report/frontend/FE-LAB-04.md). | **`SELESAI`** |
| `FR-09.4` | `LAB-DEC-033` | `VAL-50` | `LAB-API-v1` r3 | `BE-LAB-07` | `FE-LAB-04` | `AC-47` dan `AC-48` **terbukti** — nol entity tarif milik Laboratorium, dan nol jalur ubah pada grup katalog; [`BE-LAB-07.md`](../task/report/backend/BE-LAB-07.md). Sisi layar **terbukti 2026-09-04** — [`FE-LAB-04.md`](../task/report/frontend/FE-LAB-04.md). | **`SELESAI`** |
| `FR-09.5` | `LAB-DEC-036` | `INV-22`, `VAL-46` | `LAB-VAL-v1` r3 | `BE-LAB-07` | — | `AC-51` **terbukti** — menambahkan Hemoglobin ke pesanan Mikrobiologi ditolak `422` `VAL-46`; [`BE-LAB-07.md`](../task/report/backend/BE-LAB-07.md). Penegakannya menuntut kedua disiplin diketahui, sehingga pengisian nilai disiplin katalog masih menentukan seberapa luas ia berlaku | **`SELESAI`** — cakupannya bergantung pada pengisian nilai disiplin |

### `EPIC-LAB-10` — Monitoring per disiplin

| FR | Keputusan | Desain | Kontrak | Task BE | Task FE | Bukti | Status |
|---|---|---|---|---|---|---|---|
| `FR-10.1` | `LAB-DEC-025` | `03-domain-architecture.md` `S15` | `LAB-API-v1` r3 | `BE-LAB-15` | `FE-LAB-09` | `AC-41` **terbukti** — [`BE-LAB-15.md`](../task/report/backend/BE-LAB-15.md); tiga daftar dengan data campuran, tidak satu baris pun menyeberang | **`SELESAI`** |
| `FR-10.2` | `LAB-DEC-025` | — | `LAB-API-v1` r3 | `BE-LAB-15` | `FE-LAB-09` | `AC-41` **terbukti pada kedua sisinya** — backend: penyaring ditulis satu kali dan dipakai ketiga jalur, disiplin bukan ruas penyaring. Layar: ketiga disiplin menunjuk **objek definisi penyaring yang sama**, dan ujinya memeriksa identitas objeknya sehingga percabangan pertama langsung tertangkap; [`FE-LAB-09.md`](../task/report/frontend/FE-LAB-09.md) | **`SELESAI`** |
| `FR-10.3` | `LAB-DEC-025` | `erd/data-dictionary.md` | `LAB-API-v1` r3 | `BE-LAB-01` | — | `AC-11` **terbukti**, `AC-41` terbukti sebagian — [`task/report/backend/BE-LAB-01.md`](../task/report/backend/BE-LAB-01.md) bagian 6; migration terbukti dua arah pada `QuilvianNewDevYoga`, bagian 5.1 | **`SELESAI`** |
| `FR-10.4` | `LAB-DEC-064`, `LAB-DEC-071`, `LAB-DEC-074` | `06-readiness-assessment.md` `C-02` | **Nol perubahan kontrak** | — | `FE-LAB-18` | `AC-98` dan `AC-99` **terbukti** — [`FE-LAB-18.md`](../task/report/frontend/FE-LAB-18.md): 999/999 uji unit lulus, build produksi lulus, lint nol tambahan. Termasuk **pembuktian terbalik** bahwa 132 pemakai lain `FilterDatePicker` tidak berubah perilakunya | **`SELESAI`** |

### `EPIC-LAB-07` — Layar Laboratorium

| FR | Keputusan | Desain | Kontrak | Task BE | Task FE | Bukti | Status |
|---|---|---|---|---|---|---|---|
| `FR-07.1` | `LAB-DEC-010` | `03-frontend-architecture.md` §3.1 | `LAB-API-v1` r3 | — | `FE-LAB-06` | `AC-18` dan `AC-43` **terbukti di sisi layar 2026-09-04** — [`FE-LAB-06.md`](../task/report/frontend/FE-LAB-06.md): penanda cito melekat pada baris pemeriksaan dan dijaga uji unit, dan harga tampil saat memesan lewat komponen pemilih katalog. | **`SELESAI`** — kontrol cito belum dapat disembunyikan dari dokter yang bukan pemesan, karena respons pesanan tidak membawa `requestedByUserId` |
| `FR-07.2` | `LAB-DEC-010` | §3.2, `LAB-FE-009`, `LAB-FE-010` | `LAB-API-v1` r3 | — | `FE-LAB-07` | `AC-36` **terbukti pada tingkat aturan dan uji** — `LAB-FE-009` berlapis tiga dan `LAB-FE-010` muncul sebelum konfirmasi; sembilan belas uji, termasuk penjaga ketiadaan aksi berlingkup pemeriksaan (`VAL-13`); [`FE-LAB-07.md`](../task/report/frontend/FE-LAB-07.md) | **`SELESAI`** — verifikasi manual menunggu backend dijalankan |
| `FR-07.3` | `LAB-DEC-010` | §3.3, `LAB-FE-006` | `LAB-API-v1` r3 | — | `FE-LAB-08` | `AC-10` dan `AC-17` **terbukti pada tingkat aturan dan uji** — `LAB-FE-006` ditegakkan dua lapis, dan ujinya menelusuri **seluruh** pilihan pengurutan yang ditawarkan layar sehingga tidak satu pun dapat menggeser cito ke bawah; `VAL-39` dijaga uji tersendiri; [`FE-LAB-08.md`](../task/report/frontend/FE-LAB-08.md) | **`SELESAI`** — verifikasi manual menunggu backend dijalankan |
| `FR-07.4` | `LAB-DEC-010` | §3.4, `LAB-FE-011`, `LAB-FE-013` | `LAB-API-v1` r3 | — | `FE-LAB-02` | `AC-33` **terbukti di sisi layar 2026-09-04** — [`FE-LAB-02.md`](../task/report/frontend/FE-LAB-02.md): formulir ubah tidak punya satu pun isian batas kritis, payload menyalin batas yang berlaku, dan jalur pengajuan berdiri sebagai route tersendiri. `LAB-FE-013` terbukti lewat uji unit `S2` | **`SELESAI`** — `AC-34` terpenuhi sebagian: riwayat belum memuat pelaku karena respons backend tanpa nama |
| `FR-07.5` | `LAB-DEC-010` | §3.5, `LAB-FE-012` | `LAB-API-v1` r3 | — | `FE-LAB-03` | `AC-26` **terbukti di sisi layar 2026-09-04** — [`FE-LAB-03.md`](../task/report/frontend/FE-LAB-03.md): kedua penanda sistem tidak pernah menjadi isian maupun ikut pada payload, tampil terkunci beserta nilainya, dan selisih ruas terkunci yang diumumkan backend terbaca sebagai peringatan. Kelima baris `AC-26` punya jawabannya di layar | **`SELESAI`** — penyembunyian aksi penanda sistem masih sedekat peran, karena frontend belum menerima daftar permission |

---

### `EPIC-LAB-11` — Penerimaan Sampling/Specimen

Ditambahkan 2026-09-14. Gelombang `MVP-5a`.

| FR | Keputusan | Desain | Kontrak | Task BE | Task FE | Bukti | Status |
|---|---|---|---|---|---|---|---|
| `FR-11.1` | `LAB-DEC-040` | `02-backend-architecture.md` §11.3; `erd/data-dictionary.md` §12.1 | `LAB-API-v1` **r8**, `LAB-VAL-v1` r4, `LAB-PERM-v1` rev 4 | `BE-LAB-20` ✅, `BE-LAB-21` ✅ kode | `FE-LAB-10`, `FE-LAB-11` | `AC-58` **terpenuhi pada source, keduanya.** Sisi data induk **diterapkan ke `QuilvianNewDevYoga`**: 7 baris baseline terisi, kedua index unik parsial terbaca, `VAL-61` dan `VAL-62` ditolak `23505`, jalur `Down` lalu `Up` dibuktikan ([`BE-LAB-20.md`](../task/report/backend/BE-LAB-20.md)). Sisi wadah kini menolak teks bebas: `VAL-51`, `VAL-52`, `VAL-55` pada `LabSpecimenService.cs:1288-1321`, dan `AC-59` terpenuhi `VAL-53`/`VAL-54` pada `:1323-1338` ([`BE-LAB-21.md`](../task/report/backend/BE-LAB-21.md)). `VAL-63` tetap terverifikasi source; pembuktian runtime kedua task menunggu migration `BE-LAB-21` diterapkan. **Sisi layar pengelolaannya selesai 2026-09-16** oleh `FE-LAB-10`: daftar, formulir tambah, dan formulir ubah berdiri di `master-data/lab-specimen-types/` — bukan di folder laboratorium, sesuai `AC-49`. **`VAL-63` kini juga terbukti runtime**, dari dua arah sekaligus: `PUT /{id}/activation` yang menonaktifkan baris `Lainnya` terakhir ditolak **`HTTP 422`** oleh backend yang benar-benar berjalan dan **nol baris berubah** sesudahnya, sementara di layar tombolnya tampil mati beserta kalimat sebab yang **sama kata demi kata**. Penanda `Lainnya` dikunci di layar sesuai `VAL-62` dan nol pernah ikut pada payload. [`FE-LAB-10.md`](../task/report/frontend/FE-LAB-10.md) | **`SELESAI`** |
| `FR-11.2` | `LAB-DEC-040` butir 4-5 | `02-backend-architecture.md` §11.7 | `LAB-API-v1` r7 `GET /other-usage` — **Tersedia** | `BE-LAB-25` ✅ | `FE-LAB-10` | **`AC-60` terpenuhi dan terbukti terhadap database.** `T-60d` dijalankan pada `QuilvianNewDevYoga`: tiga wadah `cairan kista` menjadi **satu baris berjumlah tiga** dengan waktu pemakaian terakhir yang benar, sementara `Cairan Kista` **tetap baris tersendiri** — ejaan tidak digabung diam-diam. `ToQueryString()` membuktikan pengelompokannya satu `GROUP BY` penuh di PostgreSQL, nol evaluasi sisi klien. **Nol tabel ringkasan**, nol migration; rekapnya berubah seketika. Nol baris uji tertinggal; [`BE-LAB-25.md`](../task/report/backend/BE-LAB-25.md). **Sisi layar selesai 2026-09-16** oleh `FE-LAB-10`: layar daftar pantau berdiri di `master-data/lab-specimen-types/other-usage`, memanggil `GET /other-usage`, menampilkan keterangan **apa adanya tanpa disatukan** beserta jumlah, pertama, dan terakhir dipakai, serta membawa aksi menaikkan keterangan menjadi jenis tetap dengan **hanya nama** yang dibawa — kodenya tetap diketik kepala instalasi. **Satu batas disebut apa adanya:** isinya nol hari ini karena baris uji `T-60d` memang dihapus kembali, dan wadah berjenis `Lainnya` yang sungguhan baru lahir dari `FE-LAB-11`; perilaku layarnya karena itu dibuktikan dengan jawaban yang dipasang, bukan dengan data nyata. [`FE-LAB-10.md`](../task/report/frontend/FE-LAB-10.md) | **`SELESAI`** |
| `FR-11.3` | `LAB-DEC-041` | `erd/data-dictionary.md` §12.2 | `LAB-API-v1` r7, `LAB-VAL-v1` r4 `VAL-56`, `VAL-57` | `BE-LAB-21` ✅ | `FE-LAB-11` | `AC-62` terpenuhi `VAL-56` pada `LabSpecimenService.cs:1343`; `VAL-57` pada `:1350-1366`. **`AC-63` terpenuhi dan terbukti:** penelusuran seluruh rujukan `VolumeAmount` menemukan **nol** pembandingan terhadap batas apa pun, sehingga `RULE-021` tegak (`T-63c`). Kolom `numeric(12,3)` berdiri beserta FK `RESTRICT` ke `MstMeasurement`. Migration **diterapkan ke `QuilvianNewDevYoga` 2026-09-15**, `T-M4` ditolak `23503` dan jalur `Down` terbukti. **`mL` dan `gram` sudah tersedia**, sehingga volume dapat dipakai sekarang; [`BE-LAB-21.md`](../task/report/backend/BE-LAB-21.md) | **`SELESAI`** |
| `FR-11.4` | `LAB-DEC-041` | `erd/data-dictionary.md` §12.3 | — | `BE-LAB-21` ✅ | `FE-LAB-11` | `AC-64` **terpenuhi pada kode, dapat dipakai sebagian.** Tidak ada satu pun jalur kode yang membatasi satuan menurut jenis specimen, sehingga jaringan bebas memakai `gram`, `blok`, atau `slide`. **Datanya tersedia sebagian:** `gram` sudah ada dan ber-`IsForLaboratory`, sehingga jaringan dapat menyimpan volumenya sekarang; `blok` dan `slide` **belum ada**, dan hanya keduanya yang tertahan `DATA-MST-MEASUREMENT`. Lihat §4 dan [`BE-LAB-21.md`](../task/report/backend/BE-LAB-21.md) §7 | **`SELESAI SEBAGIAN`** — `gram` tersedia; `blok` dan `slide` menunggu Master Data |
| `FR-11.5` | `LAB-DEC-042` | `02-backend-architecture.md` §11.4; `erd/data-dictionary.md` §12.2 | `LAB-API-v1` r7, `LAB-VAL-v1` r4 `VAL-58`, **`VAL-59` terbukti tidak dapat dilaksanakan** | `BE-LAB-22` ◐ | `FE-LAB-12` | **`AC-65` terbukti secara struktural** — nol ruas permintaan bernama `ReceivedAt` pada seluruh DTO Laboratorium, satu-satunya jalur tulisnya diisi server (`LabSpecimenService.cs:1583`). **`AC-17` terbukti dua kali** — nol rujukan `PhysicallyReceivedAt` pada ketiga berkas perhitungan cito, dan nol diff pada ketiganya. `AC-67` terpenuhi pada source: rekap beralih ke waktu nyata (`:919-931`), selisih tercatat pada jejak audit (`:1507-1508`). **`AC-66` terpenuhi separuh** — `VAL-58` tegak (`:1421-1425`), `VAL-59` tertahan kontrak; [`BE-LAB-22.md`](../task/report/backend/BE-LAB-22.md) §6 | **`SELESAI SEBAGIAN`** — `AC-66` menunggu keputusan pemilik modul |
| ~~`FR-11.6`~~ | ~~`LAB-DEC-038`~~ → `LAB-DEC-050` | `02-backend-architecture.md` §11.5 | `LAB-API-v1` **r9** — ruas `Quantity` dicabut | ~~`BE-LAB-23`~~ ⛔ | `FE-LAB-11` | **Dicabut 2026-09-14.** `AC-52` terbukti tidak dapat dipenuhi: index unik `(SpecimenId, ProcedureId)` atas dasar `BR-20` dan `AC-35` menolak baris kedua. Pemilik modul mencabut `LAB-DEC-038`; kolom Jumlah tidak dibuat. `AC-52`..`AC-54` dan `VAL-60` ikut dicabut; [`BE-LAB-23.md`](../task/report/backend/BE-LAB-23.md) | **`DICABUT`** |
| `FR-11.7` | `LAB-DEC-039`, `LAB-DEC-049` | `02-backend-architecture.md` §11.6 | `LAB-VAL-v1` r3 `VAL-18` — **tidak berubah** | `BE-LAB-24` ✅ | `FE-LAB-11` | **Keempat AC terverifikasi terhadap source 2026-09-14.** `AC-55a` menambah diizinkan sebelum kelayakan; `AC-55b`/`AC-57` ditolak sesudahnya lewat `VAL-18` pada `LabExaminationService.cs:123`; `AC-55c` pembatalan **tidak terkunci** — nol rujukan `SpecimenStatus` pada `CancelAsync`; `AC-56` nol route `process` bertingkat pemeriksaan. **Nol baris source diubah, dan memang tidak perlu**; [`BE-LAB-24.md`](../task/report/backend/BE-LAB-24.md) | **`SELESAI`** |
| `FR-11.8` | `LAB-DEC-045` | `03-frontend-architecture.md` §10 | `LAB-API-v1` r7 | — | `FE-LAB-11` ✅, `FE-LAB-12` ✅ | **Ketiganya terpenuhi, dan barisnya dikoreksi 2026-09-17.** `AC-75` — satu pasien rujukan luar diselesaikan dari identifikasi sampai penetapan kelayakan **tanpa berpindah menu**, wilayah A sampai E pada satu layar; [`FE-LAB-11.md`](../task/report/frontend/FE-LAB-11.md), dan penerimaannya dapat ditelusuri kembali lewat [`FE-LAB-12.md`](../task/report/frontend/FE-LAB-12.md). `AC-77` — penetapan kelayakan berdiri pada wilayah E beserta gerbangnya. `AC-76` sebagaimana diamandemen `LAB-DEC-048` — layar lama **tidak disentuh**, dibuktikan sebagai regresi: 12 dari 12 pemeriksaan lulus, nol berkas lama diubah. **Baris ini sempat tertinggal `BELUM DIMULAI` walaupun kedua task pelaksananya sudah `SELESAI`** — lihat revision 48 | **`SELESAI`** |
| `FR-11.2b` | `LAB-DEC-055`, `LAB-DEC-056`, `LAB-DEC-057` | `02-backend-architecture.md` §12; `erd/data-dictionary.md` §13 | `LAB-API-v1` **r10**, `LAB-VAL-v1` **r5** — `approved` 2026-09-15 | `BE-LAB-26` ✅, `BE-LAB-27` ✅, `BE-LAB-28` ✅, `BE-LAB-29` ✅ | `FE-LAB-14` | **`AC-83` ditegakkan backend** lewat `BE-LAB-29` — `LAB-CONFLICT-007` **ditutup**; jalur tulis `LabOrder` terbukti tepat satu di seluruh aplikasi. **`AC-91` DITUTUP** 2026-09-15 lewat `BE-LAB-28` — `VAL-68` menolak `422`, `VAL-69` menolak `409`, permintaan yang berwadah tertaut ke baris pemeriksaannya dan yang belum terbaca menunggu; pesanan lama terbukti tidak tersentuh beserta baris kontrolnya. Ia berdiri secara struktural lebih dulu lewat `BE-LAB-26`: tabel `LabOrderedProcedure` diterapkan ke `QuilvianNewDevYoga`, unique parsial ditolak `23505`, `Restrict` ditolak `23503` terisolasi, jalur `Down` terbukti. **`AC-86` dan `AC-88` terpenuhi dan terbukti** lewat `BE-LAB-27`: service dipanggil sungguhan terhadap database — 4 pemeriksaan lintas 3 disiplin menghasilkan 3 pesanan, sedisiplin berkumpul, cito melekat per pemeriksaan; endpoint lama tetap mengembalikan tepat satu pesanan dan nol baris terpesan. `VAL-64`..`67` keempatnya `422`. `AC-87` terpenuhi pada source tetapi **belum dapat diuji** — 10 dari 10 pemeriksaan sudah tergolong, sehingga tidak ada bahan ujinya. **Ruas `clinicalNote` dicabut** lewat `r11` pada 2026-09-15 — pemilik modul memilih mencabut karena nol peminta (`FE-LAB-14` tidak memuat kotak catatan klinis) dan nol dampak kode; [`BE-LAB-27.md`](../task/report/backend/BE-LAB-27.md). **Layarnya berdiri 2026-09-16 lewat `FE-LAB-14`:** panel sesi kiosk bertujuan Laboratorium, pemilih pemeriksaan jamak yang mengirim **satu** permintaan, dan pemberitahuan hasil pemecahan sebelum maupun sesudah simpan — nol kotak pilihan disiplin. Verifikasi dijalankan terhadap aplikasi yang benar-benar berjalan dan **menemukan dua cacat yang lolos dari lint, build, serta uji unit**; keduanya diperbaiki. [`FE-LAB-14.md`](../task/report/frontend/FE-LAB-14.md). **Dua hal tetap terbuka:** prasyarat konfigurasi izin `KioskScanSession : Read` bagi peran petugas lab, dan panel rincian sesudah simpan yang belum pernah dilihat pada layar sungguhan — logikanya teruji unit | **`SEBAGIAN` — backend dan layar selesai; sisa verifikasi layar dan satu prasyarat izin** |
| `FR-11.11` | `LAB-DEC-051`, `LAB-DEC-052`, `LAB-DEC-053`, `LAB-DEC-054`, `LAB-DEC-058`, `LAB-DEC-059` | `02-backend-architecture.md` §12.6 | Dikontrakkan di sisi `registration-management` di bawah `LAB-REQ-006` | `BE-EXT-04` ✅, `BE-EXT-04b` ✅, `BE-EXT-05` ✅ | `FE-LAB-13` ✅ | **`AC-93` terpenuhi dan terbukti dari kedua sisi.** Sisi data — `T-93a`: ke-16 sesi kiosk tetap terbaca sesudah dua kolom nullable ditambahkan, 15 masih cocok ke pasien, nol kolom baru terisi; jalur `Down` terbukti. Jalur baca **dipakai ulang**, bukan dibuat baru: `GET /options?onlyUsableForRegistration=true` sudah ada dan tinggal ditambah penyaring tujuan; [`BE-EXT-04.md`](../task/report/backend/BE-EXT-04.md). **Diperbarui 2026-09-15:** `BE-EXT-04b` membuka jalur **tulis** kedua ruas itu — sebelumnya kolomnya berdiri tanpa satu pun cara mengisinya, terbukti pada 16 dari 16 sesi bernilai `null`. **Diperbarui 2026-09-16 — sisi layar berdiri:** `FE-LAB-13` mendirikan langkah Tujuan Layanan pada alur kiosk Pasien Lama, sehingga kedua ruas itu **akhirnya punya yang mengisinya**, dan `T-93b` kini terbukti dari layar — muatan cabang poliklinik diperiksa kunci demi kunci dan **nol ruas tambahan**, bukan berisi `null`; [`FE-LAB-13.md`](../task/report/frontend/FE-LAB-13.md). **Diperbarui 2026-09-17 — `AC-92` terpenuhi, dan kedua penahannya ternyata tidak pernah ada:** `LAB-OPEN-025` menyebut `EncounterIntakeService` yang bukan jalur kiosk — `POST /patient-encounters/kiosk` **sudah ada**, dijaga hanya `KioskReadPolicy`, nol memikul `[AccessPermission]`, dan sudah membentuk kunjungan beserta nomor antrean dari sesi kiosk; `LAB-OPEN-026` menyebut `IsAvailableForKiosk` yang **nol dibaca** jalur pembentukan kunjungan, sedangkan `IsAvailableForRegistration` pada `SU-LAB-001` bernilai `true`. `BE-EXT-05` karena itu hanya perlu membangun butir 4 `BR-46` — penutupan otomatis 21:00 WIB — dan **selektivitas penyaringnya diukur terhadap database sungguhan tanpa mengubah satu baris pun**: 0 sebagaimana ditulis, 14 tanpa klausa tujuan, 157 tanpa klausa kiosk yang 91 di antaranya `WaitingForNurse`; [`BE-EXT-05.md`](../task/report/backend/BE-EXT-05.md). **Empat cabang kemudian dibuktikan lewat aplikasi yang benar-benar menyala** — satu ditutup, **tiga bertahan** (punya `LabOrder`, bertujuan poliklinik, sudah `CheckedInAt`); 15 kunjungan kiosk nyata dan 91 `WaitingForNurse` tidak bergeser; idempotensi terbukti; nol baris uji tertinggal. **Uji itulah yang menemukan satu cacat:** aktor sistem `Guid.Empty` melanggar FK `NoShowByUserId`, sehingga penutupan gagal seluruhnya **tanpa satu pun tanda dari luar** — diperbaiki dengan menulis `null`, nilai yang memang diizinkan kolomnya | **`SELESAI`** |
| `FR-11.12` | `LAB-DEC-061` | `state-transition-matrix.md` §1a; `erd/data-dictionary.md` §1 | `LAB-API-v1` **r12** §7.1, `LAB-STATE-v1` **r3**, `LAB-VAL-v1` **r6** — `approved` 2026-09-15 | `BE-LAB-30` ✅, `BE-LAB-31` ✅ | `FE-LAB-15` ⛔ | **Menutup sebagian `LAB-P0-002`** — posisi `Confirmed` ditetapkan antara `Requested` dan `Accepted` sesudah terbuka sejak 2026-09-01. `AC-94`, `AC-95` beserta `T-94a`..`T-95b` ditulis. **`T-97c` menjaga klaim aditifnya:** jalur `Requested` → `Accepted` tanpa konfirmasi tetap sah, karena mewajibkan konfirmasi berarti menghentikan pesanan yang sedang berjalan (`LAB-OPEN-027`). **Prasyarat struktural berdiri 2026-09-16 lewat `BE-LAB-30`:** `LabOrderStatus.Confirmed` ada, dan ketiga kolom `ConfirmedByUserId`, `ConfirmedAt`, `ExaminerDoctorId` diterapkan ke `QuilvianNewDevYoga` — ketiganya `nullable` tanpa nilai bawaan, foreign key `Restrict` ke `MstDoctor` beserta indexnya berdiri, jalur `Down` lalu `Up` terbukti, dan **5 pesanan lama utuh dengan ketiga kolom barunya `null`**; [`BE-LAB-30.md`](../task/report/backend/BE-LAB-30.md). **Endpoint berdiri 2026-09-16 lewat `BE-LAB-31`:** `POST /lab-orders/{id}/confirm` sesuai `r12`; `VAL-70`..`VAL-73` **keempatnya terbukti menolak** sesuai matriks — `409`, `409`, `422`, `422` — konfirmator dan waktu terbukti **diturunkan server** dan ruasnya memang tidak ada pada DTO (`T-95b`), dan **`T-97c` terbukti dari database**: pesanan tanpa konfirmasi tetap mencapai `Accepted`. Satu celah gelombang ditutup di sini — turunan `Confirmed` → `Accepted` pada `LAB-STATE-v1` `r3` §1a yang tidak dimiliki `BE-LAB-30`, `BE-LAB-31`, maupun `BE-LAB-32`; tanpanya pesanan yang dikonfirmasi tidak akan pernah dapat diproses. [`BE-LAB-31.md`](../task/report/backend/BE-LAB-31.md). **`AC-94` dan `AC-95` tetap terpenuhi sebagian**, dan penyebabnya celah kontrak, bukan pekerjaan yang kurang: bagian "terbaca pada daftar" dan "tampil pada daftar serta ringkasan cetak" menuntut ruas respons yang **`r12` tidak pernah definisikan**, sehingga **`FE-LAB-15` ⛔ tertahan** menunggu `r13`. **Celah itu ditutup 2026-09-16:** `LAB-API-v1` **`r13` disetujui pemilik modul**, menambahkan kelima ruas respons yang dibutuhkan — lihat [`api-contract.md`](../contracts/api-contract.md) bagian 8 — dan diturunkan menjadi **`BE-LAB-33`** pada gelombang `MVP-5d`. Penahan `FE-LAB-15` kini **tinggal pelaksanaannya**, bukan lagi keputusan. **`BE-LAB-33` selesai pada hari yang sama:** kelima ruas terbaca dari database pada jalur daftar maupun detail, pesanan yang belum dikonfirmasi terbukti mengembalikan kelimanya `null`, dan nama konfirmator terbukti memakai jalur yang **sama** dengan `RequestedByName` — [`BE-LAB-33.md`](../task/report/backend/BE-LAB-33.md). **Seluruh sisi backend `FR-11.12` kini selesai dan terbukti; `FE-LAB-15` nol penahan**. **Layarnya berdiri 2026-09-16 lewat `FE-LAB-15`:** kolom Konfirmasi pada ketiga menu, pop-up berisi ringkasan pasien dan pemilih dokter pemeriksa, dan **konfirmasi kedua terbukti tidak mungkin dilakukan dari layar** — seluruhnya diperiksa dari DOM aplikasi yang benar-benar berjalan; [`FE-LAB-15.md`](../task/report/frontend/FE-LAB-15.md). **`AC-94` TERPENUHI PENUH.** **`AC-95` terpenuhi untuk bagian daftar**; bagian "tampil pada **ringkasan cetak**" tetap terbuka karena fitur cetaknya belum ada — `FE-LAB-17` ⛔. **Diperbarui 2026-09-16 — sebab tertahannya `FE-LAB-17` berpindah, dan ini pemindahan ketiga pada baris keluarga ini:** penahan tanda tangan **ditutup** — ketiganya dapat diisi sejak `r13`/`r14` — dan ketiga keputusan pemilik **ditetapkan**. Yang tersisa satu, dan baru ditemukan hari ini: **daftar pemeriksaan yang benar-benar dipesan tidak dikembalikan DTO maupun endpoint mana pun.** `LabOrderedProcedure` berdiri sejak `BE-LAB-26` dan terisi sejak `BE-LAB-27`, tetapi nol pembaca di luar backend; `LabOrder.ProcedureId` hanyalah **penunjuk wakil**. Mencetak sekarang berarti dokumen resmi yang menyebut satu pemeriksaan padahal pesanan gabungan `BR-47` memuat beberapa — sehingga pemilik modul memilih **menunda**. **Ditutup hari itu juga:** `r15` **disetujui** lalu dilaksanakan **`BE-LAB-35`** ✅ — `orderedProcedures` terbaca dari database lewat 12 pemeriksaan, dengan empat di antaranya sengaja menguji ketiadaan; [`BE-LAB-35.md`](../task/report/backend/BE-LAB-35.md). **`FE-LAB-17` kini nol penahan dan nol keputusan terbuka.** Satu peringatan diteruskan kepadanya: `LabOrderedProcedure` berisi **0 baris** pada database, sehingga seluruh 5 pesanan nyata menempuh jalur array kosong — jalur sah yang wajib mencetak `procedureName` sebagai isi lengkap. **Ditutup hari itu juga oleh `FE-LAB-17` ✅:** tombol Cetak per baris membuka pratinjau berisi daftar pemeriksaan terpesan dan tiga blok tanda tangan sejajar, dan **ketiga nama tanda tangan terbukti tercetak** pada pesanan yang sudah dikonfirmasi — 8 pemeriksaan layar, 8 lolos; [`FE-LAB-17.md`](../task/report/frontend/FE-LAB-17.md). **`AC-95` TERPENUHI PENUH** | **`SELESAI` — `AC-94` dan `AC-95` keduanya penuh** |
| `FR-11.13` | `LAB-DEC-063` | `state-transition-matrix.md` §1a | `LAB-API-v1` **r12** §7.2, `LAB-VAL-v1` **r6** — `approved` 2026-09-15 | `BE-LAB-32` ✅ | `FE-LAB-16` | `AC-96`, `AC-97` beserta `T-96a`, `T-97a`, `T-97b`. **Nol kolom baru:** alasan pembatalan sudah tersimpan sebagai `ReasonNote` pada `LabTransitionHistory` sejak jalur pembatalan dibangun — yang dibutuhkan hanya mewajibkan ruas yang hari ini opsional. **`VAL-75` satu-satunya pengetatan gelombang `MVP-5c`**, dan `T-97b` menjaga agar jalur yang sah tidak ikut tertutup. **Backend selesai 2026-09-16 lewat `BE-LAB-32`:** `VAL-74` menolak `422` dan `VAL-75` menolak `409` sesuai matriks; alasan terbaca kembali dari jejak audit dan terbukti dirapikan; **`T-97b` terbukti** — `Requested` dan `Confirmed` keduanya masih dapat dibatalkan, dan jejak konfirmasi tidak terhapus olehnya. **Hitungan dampak dilaporkan sebelum aturan ditegakkan**: 3 pesanan kehilangan jalur, tetapi 0 pembatalan pernah terjadi dan 0 layar memanggilnya. [`BE-LAB-32.md`](../task/report/backend/BE-LAB-32.md). **`AC-96` dan `AC-97` terpenuhi penuh** — keduanya tidak menuntut ruas respons baru, sehingga tidak tersentuh celah kontrak yang menahan `FE-LAB-15`. **Layarnya berdiri 2026-09-16 lewat `FE-LAB-16`:** pop-up beralasan, alert konfirmasi akhir berlabel `Konfirmasi Pembatalan`, dan aksi Batalkan yang disembunyikan pada status yang tidak sah — **menutup alert terbukti tidak mengirim permintaan apa pun**; [`FE-LAB-16.md`](../task/report/frontend/FE-LAB-16.md). **`AC-96` dan `AC-97` TERPENUHI PENUH** | **`SELESAI` — backend dan layar keduanya selesai dan terbukti** |
| `FR-11.8b` | `BR-50` butir 1; `LAB-DEC-065`, `LAB-DEC-071` | `05-evidence-reconciliation.md` §11.2 | `LAB-API-v1` **`r18`** | `BE-LAB-39` ✅ | `FE-LAB-20` ✅ | **Penyaring NIK dan Kategori Periode berdiri 2026-09-17.** Menutup `REC3-NEW-003` penuh dan `REC3-NEW-002` **sepenuhnya sejak `r23`** — pilihan ketiga `Tanggal Pemeriksaan` semula **sengaja tidak didirikan** karena `LabExamination` nol punya kolomnya, dan ketiadaannya **diuji sebagai gerbang**; gerbang itu dibuka `r23` sesudah **dua** syarat terpenuhi — kolomnya ada **dan** ada yang mengisinya. 20 pemeriksaan API, 1053/1053 uji unit, 4 pemeriksaan layar, 2 regresi — seluruhnya lulus. **Satu cacat yang sudah ada ditemukan dan diperbaiki:** penyaring tanggal ketiga layar Pemeriksaan menjawab `500` karena nilai `YYYY-MM-DD` — bentuk yang memang dikontrakkan dan memang dikirim layar — ditolak Npgsql; cabang `OrderDate` yang tidak disentuh `r18` gagal identik, jadi bukan regresi. Pencarian **satu hari** yang dituntut `LAB-DEC-071` sebelumnya mustahil dan kini terbukti; [`BE-LAB-39.md`](../task/report/backend/BE-LAB-39.md) | **`SELESAI`** |
| `FR-11.8c` | `BR-50` bagian 5.3 | `05-evidence-reconciliation.md` §11.2 | `LAB-API-v1` **`r19`** | `BE-LAB-40` ✅ | `FE-LAB-21` ✅ | **Ikon gender pada kolom Pasien berdiri 2026-09-17, menutup `REC3-NEW-010`.** **Catatan lamanya dikoreksi:** ia tercatat *"kewenangan UI, tidak menyentuh kontrak backend"*, padahal `LabMonitoringItemResponse` **nol** membawa gender — ikonnya mustahil dibangun dari sisi layar saja. **Artifact hanya menyebut dua warna dan diam soal dua nilai enum lainnya beserta `null`;** kediaman itu **diputuskan pemilik modul, bukan ditebak**: ikon netral berwarna sama untuk `Unknown` dan `NotDisclosed`, tetapi **labelnya dibedakan** — yang kedua adalah pilihan **sadar** pasien, bukan pengisian yang kurang, dan ada uji unit serta pemeriksaan layar yang menguji justru perbedaan label itu. Ikonnya `aria-hidden`; artinya dibawa teks, karena pada nada netral warnanya memang sama untuk dua hal berbeda. 1058/1058 uji unit, 4 pemeriksaan layar, 6 regresi; `gender` pada respons nyata **dicocokkan silang ke database**, 4 dari 4 pesanan cocok; [`BE-LAB-39.md`](../task/report/backend/BE-LAB-39.md) bagian 6 | **`SELESAI`** |
| `FR-11.8d` | `BR-50` bagian 5.5; `LAB-DEC-030` diamandemen `LAB-DEC-075` | `05-evidence-reconciliation.md` §11.2 | `LAB-API-v1` **`r20`** | `BE-LAB-42` ✅ | `FE-LAB-22` ✅ | **Label Lab dan Label Goldar berdiri 2026-09-17, menutup `REC3-NEW-009`.** **Penahannya ternyata keputusan, bukan teknis:** seluruh datanya sudah berdiri — `OrderNumber`, `orderedProcedures`, `MstPatient.BloodType`, pustaka barcode — dan yang menahannya `LAB-DEC-030` yang menempatkannya di Rilis 2. Pemilik modul memindahkannya ke Rilis 1. **Nota Lab ternyata sudah berdiri** sebagai "Ringkasan Pesanan Laboratorium" (`FE-LAB-17`) dengan isi yang sama, sehingga hanya dua label yang benar-benar baru. **Dua hal diuji sebagai "tidak boleh terjadi":** nomor order kosong **tidak** menghasilkan barcode — batang tak terpindai terlihat sah sampai dicoba — dan golongan darah tak diketahui **tidak** tercetak kosong, karena bagian kosong pada label tube tidak dapat dibedakan dari cetakan yang gagal. Kedua label terbukti **nol memanggil rincian pesanan**. 1067/1067 uji unit, 4 pemeriksaan layar, 10 regresi. **`LAB-OPEN-032` tetap terbuka** — ukurannya default berlabel belum dikonfirmasi, hidup sebagai satu konstanta; [`BE-LAB-39.md`](../task/report/backend/BE-LAB-39.md) bagian 7 | **`SELESAI`** |
| `FR-11.9` | `LAB-DEC-043` | `02-backend-architecture.md` §11.9 — **titik sambung, tanpa kontrak** | — | **tidak ada** | **tidak ada** | Terblokir `LAB-COORD-006` | **`OPEN DECISION`** |
| `FR-11.10` | `LAB-DEC-044` `amended`, `LAB-DEC-046`, `LAB-DEC-047` | `02-backend-architecture.md` §11.9 — **titik sambung, tanpa kontrak** | — | **tidak ada** | **tidak ada** | Terblokir `LAB-COORD-007` | **`OPEN DECISION`** |

**`FR-11.7` perlu dibaca dengan teliti.** Statusnya `SEBAGIAN SUDAH ADA`, bukan
`BELUM DIMULAI`. `VAL-18` sudah menegakkan penguncian pada jalur tambah sejak sebelum
`LAB-DEC-039` ditulis; keputusan itu menyamakan `AC-20` dengan kenyataan, bukan mengubah
perilaku. Yang tersisa adalah memeriksa jalur hapus dan menulis pengujian penjaganya.

**`FR-11.9` dan `FR-11.10` sengaja tidak punya kolom Task.** Keduanya berstatus `OPEN DECISION`
dan tidak diberi ID task sama sekali — bukan diberi ID lalu ditandai tertahan. ID task yang
sudah ada cenderung ikut masuk rencana kapasitas walaupun bertanda tertahan.

## 2. Rekapitulasi Cakupan

| Yang diperiksa | Jumlah | Tercakup | Keterangan |
|---|---:|---:|---|
| Functional requirement `FR-*` | 45 | **45** | Seluruhnya punya task backend, task frontend, atau keduanya |
| Acceptance criteria pada `testing/acceptance-test-matrix.md` | 30 | **30** | Naik dari 28 setelah `AC-11` dan `AC-19` ditambahkan 2026-09-02. Seluruhnya dirujuk minimal satu task |
| Epic dalam scope | 10 | **10** | `EPIC-LAB-01` .. `EPIC-LAB-10` |
| Slice dalam scope | 10 | **10** | `S1a`, `S2`, `S3`, `S7`, `S10`, `S11`, `S13a`, `S13b`, `S14`, `S15` |
| Endpoint To-Be pada `contracts/api-contract.md` | 37 | **37** | Diaudit 2026-09-02; empat endpoint Lab Examination semula tanpa pemilik, ditutup `BE-LAB-16` |
| Aturan validasi `VAL-*` | 50 | **50** | Seluruhnya berpemilik per bagian matriks. `VAL-09` — empat mata pada tingkat wadah — semula tidak dikutip task mana pun; kini tegas di `BE-LAB-12` |
| Entity pada `02-backend-architecture.md` §4 | 9 | **9** | Delapan berpemilik task; `TrxLabTransitionHistory` dipakai apa adanya tanpa pekerjaan struktur |
| Pasangan kewenangan `resource : action` | 29 | **29** | 16 baru dan berpemilik task; 13 sisanya sudah terdaftar pada `c87d9c0` |
| Kontrak integrasi `INT-*` | 6 | **6** | `INT-01`, `INT-05`, `INT-06` berpemilik task; `INT-02` sampai `INT-04` sudah berjalan dan tidak disentuh Rilis 1 |
| Task backend | 19 | — | 16 milik Laboratorium, 3 dependency eksternal |
| Task frontend | 9 | — | Seluruhnya milik Laboratorium |

---

## 3. Kemampuan Existing yang Dipakai Ulang

Delapan belas task di atas bersandar pada kemampuan yang **sudah ada dan terbukti**, bukan
dibangun ulang:

| Capability | Status | Dipakai oleh |
|---|---|---|
| `CAP-01` pesanan lab | `Extend` | `BE-LAB-01`, `BE-LAB-15` |
| `CAP-02` siklus hidup sampel | `Extend` | `BE-LAB-09`, `BE-LAB-12` |
| `CAP-04` riwayat perpindahan status | `Ready to reuse` | `BE-LAB-03`, `BE-LAB-10` |
| `CAP-05` alasan penolakan | `Reuse with adapter` | `BE-LAB-06` |
| `CAP-06` katalog `MstProcedure` | `Reuse with adapter` | `BE-LAB-07` |
| `CAP-08` kunjungan pasien | `Ready to reuse` | `BE-LAB-08`, `BE-LAB-15` |
| `CAP-09` identitas pasien dan dokter | `Ready to reuse` | `BE-LAB-08` |
| `CAP-10` tarif dan salinannya | `Ready to reuse` | `BE-LAB-07`, `BE-LAB-11` |
| `CAP-11` fakta kelayakan tagih | `Ready to reuse` | `BE-LAB-13` |
| `CAP-12` batas kewenangan finansial | `Ready to reuse` | `BE-LAB-13` |
| `CAP-13`, `CAP-14` kewenangan per aksi | `Ready to reuse` | `BE-LAB-04`, `BE-LAB-05`, `BE-LAB-06` |
| `CAP-15` identitas pelaku | `Ready to reuse` | `BE-LAB-05`, `BE-LAB-10` |
| `CAP-17` perlindungan konkurensi | `Ready to reuse` | `BE-LAB-03`, `BE-LAB-12` |
| `CAP-22` pola tujuh lapis frontend | `Ready to reuse` | `FE-LAB-01` — **terpakai** 2026-09-04. Tujuh lapis modul `laboratory-management` berdiri mengikuti pola yang sama, tanpa lapis kedelapan dan tanpa pola route baru. [`task/report/frontend/FE-LAB-01.md`](../task/report/frontend/FE-LAB-01.md) |
| `CAP-23` `axiosInstance` dan Redux | `Ready to reuse` | `FE-LAB-01` — **terpakai** 2026-09-04. `InstanceAxios` yang sudah ada dipakai apa adanya tanpa instance baru, dan potongan Redux `labOrder` terdaftar pada `store.jsx` mengikuti pola slice Health Services yang berjalan. [`task/report/frontend/FE-LAB-01.md`](../task/report/frontend/FE-LAB-01.md) |

**Satu kemampuan yang sengaja tidak dipakai ulang.** `CAP-16` — penegakan prinsip empat mata —
berstatus `Missing`, dan sistem permission yang ada **tidak dapat** menggantikannya:
`AccessPermissionService.HasAccessAsync` hanya menjawab boleh atau tidak, tidak pernah
membandingkan pelaku sebelumnya. `BE-LAB-05` wajib menegakkannya di dalam service.

---

## 4. Coverage Gap

### 4.1 Dua acceptance criteria dalam scope — ✅ **ditutup 2026-09-02**

| AC | Isi | Gap yang ditemukan | Yang dilakukan |
|---|---|---|---|
| `AC-11` | Pesanan lab dapat dibuat dari kunjungan Rawat Jalan, Rawat Inap, maupun IGD dengan alur kerja yang sama | Tercatat di decision log (`LAB-DEC-009`) tetapi tidak punya baris di `testing/acceptance-test-matrix.md`. `CAP-08` menyatakan kemampuannya sudah ada, tetapi `BE-LAB-01` menambah kolom pada `LabOrder` sehingga jalur pembuatan pesanan tersentuh | Bagian **1b. Alur Pemesanan Lintas Unit** ditambahkan ke matriks dengan empat skenario, termasuk pembuktian bahwa penambahan kolom disiplin tidak melahirkan cabang khusus per jenis kunjungan. `AC-11` menjadi bukti verifikasi `BE-LAB-01` |
| `AC-19` | Tidak ada satu pun tabel atau endpoint Laboratorium yang menyimpan stok, pembelian, atau pemakaian reagen | Penjaga batas scope sejenis `AC-42` Bank Darah, yang sudah punya baris uji. `AC-19` tidak punya | Ditambahkan sebagai uji unit penelusuran pada bagian 7e matriks, sejajar `AC-42`, dan menjadi bukti verifikasi `BE-LAB-15` |

Dengan keduanya ditutup, **seluruh acceptance criteria dalam scope kini punya baris uji.**

### 4.1b Gap yang dibuka amandemen 2026-09-14

| Gap | Isi | Kenapa tidak ditutup sekarang |
|---|---|---|
| Grafik urutan dependency gelombang lama | `backend-roadmap.md` dan `frontend-roadmap.md` tidak punya grafik urutan dependency untuk `MVP-0` sampai `MVP-3` — sembilan belas task backend dan sembilan task frontend | Kedua roadmap ditulis sebelum grafik itu menjadi kewajiban. Menurunkannya sekarang berarti menyimpulkan ulang dependency dari ingatan dokumen, dan itu berisiko keliru. Gelombang `MVP-5a` punya grafiknya sendiri |
| Verifikasi volume `BE-LAB-21` | **Dipersempit drastis 2026-09-15 setelah database dibaca langsung.** Dugaan bahwa `MstMeasurement` kosong dari satuan laboratorium **terbantah**: 17 baris ber-`IsForLaboratory` yang aktif sudah ada, dan **`mL` serta `gram` termasuk di dalamnya**. Volume bersatuan mililiter dan gram **dapat dipakai sekarang**. Yang tersisa hanya **tiga** satuan — `µL`, `blok`, `slide` — sehingga `AC-62` tidak lagi tertahan dan `AC-64` tertahan sebagian | Pengisian data induk global milik `master-data`. Permintaannya **perlu ditulis ulang**: tiga baris bukan lima, dan tanpa mendikte kode karena `MstMeasurement` memakai seri tergenerasi `STN`. Lihat `DATA-MST-MEASUREMENT` pada bagian 5 |
| Kelonggaran `IsForLaboratory` | **Baru 2026-09-15, temuan sampingan.** Penanda itu dibawa juga oleh `GALON`, `M3`, `KG`, `ONS`, `IU`, `mcg`, dan `Liter/Jam`, sehingga daftar pilihan satuan volume di layar penerimaan akan menawarkan **galon dan meter kubik** untuk sebuah tabung darah. Ada pula **dua baris gram bersimbol sama** (`GRAM` dan `GR`) | Kebersihan data induk global, milik `master-data`. **`VAL-57` sengaja tidak dipersempit** — penyaring tambahan berarti mengarang aturan yang tidak pernah diputuskan, dan justru akan menolak `blok` dan `slide` yang diminta `AC-64` |
| ~~Pembuktian runtime `BE-LAB-21` dan `BE-LAB-22`~~ | ✅ **Ditutup 2026-09-15.** Kedua migration diterapkan ke `QuilvianNewDevYoga` dalam satu jendela; `T-M2` dan `T-M4` (`23503`) terbukti, jalur `Down` lalu `Up` dibuktikan dengan verifikasi ulang yang tetap lulus, nol baris uji tertinggal. Butir DoD *"migration jalan maju dan mundur"* terpenuhi pada kedua task | Tersisa `T-58c`, `T-59c`, `T-61a`, `T-65a`, `T-67a`, `T-67b` yang memerlukan **aplikasi dijalankan**, bukan database. Bukan gap desain maupun gap kode |
| **`VAL-59` tidak dapat dilaksanakan** | **Baru 2026-09-15, gap desain yang sesungguhnya.** `VAL-59` membandingkan waktu penerimaan fisik terhadap waktu pengambilan, tetapi satu-satunya permintaan yang membawa waktu penerimaan fisik adalah `PlanLabSpecimenRequest` — dan di sana `CollectedAt` masih kosong karena diisi server pada tindakan pengambilan. Menegakkannya pada tindakan pengambilan justru menolak skenario contoh `BR-37` sendiri. `AC-66` terpenuhi separuh | **Pemilik modul.** Tiga pilihan pada [`BE-LAB-22.md`](../task/report/backend/BE-LAB-22.md) §6: tambah ruas waktu pengambilan lewat `r10`, persempit `VAL-59`, atau cabut seperti `VAL-60`. **Temuan ketiga dengan pola yang sama** setelah `BE-LAB-23` dan `BE-LAB-24` |

**Gap pertama dicatat, bukan dikerjakan diam-diam.** Menuliskan grafik yang dependency-nya
ditebak lebih berbahaya daripada tidak punya grafik sama sekali: yang pertama terlihat
otoritatif dan akan dipakai orang untuk mengurutkan pekerjaan.

**`AC-60` perlu satu catatan.** Ia dibuktikan `BE-LAB-25` dan `FE-LAB-10`, tetapi daftar
pantaunya baru berisi setelah ada wadah berjenis `Lainnya` yang tercatat. Pengujiannya karena
itu menyiapkan datanya sendiri, bukan menunggu pemakaian nyata.

> **Catatan itu terbukti benar ketika `FE-LAB-10` dikerjakan, 2026-09-16.** `GET /other-usage`
> terhadap `QuilvianNewDevYoga` mengembalikan `totalData 0` — baris uji `T-60d` memang dihapus
> kembali oleh `BE-LAB-25`, sebagaimana diwajibkan butir kebersihannya. Skenario verifikasi
> manual *"daftar pantau menampilkan cairan kista berjumlah tiga"* karena itu **tidak dapat
> dijalankan** pada task frontend, dan perilaku layarnya dibuktikan dengan jawaban yang
> dipasang. **Ketiadaan data, bukan ketiadaan layar** — dan bukan pula alasan untuk membuat
> data uji pada basis data bersama, yang berada di luar wewenang task frontend. Jalur yang
> mengisinya sungguhan adalah `FE-LAB-11`.

### 4.1c Gap yang dibuka bukti putaran 3 — Menu Hasil, 2026-09-16

| Gap | Isi | Kenapa tidak ditutup sekarang |
|---|---|---|
| **`BR-50` nol task, nol epic, nol FR** | Sepuluh keputusan — `LAB-DEC-064` sampai `LAB-DEC-073` — berdiri tanpa satu pun task backend maupun frontend, tanpa nomor `EPIC-LAB-*`, dan tanpa satu pun `FR`. Isinya: daftar hasil tiga disiplin, Nota Lab, Label Lab, Label Goldar, dan pengiriman hasil ke pasien | **Seluruhnya slice `S17`, dan `S17` di luar scope roadmap ini.** Menurunkannya menjadi task sekarang berarti menjadwalkan pekerjaan yang datanya belum ada: `LabExamination` nol kolom hasil, nol waktu pemeriksaan, nol status verifikasi. Yang membentuknya `S4`, tertahan `LAB-SIGN-001` |
| ~~**Kolom nomor order belum punya task**~~ | ~~`LAB-DEC-072` menetapkan `LabOrder` memperoleh kolom nomor order berupa nomor urut terbaca~~ | ✅ **Ditutup 2026-09-17 oleh `BE-LAB-36`.** Perancangannya ditulis lebih dulu pada `backend-roadmap.md` bagian 6g — sebagaimana gap ini menuntut — lalu dilaksanakan. Kolom `OrderNumber` berdiri `NOT NULL` dan unik, kedelapan pesanan lama terisi `LAB-RSMMC-000001`..`000008`, dan kelemahan pola acuannya **tidak ditiru**: nol baris dimuat ke memori, dan celah tidak pernah diisi ulang. **Dua temuan perancangan yang tidak terlihat dari keputusannya:** alokasi wajib **berblok** karena `by-examinations` membentuk beberapa pesanan dalam satu penyimpanan dan entity yang belum tersimpan tidak terlihat oleh kueri mentah; dan kunci advisory **tidak menjaga apa pun** di luar transaksi eksplisit. [`BE-LAB-36.md`](../task/report/backend/BE-LAB-36.md) |
| ~~**Tanggal pemeriksaan nol punya sumber data**~~ | ~~Kategori Periode menawarkan tiga pilihan~~ | ◐ **Ditutup sebagian 2026-09-17 oleh `BE-LAB-39`/`FE-LAB-20` di bawah `r18`.** Dua pilihan yang **punya** sumber data berdiri: `Tanggal Order` dari `RequestedAt ?? CreateDateTime`, dan `Tanggal Sampling` dari `LabSpecimen.CollectedAt`. **Pilihan ketiga sengaja tidak didirikan sama sekali** — bukan ditampilkan nonaktif — dan **ketiadaannya diuji** pada unit maupun layar, sehingga menambahkannya tanpa kolomnya akan gagal lebih dulu. Ia tetap melekat pada `S4`. [`BE-LAB-39.md`](../task/report/backend/BE-LAB-39.md) |
| **Penyaring tanggal menjawab `500`, bukan daftar** | Ditemukan 2026-09-17 saat `r18` diverifikasi: nilai `YYYY-MM-DD` — bentuk yang **memang dikontrakkan** dan **memang dikirim layar** — terikat tanpa zona dan ditolak Npgsql | ✅ **Ditutup seluruhnya 2026-09-17.** Mula-mula pada grup `Lab Monitoring`; pemeriksaan susulan pada hari yang sama menemukan **lima endpoint lain** ikut terkena — daftar dan rekap pesanan, daftar dan rekap wadah, serta pantau pemakaian jenis Lainnya. Aturannya dipindah ke `LabQueryDateRange`, satu tempat untuk enam pemanggil. **Satu negatif palsu dicatat:** putaran pertama menyangka `lab-specimen-types` selamat, padahal yang diprobe endpoint yang **nol menerima** ruas tanggal — probe yang `200` karena tidak membaca masukannya terlihat persis sama dengan probe yang benar-benar lulus |
| ~~**Counter `Terkirim ke Pasien` belum punya bentuk simpan**~~ | ~~`LAB-DEC-066` menetapkan angkanya, bukan tempatnya~~ | ✅ **Dirancang 2026-09-17** — `02-backend-architecture.md` bagian 13. **Log pengiriman `LabResultDelivery` dengan counter sebagai turunan**, bukan kolom counter: aturan "kegagalan tidak menambah angka" berarti pada bentuk counter, kegagalan **nol meninggalkan jejak**, sehingga `0` tidak dapat dibedakan dari "sudah tiga kali gagal" — padahal keputusannya sendiri mengandaikan keadaan itu ada. Nomor tujuan disimpan sebagai **snapshot** karena nomor pasien dapat berubah dan mengubah arti catatan lama tanpa satu pun baris disunting. **Migration sengaja TIDAK dibuat** — tabelnya nol punya penulis (`LAB-COORD-011`) **dan** nol punya pembaca (`S17`), dan mendirikannya sekarang mengulang `BE-EXT-04` dengan kedua sisi kosong sekaligus. Siap dilaksanakan sebagai `BE-LAB-41` begitu `LAB-COORD-011` dijawab |

**Keempatnya dicatat, bukan dikerjakan diam-diam.** Menurunkan task untuk kebutuhan yang
datanya belum ada menghasilkan roadmap yang terlihat lengkap dan tidak dapat dijalankan —
persoalan yang sama dengan grafik dependency yang dependency-nya ditebak pada `4.1b`.

### 4.2 Dua puluh satu acceptance criteria di luar scope — bukan gap

`AC-01` .. `AC-09`, `AC-14` .. `AC-16`, `AC-20` .. `AC-23`, `AC-27`, `AC-29` .. `AC-32` juga
tidak punya baris uji, tetapi seluruhnya milik slice yang **memang di luar scope**: pengisian
dan validasi hasil (`S4`), nilai kritis (`S5`), koreksi hasil (`S6`), pemberitahuan (`S8`), dan
penyuntingan pesanan oleh dokter (`S1b`). Ketiadaan barisnya wajar dan tidak perlu ditutup
sekarang.

### 4.3 Dua peran yang belum ditetapkan

Keduanya bukan penahan pembangunan, melainkan penahan pernyataan siap pakai. Task dapat
dibangun dan diuji dengan peran contoh, tetapi belum dapat dinyatakan siap dipakai sebelum
manajemen rumah sakit menetapkan pemegangnya.

| Hak akses | Dibangun oleh | Akibat selama pemegangnya belum ada |
|---|---|---|
| `LabCriticalBound : Approve` | `BE-LAB-05` | Tidak ada akun yang dapat menyetujui pengajuan, sehingga batas kritis tetap tidak dapat diubah lewat aplikasi |
| `LabRejectionReason : SystemFlag` | `BE-LAB-06` — **ditemukan 2026-09-03** | Tidak ada akun yang dapat menyetel penanda kesalahan internal, sehingga setiap alasan penolakan baru selalu bernilai "bukan kesalahan internal". Akibat nyatanya: pengambilan ulang untuk alasan-alasan baru itu **dapat ditagihkan kepada pasien** sampai peran ini ditetapkan. Perlu diketahui Billing |

### 4.4 Utang pembukuan — ✅ **keduanya ditutup 2026-09-02**

| Butir | Keadaan |
|---|---|
| ~~`input_hashes` pada manifest masih milik revisi lama~~ | ✅ **Ditutup.** Konvensinya ditemukan dari `pharmacy/blueprint-manifest.md` dan `billing-kasir/blueprint-manifest.md`: **sha256 penuh atas isi ber-line-ending LF**, bukan 16 digit. Metodenya diverifikasi cocok dengan keempat `artifact_hashes` pharmacy sebelum dipakai. Keempat hash Laboratorium dihitung ulang, dan nilai 16 digit lama dinyatakan keliru — tidak cocok dengan isi berkas mana pun |
| ~~`Riwayat Revisi` pada `00-interview-decisions.md` memuat baris revision 19 dua kali~~ | ✅ **Ditutup 2026-09-02.** Kedua salinan digabungkan; keduanya sempat bertentangan soal lokasi canonical dokumen tata kelola dan soal arti `LAB-OPEN-018`. Dicatat sebagai decisions revision 21 |

---

## 5. Penahan yang Masih Terbuka

| ID | Yang tertahan | Pencabut |
|---|---|---|
| ~~`LAB-OPEN-018`~~ | ~~Eksekusi seluruh task backend~~ | ✅ Ditutup 2026-09-02 — rules root runtime 32 berkas |
| `LAB-OPEN-018b` | Tidak menahan apa pun; `/plugin update` berikutnya mengembalikan rules root ke 13 berkas | Pendaftaran ulang marketplace ke `DevBenari/QuilvianEngineeringSkills` |
| ~~`LAB-OPEN-019`~~ | ~~Entity `Lab*` dan migration~~ | ✅ Ditutup 2026-09-02 — registry kini `ACTIVE` |
| ~~`LAB-OPEN-020`~~ | ~~Pemeriksaan konformansi QBE~~ | ✅ Ditutup 2026-09-02 — checker `PASS`, exit 0 |
| ~~`LAB-OPEN-021`~~ | ~~Penamaan dua tabel batas nilai~~ | ✅ Ditutup 2026-09-02 — ditetapkan `Lab` |
| `LAB-OPEN-012` | `BE-LAB-11`, `FR-02.4`, `FR-02.6` | Pemilik repository backend atau DBA. **Terjawab `0` untuk dev pemilik, diverifikasi ulang oleh mesin 2026-09-04**; angka produksi tetap belum diketahui. Sejak `BE-LAB-11` prasyarat ini ditegakkan migration itu sendiri: bila tabelnya masih berisi, migration berhenti sebelum satu kolom pun dihapus |
| ~~`LAB-SIGN-001`~~ | ~~Slice `S4`, `S4b`, `S4c`, `S5`, `S6`~~ | ✅ **Ditutup 2026-09-17** oleh `LAB-DEC-079` — ditandatangani per disiplin oleh `DR-LAB-001`, `DR-LAB-002`, `DR-LAB-003`. **Dinilai ulang gerbang `LAB-RCG-001-r6` pada hari yang sama:** hanya **`S4c`** yang benar-benar bebas dan dikirim ke arsitektur domain. **Diperbarui 2026-09-18 sesudah amendment pass putaran 6:** `S4b` ikut bebas, sehingga **dua** slice siap dikirim. Sisanya tertahan penahan masing-masing — `S4` oleh `DEC-LAB-011` saja; `S4d` dan `S4e` (slice baru) oleh `DEC-LAB-011` dan `LAB-OPEN-034`; `S5` oleh `LAB-P0-004`, `LAB-OPEN-014`, `DEC-LAB-012`; `S6` oleh `DEC-LAB-014`. Kelimanya **tetap di luar scope roadmap ini**: memasukkannya menuntut arsitektur domain, amandemen kontrak, lalu penurunan task, dan **nol** di antaranya sudah dikerjakan |
| `DEC-LAB-011` | `S4` validasi dan rilis — **di luar scope roadmap ini** | Kepala instalasi laboratorium + manajemen RS. Siapa yang **menetapkan** pemegang kewenangan validasi, dan jaminan dua pemegang per shift. Sudah ditanyakan `LAB-REQ-004` bagian 5.1 sejak 2026-09-09 dan tidak ikut dijawab saat penandatanganan |
| `DEC-LAB-012` | `AC-33` jalur persetujuan perubahan batas kritis, dan `S5` — **di luar scope roadmap ini** | Pihak klinis. `LAB-DEC-078` menyediakan kandidat, tetapi menyetujui aturan keselamatan dan menyetujui perubahan **angka** batas adalah dua wewenang berbeda |
| ~~`DEC-LAB-013`~~ | ~~Bagian validasi dan rilis Mikrobiologi dan Patologi Anatomi~~ | ✅ **Ditutup 2026-09-18** oleh `LAB-DEC-083` — `S4d` dan `S4e` didirikan; tiga disiplin kini punya tiga pasang sejajar. Keduanya **tetap di luar scope roadmap ini**, tertahan `DEC-LAB-011` dan `LAB-OPEN-034` |
| ~~`DEC-LAB-015`~~ | ~~`S4b` pengisian hasil Mikrobiologi~~ | ✅ **Ditutup 2026-09-18** oleh `LAB-DEC-084` — organisme dan antibiotik menjadi data induk terkendali **milik Laboratorium**, berprefix `Lab`. `S4b` naik `DOMAIN_ARCHITECTURE_READY`. **Pekerjaan yang lahir darinya dan wajib masuk roadmap saat slice dijadwalkan:** dua data induk baru **beserta cara mengisinya** |
| `DEC-LAB-016` | Bagian **gambar** `S4c` — **di luar scope roadmap ini**. Laporan narasinya **tidak** terkena | Pemilik platform + Yoga Aji Pratama. Di mana gambar patologi disimpan dan bolehkah terlayani web. Keputusan privasi, bukan teknis |
| `DEC-LAB-014` | `S6` koreksi hasil — **di luar scope roadmap ini** | Ketiga pemegang wewenang klinis. Sisa klinis `LAB-P0-003`: apakah kewenangan validasi/rilis cukup untuk mengoreksi, siapa lagi yang diberi tahu, dan adakah batas waktu. Ketiganya sudah ditanyakan `LAB-REQ-004` bagian 4.5 sejak 2026-09-09 dan tidak ikut dijawab |
| `LAB-AMD-001` | Slice `S1b` — **di luar scope roadmap ini** | Pemilik blueprint `rawat-jalan` |
| `LAB-COORD-006` | `FR-11.9` — tidak diberi task sama sekali | Pemilik `master-data`, lewat `LAB-REQ-005` butir 1-3 |
| `LAB-COORD-011` | Seluruh pengiriman hasil ke pasien (`BR-50`) — **di luar scope roadmap ini** | Pemilik platform, lewat `LAB-REQ-007` bagian 4. Gerbang pengiriman pesan **dan** pembangkit berkas PDF keduanya nol; `QRCoder 1.8.0` sudah ada dan menutup barcode, tidak menutup PDF |
| `LAB-OPEN-029` | Persetujuan hasil sebelum dikirim (`BR-50` butir 5) — **di luar scope roadmap ini** | **Menyempit 2026-09-17**: pemegang wewenangnya kini ditetapkan (`LAB-DEC-078`), tetapi apakah `RULE-017` sama dengan tanda tangan klinis **tetap terbuka** — dan menajam, sebab **nol** di antara ketiga nama bergelar Profesor. Lewat `LAB-REQ-007` bagian 3 |
| `LAB-OPEN-030` | Kolom Dokter Konfirmator angka kritis — **di luar scope roadmap ini** | Wewenang klinis, lewat `LAB-REQ-007` bagian 5. **Sudah tertahan lebih dahulu** oleh `LAB-P0-004` dan `LAB-OPEN-014`, sehingga jawabannya tidak mempercepat apa pun sendirian |
| `LAB-OPEN-032` | Ukuran cetak ketiga dokumen — **tidak memblokir slice** | Kepala instalasi laboratorium, lewat `LAB-REQ-007` bagian 6. Menentukan apakah pekerjaannya dilakukan sekali atau dua kali, bukan apakah ia dapat dimulai |
| `LAB-COORD-007` | `FR-11.10` — tidak diberi task sama sekali | Pemilik `registration-management` dan `billing-kasir`, lewat `LAB-REQ-005` butir 4-7 |
| `DATA-MST-MEASUREMENT` | **Dipersempit 2026-09-15 oleh pembacaan database.** Bukan lima baris, melainkan **tiga**: `µL`, `blok`, dan `slide`. `mL` dan `gram` **sudah ada** dan ber-`IsForLaboratory`, sehingga volume sudah dapat dipakai dan `AC-62` tidak lagi tertahan. Yang tersisa menahan separuh `AC-64` — patologi anatomi belum dapat menyatakan blok maupun slide | Pemilik `master-data`. **DIAJUKAN 2026-09-17** lewat `LAB-REQ-010`. Permintaannya ditulis ulang sesuai catatan ini: tiga baris bukan lima, tanpa mendikte kode, dan `IsDecimalAllowed = false` untuk blok serta slide. **Pembacaan ulang database 2026-09-17 memperbesar temuannya:** dari 17 baris ber-`IsForLaboratory`, **sembilan adalah duplikat dari empat simbol** — `g` 2 baris, `mg` 2, `L` **3** termasuk satu salah eja `LITTER`, dan `IU` 2. Kebersihan itu dilaporkan, bukan diminta |
| ~~`LAB-CONFLICT-004`~~ | **Ditutup 2026-09-14 oleh `LAB-DEC-049`.** ~~`BE-LAB-24` dan `FR-11.7` bagian jalur batal. **`AC-55` dan `AC-57` bertentangan dengan rancangan warisan** yang mengizinkan pembatalan terkendali sesudah titik tagih | Yoga Aji Pratama. Bila pilihan B diambil, juga pemilik `billing-kasir` dan `rawat-jalan` karena menyentuh `RJ-BIL-GATE-DEC-003` |
| ~~`LAB-CONFLICT-005`~~ | **Ditutup 2026-09-14 oleh `LAB-DEC-050`.** ~~`BE-LAB-23` dan `FR-11.6`. **`AC-52` bertentangan dengan index unik `(SpecimenId, ProcedureId)`** yang dipasang atas dasar `BR-20` dan `AC-35`. Contoh pada `BR-33` juga keliru: Glukosa Puasa dan Glukosa 2 Jam PP adalah dua `MstProcedure` berbeda | Yoga Aji Pratama. Sebaiknya dijawab bersamaan dengan `LAB-CONFLICT-004` |

**Akar kedua pertentangan itu sama, dan pantas dicatat sebagai pola.** Keduanya adalah
acceptance criteria yang ditulis amandemen 2026-09-14 dari `LAB-EVD-001`, dan keduanya lolos
sampai tahap implementasi tanpa pernah diadu dengan model data yang dikunci `LAB-DEC-024` pada
2026-09-01. Artifact lapangan menggambarkan satu layar sebagaimana dipahami penggunanya; ia
tidak tahu wadah dan pemeriksaan sudah dipisahkan, dan bahwa pemisahan itu membawa aturan
keunikan.

Amandemen berikutnya yang berangkat dari artifact lapangan perlu satu langkah tambahan:
**mengadu setiap acceptance criteria baru dengan ERD dan index yang sudah berjalan sebelum
disetujui**, bukan sesudah implementasinya dicoba.
| ~~`DB-EXEC-MVP5A`~~ | **Ditutup 2026-09-14.** Migration diterapkan ke `QuilvianNewDevYoga` atas wewenang pemilik modul; `T-M3`, `VAL-61`, dan jalur `Down` terbukti. Tersisa `T-M4` yang menunggu foreign key `BE-LAB-21`, dan `VAL-63` yang pembuktian runtime-nya menunggu aplikasi dijalankan | — |
| ~~`DB-EXEC-BE-LAB-21`~~ | ✅ **Ditutup 2026-09-15, hari yang sama ia dibuka.** Kedua migration diterapkan ke `QuilvianNewDevYoga` atas wewenang pemilik modul yang menyebut targetnya secara tegas; `T-M2`, `T-M4`, dan jalur `Down` lalu `Up` terbukti. Uraian aslinya disimpan sebagai jejak: **Dibuka 2026-09-15, cakupannya diperluas hari yang sama.** Dua migration sudah dibuat tetapi **belum diterapkan ke database mana pun**: `20260915035317_AddLabSpecimenTypeAndVolumeColumns` dan `20260915042458_AddLabSpecimenPhysicallyReceivedAt`. Menahan `T-M2`, `T-M4`, `T-58c`, `T-59c`, `T-61a`, `T-65a`, `T-67a`, `T-67b`, dan butir DoD *"migration jalan maju dan mundur"* pada kedua task. **Inilah foreign key yang ditunggu `DB-EXEC-MVP5A` untuk `T-M4`** | Pemilik modul. Wewenang yang diberikan pada sesi 2026-09-15 adalah **pembuatan migration saja**. Keduanya dapat diterapkan dalam **satu jendela**. **Perlu dikoordinasikan dengan `FE-LAB-11`** — `specimenTypeId` kini wajib, sehingga layar wadah yang sudah berjalan akan menjawab `422` begitu migration diterapkan |
| `LAB-CONFLICT-006` | **Dibuka 2026-09-15.** `VAL-59` dan separuh `AC-66` **tidak dapat dilaksanakan** pada kontrak `r7`: pembandingnya — waktu pengambilan yang dinyatakan petugas — tidak ada pada satu pun DTO permintaan, dan `CollectedAt` yang tersimpan adalah cap waktu server yang justru lebih akhir daripada waktu kedatangan pada jalur rujukan luar | **DIPUTUSKAN 2026-09-16 — pilihan A**, oleh Yoga Aji Pratama selaku pemilik modul, sesudah menggantung sejak 2026-09-15. Ruas **waktu pengambilan yang dinyatakan petugas** ditambahkan ke `PlanLabSpecimenRequest`, sehingga `VAL-59` dan `AC-66` tegak penuh. **Alasan yang menentukan bukan aturannya, melainkan datanya:** waktu pengambilan di klinik perujuk memang diketahui petugas dan hari ini **hilang — tidak tersimpan di mana pun**. Pilihan B ditolak karena aturannya akan tidak pernah menyala sehingga sama dengan mencabut, tetapi meninggalkan baris matriks yang terlihat aktif padahal tidak. **Satu koreksi atas catatan lama:** laporan `BE-LAB-22` §6 menyebut amandemen "ke `r10`" — angka itu ditulis ketika kontrak masih `r7`; sasaran sebenarnya kini **`r16`**. Amandemennya beserta task pelaksananya **belum ditulis** |

**`DATA-MST-MEASUREMENT` adalah penahan jenis ketiga**, berbeda dari dua di atasnya: ia tidak
menahan penulisan kode dan tidak menahan approval. Yang tertahan hanya pembuktiannya.
`BE-LAB-21` dapat dinyatakan selesai secara kode sambil verifikasi volumenya menunggu — dan
statusnya wajib ditulis **menunggu data, bukan menunggu kode** pada laporannya.

Pola ini sudah pernah terjadi di modul ini. `FE-LAB-05` selesai 2026-09-07 dan delapan skenario
verifikasi manualnya masih menunggu sampai hari ini karena daftar instansi perujuk kosong.
Mencatatnya sebagai penahan tersendiri sejak awal mencegah task terlihat "selesai" padahal
belum pernah dijalankan sekalipun.

---

## 6. Riwayat Revisi

| Revision | Tanggal | Perubahan | Status |
|---:|---|---|---|
| 79 | 2026-09-22 | **`FE-LAB-24` selesai, dan bersamanya `FR-13.6` punya bukti pada kedua sisi.** Sisi backend sudah terbukti lewat `BE-LAB-44` dan `BE-LAB-65`; sisi layar terbukti hari ini — [`FE-LAB-24.md`](../task/report/frontend/FE-LAB-24.md). **Yang pantas dicatat bukan jumlah berkasnya, melainkan bahwa satu penahan berbentuk bukan-kode kini punya jalan ditutup.** `B3` — daftar organisme dan antibiotik kosong — selama ini menahan `FE-LAB-26` dan membuat analis melihat pemilih kosong. Sampai hari ini nol satu pun layar dapat mengisinya, sehingga satu-satunya jalan yang tersisa adalah SQL langsung ke basis data. Itu persis bentuk kegagalan yang sudah dua kali dibayar sistem ini lewat `LAB-COORD-006` dan `MST-POS-WRITE`, dan ketiga kalinya **nol terjadi**. **Tiga acceptance criteria terbukti, dan yang paling berisiko dibuktikan terbalik:** `AC-117` menuntut KETIADAAN tombol Hapus, dan ketiadaan adalah hal yang paling mudah hilang karena seseorang merasa layar belum lengkap. Ia dikunci pada tiga lapis — nol thunk penghapusan, halaman detail merender dua aksi saja, kolom aksi tanpa Hapus — beserta dua uji unit yang memeriksa `isDeletable: false` dan ketiadaan pesan gagal penghapusan. **Satu jebakan backend ditutup di layar:** `Update*Request.IsActive` bernilai `true` ketika ruasnya nol dikirim dan `UpdateAsync` menyetelnya tanpa syarat, sehingga menyunting keterangan satu baris nonaktif akan menghidupkannya kembali **tanpa satu pun galat**. **Satu cacat warisan dicatat, bukan ditambal diam-diam:** pemeriksaan panjang maksimal pada modul rujukan nol pernah menyala sebab nilainya dipotong sebelum diukur; diperbaiki hanya pada berkas baru, dua berkas lama sengaja nol disentuh. **Satu butir verifikasi belum tercapai dan disebut apa adanya:** layar belum dijalankan terhadap backend berisi data — backend gagal start karena `Jwt:Key` belum dikonfigurasi di environment ini, dan nilainya nol ditebak maupun diisi | `DRAFT` |
| 78 | 2026-09-18 | **`BE-LAB-44` selesai — gelombang `MVP-6a` dibuka, dan kegagalan yang sudah dua kali terjadi di modul ini dicegah untuk ketiga kalinya.** Dua data induk Mikrobiologi berdiri **beserta jalur tulisnya sejak hari pertama** — persis yang `LAB-COORD-006` dan `MST-POS-WRITE` buktikan sebagai kegagalan ketika dilewatkan: satu masih terbuka sampai hari ini, satu lagi harus ditambal lewat SQL langsung ke basis data. Delapan endpoint, dua service, satu migration yang **diterapkan**. **`AC-98`..`AC-100` terbukti pada database dan terhadap aplikasi yang berjalan**: kode unik dengan normalisasi (`  zZtEsT-oRg  ` ditolak `409` sama dengan `ZZTEST-ORG`), baris nonaktif **tidak hilang** (`IsActive=false`, `IsDelete=false` — terbaca pada layar kelola, lenyap dari pilihan analis), dan **nol `DELETE`** (`405` pada kedua resource). Index unik pada kedua kode **berbentuk parsial**. **SATU SELISIH CAKUPAN DIPUTUS LEWAT URUTAN WEWENANG `AGENTS.md`:** roadmap menulis *"tanpa service"*, sedangkan `BACKEND_ENGINEERING_CONTRACT.md` menetapkan Controller → Module Service → DbContext — dan baris **Reuse** pada roadmap yang sama mengandaikan service, sebab `LabSpecimenType` punya satu. Kedua baris roadmap itu bertentangan, dan kontrak rekayasa memutusnya. **PENAHAN `MVP-6c` YANG TERSISA BUKAN KODE**: daftar organisme dan antibiotik yang sebenarnya milik kepala instalasi bersama `DR-LAB-002`; daftar pilihan analis saat ini berisi **0 baris**. Dua baris uji berawalan `ZZTEST-` tertinggal **nonaktif** dan didaftar. Lihat [`BE-LAB-44.md`](../task/report/backend/BE-LAB-44.md) | `DRAFT` |
| 77 | 2026-09-18 | **`BE-LAB-52` selesai dan gelombang `MVP-6b2` TUNTAS — Patologi Anatomi kini dapat diisi ujung ke ujung, kecuali dirilis.** Enam endpoint, tujuh DTO, satu service, nol migration. **`AC-136`..`AC-142` ketujuhnya terbukti terhadap aplikasi yang berjalan.** **`AC-136` dibuktikan pada pesanan EMPAT golongan** — dan pasangan "Histologi + IHK" yang disebut roadmap **dikoreksi**, sebab keduanya nol beririsan; yang beririsan adalah Histologi↔Sitologi Non-Gin (tiga ruas) dan Sitologi Gin↔IHK (`Anjuran`). Hasilnya **19 pasangan keberlakuan → 15 ruas**, empat bergolongan ganda: bukti `LAB-DEC-086` dalam satu tabel. **`AC-140` dibuktikan dari DUA SUMBER BERBEDA**: `issuedAt` dari `FinalizedAt` dan `effectiveAt` dari `LabSpecimen.CollectedAt`, bernilai berbeda — terbukti diturunkan, bukan disimpan; pengirimannya ditolak `422` lewat `[JsonExtensionData]` supaya Swagger nol menjanjikan ruas yang memang tidak diterima. **DoD terbukti: nol kebocoran** — lima kata uji dicari pada log aplikasi, nol kemunculan; yang tercatat hanya action, jalur, pelaku, dan trace id. **DUA CACAT DITEMUKAN PENGUJIAN:** usulan menggolongkan Imunohistokimia ke Histologi (kata kunci `HISTO` termuat di dalam "Imuno**histo**kimia"), dan `reopen` tanpa alasan menjawab `400` berpesan kosong alih-alih `422` — keduanya lolos build dan lolos pembacaan kode. **`PathologyReport.AmendValue` mewarisi alasan dari `reopen`**, sebab kontrak nol menyediakan ruas alasan pada `PUT` sedangkan `VAL-96` menjamin nol perubahan dapat terjadi tanpa melewati `reopen` lebih dulu. **Data uji tertinggal di dev dan didaftar satu per satu**, termasuk empat pemetaan golongan yang **perlu ditinjau kepala instalasi**. `FE-LAB-28` dan `FE-LAB-29` kini terbuka. Lihat [`BE-LAB-52.md`](../task/report/backend/BE-LAB-52.md) | `DRAFT` |
| 76 | 2026-09-18 | **`BE-LAB-51` selesai — tempat tersimpan laporan Patologi Anatomi berdiri, dan tiga dari empat AC-nya dibuktikan TERBALIK.** Tiga tabel (`LabPathologyReport`, `LabPathologyReportValue`, `LabPathologyOrderContext`), satu enum `LabPathologyFindingStatus`, satu migration yang **diterapkan**; nol endpoint sesuai cakupan. **`AC-132`** 3/3 index unik parsial. **`AC-133`, `AC-134`, dan `AC-135` dibuktikan dengan pencarian yang HARUS tidak menemukan apa-apa** — dan itu bentuk pembuktian yang diminta roadmap: nol `IssuedAt`/`EffectiveAt`/status lifecycle (0 baris), nol kolom `Pathology*` pada `LabExamination` (0 baris), nol kolom konteks klinis pada `LabOrder` (0 baris), nol FK menyentuh `AnalystUserId`/`FinalizedByUserId` (0 baris). **Yang paling dijaga: nol kolom status lifecycle** (`INV-36`) — sebab bila `Final` berarti *rilis*, `LAB-DEC-003` yang menuntut empat mata bertabrakan dengan `LAB-DEC-090` pada orang yang sama; **Final berarti patolog selesai menulis, bukan hasilnya boleh keluar**, dan rilis tetap `S4e` yang tertahan `DEC-LAB-011`. **Konteks klinis dibuat sebagai tabel tersendiri karena PENULISNYA orang lain** — dokter pemesan, bukan patolog (`INV-40`); menggabungkannya ke laporan akan memberi patolog hak menulis riwayat penyakit yang tidak pernah ia tanyakan, dan itu masuk rekam medis. **DUA TEMUAN:** `LabResultForm` nol punya nilai `AnatomicPathologyNarrative` yang diandaikan arsitektur bagian 15.9 — milik `BE-LAB-45` yang belum dibangun, nol menahan task ini; dan **separuh `AC-135` belum dapat diuji** sebab nol ada penulis, diwariskan ke `BE-LAB-52` beserta peringatan bahwa tanpa FK, `Guid.Empty` nol akan ditolak database. `BE-LAB-52` naik `SIAP DIKERJAKAN`. Lihat [`BE-LAB-51.md`](../task/report/backend/BE-LAB-51.md) | `DRAFT` |
| 75 | 2026-09-18 | **Ketiga usulan kontrak `S4c` disetujui, dan `BE-LAB-50` dikerjakan sampai batas wewenangnya.** `LAB-API-v1` `r25`, `LAB-VAL-v1` `r8`, dan `LAB-PERM-v1` rev 7 disetujui pemilik modul; manifest naik revision 68. **Satu hal yang BUKAN aditif dan itu disengaja: `VAL-88` DICABUT** — ia mengeraskan tiga nama kolom, sedangkan kewajiban ruas kini bergantung kategori dan ditegakkan `VAL-95` terhadap data induk keberlakuan. **Impact scan dijalankan lebih dulu**, sebab kedua SHA bergeser: backend `13665452` → `5ee03294` dan frontend `686038858` → `f89b728b7`, masing-masing satu commit, **keduanya nol menyentuh permukaan Patologi Anatomi** — `LabPathology` nol kemunculan di seluruh backend — dan keempat `input_hashes` cocok persis. **`BE-LAB-50` membangun empat tabel data induk**, empat index unik yang **seluruhnya parsial** (`AC-128` 4/4), tiga controller ber-**nol `DELETE`** (`AC-130`), dan `GET /suggestions` yang terbukti **nol menyimpan** (`AC-131`). Build hijau 0 error. **DUA SELISIH DILAPORKAN, NOL DITAMBAL DIAM-DIAM.** Pertama, **`AC-129` berselisih empat pasangan**: ia menulis 21, sedangkan rinciannya sendiri — Histologi 3, Sitologi Non-Gin 3, Sitologi Gin 3, IHK 10 — berjumlah **19**, dan `LAB-EVD-003` bagian 5.6 juga menghasilkan 19; selisih 19 terhadap 15 ruas adalah **empat** ruas yang dipakai dua golongan (`MAKROSKOPIK`, `MIKROSKOPIK`, `KESIMPULAN`, `ANJURAN`), bukan enam. Seeder diisi 19 sesuai bukti; angka 21 menunggu keputusan pemilik modul. Kedua, **cakupan menulis "dua controller" sedangkan kontrak menetapkan TIGA base URL** — kontrak yang diikuti, taksiran perencanaan yang dilaporkan. **SATU TEMUAN YANG PERLU DIWASPADAI FRONTEND:** ketiga resource **nol punya `GET /{id}`**, dan itu kelas kesalahan yang sudah pernah dibayar modul ini lewat `r6` sesudah `FE-LAB-03` diam-diam gagal di luar halaman daftar; tidak ditambal sepihak, diusulkan sebagai `r26` bila dikehendaki. **`AC-129` DIKOREKSI MENJADI 19 dan disetujui pemilik modul**, pada roadmap dan arsitektur sekaligus. **Migration DITERAPKAN dan seeder DIJALANKAN atas wewenang eksplisit pemilik modul** terhadap dev miliknya sendiri; nol reset database, nol perubahan credential, nol eksekusi di luar dev pemilik. **Keempat AC terbukti pada database sungguhan:** empat tabel berdiri, keempat index unik berpredikat `("IsDelete" = false)`, isi **4 / 15 / 19** dengan pecahan 3/3/3/10, dan pemetaan **0** yang memang disengaja beserta peringatan penyalaannya. **Database membuktikan aritmetikanya sendiri** — `MAKROSKOPIK`, `MIKROSKOPIK`, `KESIMPULAN`, dan `ANJURAN` terbukti dipakai dua golongan, sehingga 15 + 4 = 19. Urutan sepuluh ruas Imunohistokimia cocok kata demi kata dengan `LAB-EVD-003` bagian 5.6. `BE-LAB-51` naik `SIAP DIKERJAKAN`. Lihat [`BE-LAB-50.md`](../task/report/backend/BE-LAB-50.md) | `DRAFT` |
| 74 | 2026-09-18 | **`S4c` diturunkan dari arsitektur menjadi desain dan task, dan rantainya tuntas dalam satu hari: bukti → rekonsiliasi → wawancara → arsitektur → desain → rencana.** `02-backend-architecture` rev 8 bagian 15; `erd/` dan `data-dictionary` amandemen sore; `03-frontend-architecture` rev 8 bagian 13; `04-prd-to-mvp` rev 6 bagian 19; kedua roadmap. **Tujuh tabel baru, dan NOL `ALTER TABLE` pada tabel yang sudah berisi data** — termasuk `LabOrder` dan `LabExamination`. **Tiga usulan kontrak baru, seluruhnya belum disetujui:** `LAB-API-v1` `r25` menggantikan `r24` bagian 19.3 yang sudah `superseded`; `LAB-VAL-v1` `r8` yang **mencabut `VAL-88`**; dan `LAB-PERM-v1` rev 7. **Tiga kolom dicabut dari `BE-LAB-45` sebelum sempat dibangun** — `PathologyMacroscopic`, `PathologyMicroscopic`, `PathologyConclusion`. Bila task itu sudah dikerjakan, yang tertinggal tiga kolom `varchar(4000)` tanpa penulis dan tanpa pembaca, **persis `BE-EXT-04`** yang sudah pernah dibayar modul ini. **`BE-LAB-46` dan `FE-LAB-25` dibatalkan**, digantikan enam task baru. **Nol baris kode terbuang, sebab nol baris pernah ditulis** — dan itu satu-satunya sebab seluruh rantai hari ini masih murah. **Satu pemisahan hak akses yang baru dan disengaja:** konteks klinis memakai `LabOrder : Update`, bukan `LabExamination : Update`, sebab penulisnya **dokter pemesan** — bila keduanya berbagi satu izin, patolog dapat menulis riwayat penyakit pasien yang tidak pernah ia tanyakan, dan itu masuk rekam medis. **Gelombang `MVP-6b` dipecah menjadi `MVP-6b1` data induk dan `MVP-6b2` laporan, dan urutannya tidak boleh ditukar:** layar laporan yang jadi sebelum pemetaan terisi akan menampilkan formulir kosong bagi **setiap** pesanan PA — dan itu **bukan bug yang akan terlihat saat pengujian**, sebab pengujiannya memakai data yang sengaja disiapkan. **Nol baris source ditulis** | `DRAFT` |
| 73 | 2026-09-18 | **`S4c` dirancang ulang seluruhnya — `LAB-DA-001` rev 7 bagian A4, dan A3 dicabut sejauh menyangkut Patologi Anatomi.** Delapan konsep, sembilan invariant baru, **nol aggregate baru**. Verdict **`DOMAIN_ARCHITECTURE_READY`**, berdiri sendiri. **Satu penilaian saya sendiri dari kemarin saya cabut, dan alasannya pantas disimpan:** laporan PA dinilai `VALUE_OBJECT` atas tiga alasan — ketiga bagiannya selalu utuh, nol yang menunjuk kepadanya, dan ia berubah sebagai satu kesatuan. **Ketiganya gugur:** ruasnya bukan tiga melainkan sampai lima belas dan mana yang wajib bergantung kategori; nilai parameter kini menunjuk kepadanya dan ditambah-kurangi satu per satu; dan ia punya **lifecycle sendiri**. Sesuatu yang punya lifecycle dan punya yang menunjuk kepadanya adalah **entity**. Penilaian kemarin benar terhadap bukti saat itu — **buktinya yang bertambah**, dan itu bukan hal yang sama dengan salah menilai. **Yang paling saya jaga pada revision ini satu kata:** `Final` **bukan** rilis. `INV-36` menegakkan nol status lifecycle, dan alasannya bukan kerapian — bila `Final` diartikan rilis, `LAB-DEC-003` yang melarang pengisi merilis hasilnya sendiri bertabrakan dengan `LAB-DEC-090` yang menetapkan pengisi hasil PA adalah Dokter Lab, **pada orang yang sama**. **`INV-38` menolak dua kolom yang diminta artifact**: Waktu Issued dan Waktu Efektif nol disimpan. **Dua gap baru dicatat, dan yang kedua adalah biaya yang paling mudah ditemukan terlambat:** `ARCH-GAP-LAB-07` — konteks klinis diisi dokter pemesan, sehingga ia menyentuh **alur pemesanan yang sudah berjalan dan sudah dipakai**. Bukan penahan, tetapi ruasnya dibuat opsional supaya pesanan lama nol terdampak. **Empat pengecualian pada handoff seluruhnya BAGIAN, bukan slice** — gambar, HL7, cetak bilingual, Informasi Specimen — dan laporan PA tetap utuh tanpa satu pun di antaranya. **Nol baris kode disentuh** | `DRAFT` |
| 72 | 2026-09-18 | **Empat dari enam butir sisa `LAB-EVD-003` ditutup, dan dua di antaranya mengoreksi artifact-nya sendiri.** `LAB-DEC-091` sampai `LAB-DEC-094`. **`LAB-DEC-092` koreksi paling tegas:** Klarifikasi Q8 artifact menyatakan `Waktu Issued` dan `Waktu Efektif` **diisi manual**, dan itu ditolak — keduanya ruas HL7 `DiagnosticReport` yang **sistemnya sudah mencatat**, *effective* dari `LabSpecimen.CollectedAt` sejak `S2` dan *issued* dari `FinalizedAt` yang baru ditetapkan `LAB-DEC-088`. Isian manual berarti **dua sumber kebenaran untuk satu kejadian**, dan yang diketik nol akan pernah ketahuan melenceng dari yang tercatat — alasan yang sama dipakai `LAB-DEC-066` menolak counter telanjang. **`LAB-DEC-091` memindahkan empat ruas konteks klinis ke PESANAN dan ke DOKTER PEMESAN**, bukan ke hasil dan bukan ke patolog: riwayat penyakit dan masa terakhir haid ada di hadapan dokter pemesan, **bukan di hadapan patolog**, dan memintanya mengetik ulang berarti meminta ia mengarang atau mengosongkan. **`LAB-DEC-093` menolak dua jalan pintas sekaligus**, dan yang kedua hanya terlihat karena `LAB-DEC-090` diambil beberapa menit sebelumnya: menurunkan `Penanggung Jawab Analis` dari pelaku pengisian akan **selalu** menghasilkan nama dokter, sebab pengisi hasil PA memang Dokter Lab. **`LAB-DEC-094` menjaga `S4c` tetap dapat dikirim** — `Status Hasil PA` dibangun sebagai **nilai**, sedangkan tombol konfirmasi, WhatsApp, dan daftar pantau tetap `S5`. **`LAB-OPEN-014` ikut menyempit tetapi tidak tertutup:** terbukti Patologi Anatomi mengenal temuan yang wajib diperhatikan segera dan penandanya manual, sedangkan **apa** yang tergolong kritis beserta batas waktu dan eskalasinya tetap milik `DR-LAB-003`. **Sisa dua butir bukan milik Laboratorium** — `LAB-COORD-012` HL7 dan `LAB-COORD-013` terjemahan otomatis — dan keduanya memblokir **satu ruas** serta **satu bentuk cetak**, bukan seluruh halaman. **Nol baris kode disentuh** | `DRAFT` |
| 71 | 2026-09-18 | **Ketujuh pertentangan `LAB-EVD-003` ditutup dalam satu sesi wawancara, dan nol keputusan berumur kurang dari 48 jam dibatalkan.** `LAB-DEC-085` sampai `LAB-DEC-090`; `REC4-CONF-006` **larut sendiri** sebagai konsekuensi `LAB-DEC-088`, bukan diputuskan. Verdict rekonsiliasi putaran 4 naik dari `NOT_RECONCILED` menjadi **`PARTIALLY_RECONCILED`** pada hari yang sama. **Keputusan yang paling menentukan bentuk kode kelak `LAB-DEC-085`:** tempat hasil melekat **berbeda per disiplin** — Patologi Klinik dan Mikrobiologi tetap per pemeriksaan, Patologi Anatomi menjadi **per order**. Dasarnya bahan yang diperiksa, bukan kerapian model: Hemoglobin dan Kalium dua angka dari satu tabung, sedangkan makroskopik dan mikroskopik menguraikan **satu jaringan** — satu order PA berisi Histologi dan IHK pada blok parafin yang sama akan meminta uraian jaringan itu dua kali, dan keduanya dapat melenceng. **`S4a` yang sudah dipakai analis sejak 2026-09-17 nol perlu dirombak.** **`LAB-DEC-088` yang paling saya khawatirkan sebelum ditanyakan, dan jawabannya menyelamatkan dua keputusan sekaligus:** `Simpan Final` berarti patolog selesai menulis, **bukan** rilis. Bila ia diartikan rilis, artifact memberi Dokter Lab hak mengisi **dan** merilis hasil yang sama — melanggar prinsip empat mata yang baru ditandatangani `DR-LAB-003` kemarin. Dengan jawaban ini `LAB-DEC-080` dan `LAB-DEC-003` sama-sama bertahan. **Harga yang tetap harus dibayar, dan saya tulis terang:** `LAB-API-v1` `r24` bagian 19.3 — yang **disetujui pagi ini** — **tergantikan**, sebab path `/lab-examinations/{id}/result/pathology` kini salah alamat dan tiga kolom tetapnya berubah menjadi lima belas parameter dari data induk. Amandemen `r25` dibutuhkan, dan **belum ditulis**. `BE-LAB-46` dan `FE-LAB-25` **tetap dibekukan dengan sebab yang berubah**: bukan lagi menunggu keputusan, melainkan karena keputusannya mengubah rancangannya. **Bagian 19.2 Mikrobiologi dan 19.4 data induk nol terdampak** — artifact itu nol membahas Mikrobiologi. **Enam butir baru dibuka**, dua di antaranya bukan milik Laboratorium: `LAB-COORD-012` HL7 yang **nol kemunculan di seluruh backend**, dan `LAB-COORD-013` terjemahan otomatis — yang bukan sekadar kemampuan platform, sebab menerjemahkan laporan patologi berarti **mengirim diagnosis pasien ke luar sistem**, sekelas `LAB-DEC-030` dan belum pernah ditandai | `DRAFT` |
| 70 | 2026-09-18 | **Bukti lapangan baru `LAB-EVD-003` datang, dan ia membekukan dua task yang saya rencanakan beberapa jam sebelumnya.** Artifact *Detail Hasil Patologi Anatomi* direkonsiliasi sebagai **putaran 4**, verdict **`NOT_RECONCILED`** — 13 butir baru dicatat, **nol diadopsi**, 7 pertentangan dibuka dan **nol dapat ditutup tanpa pemilik modul**. **Kenapa putaran ini berbeda dari tiga sebelumnya:** ketiganya merekonsiliasi bukti terhadap blueprint yang belum diturunkan menjadi task. Putaran ini menyentuh `S4c` — kontraknya disetujui **pagi ini**, arsitekturnya selesai **kemarin**, task-nya diturunkan **beberapa jam lalu**. **Tiga pertentangan membantah keputusan berumur kurang dari 48 jam:** `Draft`/`Final`/`Reopen` adalah **status hasil** sedangkan `LAB-DEC-080` menetapkan nol status hasil; bentuk hasil PA **bergantung kategori** dan IHK sendirian punya sepuluh ruas sedangkan BR-23 hanya mengenal satu bentuk tiga ruas; dan hasil mungkin melekat pada **order** bukan pemeriksaan — yang terakhir menyentuh `S4a` yang **sudah berjalan** sekaligus `S4b` dan `S4c`. **`BE-LAB-46` dan `FE-LAB-25` dibekukan.** Itu keputusan yang masih murah **hanya karena nol baris kode sudah ditulis**; bila artifact ini datang seminggu lagi, ia datang sesudah tabelnya berdiri. **Yang sengaja TIDAK ikut dibekukan, dan alasannya saya tulis supaya tidak ikut berhenti karena ragu:** `BE-LAB-44`, `BE-LAB-45`, `BE-LAB-47`, `BE-LAB-48`, `BE-LAB-49`, dan `FE-LAB-24` — artifact ini **nol membahas Mikrobiologi**, dan keempat kolom `BE-LAB-45` tidak terbantah kebenarannya melainkan kecukupannya. **Enam kemampuan yang nol ada di mana pun pada sistem ikut tercatat**, termasuk `HL7` yang nol kemunculan di **seluruh** backend, layanan terjemahan otomatis yang mengirim diagnosis pasien keluar sistem tanpa pernah ditandai privasinya, dan empat ruas konteks klinis pada `LabOrder` yang justru pernah **dicabut** `LAB-API-v1` `r11` karena tidak ada tempatnya. **Nol keputusan dibuat, nol baris kode disentuh** | `DRAFT` |
| 69 | 2026-09-18 | **`EPIC-LAB-13` diturunkan menjadi sembilan task — enam backend (`BE-LAB-44`..`BE-LAB-49`) dan tiga frontend (`FE-LAB-24`..`FE-LAB-26`).** Backend roadmap rev 51 bagian 6i; frontend roadmap rev 30 bagian 6e. **Seluruh task backend `SIAP DIKERJAKAN`** — nol yang `TERTAHAN` oleh kontrak, sebab ketiga amandemen sudah disetujui pagi ini. Ketiga task frontend `MENUNGGU` pasangan backend-nya, dan itu disengaja. **Tiga puluh acceptance criteria baru, `AC-98` sampai `AC-127`, dan tujuh di antaranya berbentuk KETIADAAN** — nol `DELETE`, nol status hasil bertambah, nol jalur simpan sebagian, nol tombol Validasi/Rilis, nol lencana status, nol isian teks bebas untuk organisme. Ketujuhnya **diuji secara terbalik**, bukan diasumsikan, sebab seluruhnya hal yang terasa melengkapi dan justru karena itu paling mungkin ditambahkan pelaksana dengan niat baik. **`BE-LAB-49` berdiri sebagai task tersendiri, dan saya jelaskan kenapa bentuknya begitu:** ia membuktikan index unik parsial benar-benar mengizinkan hapus-lalu-pilih-ulang antibiotik yang sama. Yang dipisahkan **pembuktiannya**, bukan migration-nya — memisahkan index dari tabelnya akan meninggalkan jendela waktu ketika duplikat dapat tertulis, dan itu lebih berbahaya daripada yang hendak dicegah. Modul ini sudah membayar kelas kesalahan itu lewat `LAB-CONFLICT-005`, dan pemisahan pembuktian adalah cara menjaganya tidak ikut tertelan anggapan *"migration sudah jalan, berarti beres"*. **Satu penahan `MVP-6c` dicatat terbuka dan ia bukan pekerjaan programmer:** daftar organisme dan antibiotik **terisi**. `BE-LAB-44` menyediakan jalannya, bukan isinya — dan dua penahan modul ini yang masih terbuka hari ini, `LAB-COORD-006` dan `MST-POS-WRITE`, keduanya persis berbentuk tabel yang berdiri tanpa terisi. **Nol baris source ditulis** | `DRAFT` |
| 68 | 2026-09-18 | **Tiga amandemen kontrak disetujui pemilik modul, dan `EPIC-LAB-13` boleh masuk perencanaan.** `LAB-API-v1` naik ke `r24`, `LAB-VAL-v1` ke `r7`, `LAB-PERM-v1` ke revision 6 — ketiganya **aditif**, nol endpoint, ruas, aturan, atau permission yang sudah ada berubah. **Nol keputusan bisnis baru dibuat**; yang terjadi persetujuan atas rancangan yang keputusannya sudah diambil `LAB-DEC-027`, `LAB-DEC-080`, `LAB-DEC-081`, dan `LAB-DEC-084`. **Satu hal saya catat supaya tidak salah dibaca berbulan-bulan kemudian:** label *"Rencana (belum tersedia)"* pada tabel endpoint **tetap melekat dan itu benar** — ia menyatakan endpointnya belum ada **di kode**, bukan bahwa kontraknya belum disetujui. Dua hal yang berbeda, dan label itu baru dicabut ketika task pembangunannya selesai. **Yang tetap terbuka dan bukan penahan epic ini:** `DEC-LAB-016` hanya memblokir lampiran gambar Patologi Anatomi, dan `DEC-LAB-011` memblokir validasi/rilis yang memang di luar scope. **Yang tetap memblokir gelombang `MVP-6c` dan bukan pekerjaan programmer:** siapa mengisi daftar organisme dan antibiotik, dan kapan — itu sebabnya `MVP-6b` Patologi Anatomi ditempatkan lebih dulu | `DRAFT` |
| 67 | 2026-09-18 | **Desain modul disusun untuk `S4b` dan `S4c` — dua slice pertama bagian hasil yang menembus seluruh rantai gerbang.** `02-backend-architecture` rev 7 bagian 14; `erd/laboratory-operations` dan `data-dictionary` amandemen 2026-09-18; `03-frontend-architecture` rev 7 bagian 12; `04-prd-to-mvp` rev 5 `EPIC-LAB-13`. **Empat tabel baru, empat kolom, dua enum baru — dan nol status hasil**, yang terakhir saya tegakkan tiga kali: sebagai `INV-29`, sebagai baris pada daftar "yang sengaja tidak dibuat", dan sebagai **butir Definition of Done berbentuk ketiadaan**. Tiga kali karena ia hal yang paling mungkin ditambahkan implementer dengan niat baik. **Tiga usulan kontrak diajukan dan tidak satu pun saya tandai disetujui:** `LAB-API-v1` `r24`, `LAB-VAL-v1` `r7`, `LAB-PERM-v1` rev 6. Persetujuan tetap tindakan manusia, dan nol baris boleh dibangun sebelum itu. **Impact scan wajib dijalankan karena kedua SHA bergeser, dan temuannya yang ketiga adalah yang paling perlu diketahui:** `S4a` — pola acuan seluruh rancangan ini, yang blueprint catat selesai 2026-09-17 — **nol ada pada commit mana pun**. Kolom hasilnya hanya hidup di working tree. Rancangannya tetap sah sebab source dibaca langsung, tetapi **tingkat buktinya turun**: siapa pun yang meng-clone repository hari ini, termasuk CI, tidak akan menemukannya. Dicatat `LAB-SRC-UNCOMMITTED`, sekelas `LAB-RDY-C04`. **Dua jebakan ditemukan sebelum sempat dibangun.** Pertama, index unik `(isolat, antibiotik)` **wajib parsial** — penghapusan di sistem ini bersifat penandaan, sehingga baris tertandai hapus akan tetap menempati kuncinya dan analis yang memilih ulang antibiotik yang sama akan ditolak tanpa sebab yang masuk akal; modul ini sudah membayar persis itu lewat `LAB-CONFLICT-005`. Kedua, kedua data induk baru **wajib punya endpoint tulis sejak hari pertama** — `LAB-COORD-006` dan `MST-POS-WRITE` membuktikan tabel data induk tanpa cara mengisinya adalah kegagalan yang sudah berulang dua kali, dan `LabDummyDataSeeder.cs` justru sedang dalam keadaan terhapus. **Satu selisih pembukuan ikut ditemukan dan sengaja tidak ditambal:** `VAL-76` sampai `VAL-82` lahir pada `LAB-API-v1` `r18`/`r21` tetapi tidak pernah masuk `validation-matrix`; menyalinnya sekarang berarti mengaku ia sudah tercatat sejak `r18`, jadi yang dilakukan hanya mencatat keberadaan selisihnya. **Urutan gelombang disusun berbasis risiko, bukan abjad:** `MVP-6b` Patologi Anatomi mendahului `MVP-6c` Mikrobiologi, sebab PA nol bergantung pada data induk mana pun sedangkan layar Mikrobiologi dengan daftar organisme kosong adalah layar yang tidak dapat dipakai. **Nol migration, endpoint, service, UI, task, atau baris kode dibuat** | `DRAFT` |
| 66 | 2026-09-18 | **Satu pertanyaan, dan penahan yang saya buka sendiri beberapa jam sebelumnya tertutup.** `LAB-DEC-084` menjawab `DEC-LAB-015`: organisme dan antibiotik menjadi **dua data induk terkendali milik Laboratorium**, berprefix `Lab` mengikuti `LAB-OPEN-021` yang dulu dijawab Muhammad Hamzah dan melahirkan `LabValueBound` serta `LabValueOption`. **Kepemilikan Laboratorium dipilih di atas `master-data`, dan dasarnya bukti dari modul ini sendiri — bukan preferensi arsitektur:** `LAB-COORD-006` membuktikan instansi perujuk **nol punya endpoint tulis sama sekali**, dan `MST-POS-WRITE` membuktikannya lagi pada `MstPosition` sampai barisnya harus disisipkan lewat SQL langsung ke basis data. Dua kali pola yang sama ditemukan modul ini, dan yang ketiga tidak perlu dicoba. **Dasar domainnya berdiri terpisah:** panel antibiotik yang diuji kepekaannya adalah keputusan laboratorium tentang pekerjaannya sendiri, bukan formularium farmasi. **Arsitektur domain naik ke revision 6 dan `DOMAIN_ARCHITECTURE_READY`:** `S4b` dan `S4c` **keduanya** diserahkan ke `design-business-module`, dan yang dikecualikan tinggal **satu bagian, bukan satu slice** — gambar Patologi Anatomi, `DEC-LAB-016`. Dua invariant ditambahkan, dan `INV-31` yang paling perlu diingat: data induk yang dinonaktifkan tidak dapat dipakai pada baris **baru** tetapi **tidak mengubah** baris lama — pola yang sama dengan penunjuk batas nilai pada `S4a`, alasannya pun sama, yaitu hasil yang sudah terjadi tidak boleh berubah karena data induknya diperbarui. **Satu pekerjaan saya wajibkan ikut ke blueprint dan saya tulis tegas:** dua data induk baru **beserta cara mengisinya** — sebab modul ini sudah dua kali menemukan daftar yang ada tanpa cara mengisinya. **Jejak `S4b` yang sempat terblokir sengaja dipertahankan** di gerbang bagian 0B.6 dan arsitektur A3.16: urutannya — gerbang menyatakan siap, arsitektur menemukan gap, pemilik menutupnya dalam satu pertanyaan — justru bukti kedua tahap itu bekerja, dan menghapusnya akan membuat gerbang terbaca lebih tepat daripada yang sebenarnya. **Nol task, nol kontrak, nol kode** | `DRAFT` |
| 65 | 2026-09-18 | **Arsitektur domain disusun untuk `S4b` dan `S4c` — dua slice pertama bagian hasil yang pernah lolos gerbang — dan hasilnya satu lolos, satu terblokir.** `LAB-DA-001` revision 5, bagian A3. Tujuh konsep ditambahkan, **nol bounded context baru, nol aggregate baru, nol status baru** — yang terakhir konsekuensi langsung `LAB-DEC-080`, dan saya menegakkannya sebagai invariant `INV-29` supaya tidak tergelincir kemudian. **Keputusan pemodelan yang paling menentukan bentuk kode kelak:** laporan Patologi Anatomi menjadi `VALUE_OBJECT`, bukan entity — ketiga bagiannya wajib terisi, nol konsep lain menunjuk kepadanya, dan ia berubah sebagai satu kesatuan; sedangkan isolat mikrobiologi dan kepekaan antibiotik **memang** entity, sebab barisnya ditambah dan dikurangi satu per satu. **Satu kata saya beri peringatan tersendiri**, sebab ia paling mudah menjatuhkan orang berikutnya: BR-23 menyebut *"status `Normal`/`Positif`/`Negatif`"* — itu **nilai hasil**, bukan status lifecycle, dan ia tidak melanggar `LAB-DEC-080`. Saya namai ulang **status temuan**. **Verdict `DOMAIN_ARCHITECTURE_PARTIAL`, dan ia memuat koreksi ketiga saya atas gerbang dalam dua hari:** `S4c` `READY` dan dikirim ke penyusunan blueprint; **`S4b` `BLOCKED`** oleh `DEC-LAB-015`. Gerbang `r7` menyatakan `S4b` siap dengan **nol** penahan — dan yang saya lewatkan adalah bahwa BR-23 memerinci hasil mikrobiologi sampai ke zona hambat dalam milimeter **tanpa pernah menyatakan apakah nama kuman dan nama antibiotik diambil dari daftar terkendali**. Bila teks bebas, satu kuman akan tertulis empat cara dan **antibiogram rumah sakit mustahil disusun** — persis alasan yang pemilik modul sendiri pakai kemarin untuk menutup `LAB-DEC-082`, dan saya tidak menerapkannya. Penilaian itu dicabut pada dokumen gerbang bagian 0B.6. **`S4c` lolos karena narasi tidak menunjuk data induk apa pun** — dan itu satu-satunya sebabnya. **Dua gap lain dicatat apa adanya:** `DEC-LAB-016`, gambar patologi adalah data klinis yang dapat mengidentifikasi pasien sedangkan pola penyimpanan berkas di platform berupa `SaveFileAsync` privat per area di atas `IWebHostEnvironment` — nol layanan bersama, sehingga menaruhnya di direktori yang dilayani web adalah keputusan **privasi**; dan `ARCH-GAP-LAB-04`, **hasil mikrobiologi tidak punya cara menyatakan dirinya lengkap** sebab biakan dibaca bertahap berhari-hari — **itulah biaya `LAB-DEC-081` yang baru terlihat di meja arsitektur**, dan ia menjadi memblokir saat `S4d` dibuka. **Nol migration, endpoint, service, UI, task, atau baris kode** | `DRAFT` |
| 64 | 2026-09-18 | **`DEC-LAB-011` diajukan resmi sebagai `LAB-REQ-013`**, ditujukan kepada **dr. Arya Wicaksana, Sp.Rad** selaku kepala instalasi. Penahan termahal seluruh modul kini punya alamat: ia menahan `S4`, `S4d`, dan `S4e` sekaligus — **seluruh kemampuan merilis hasil, untuk ketiga disiplin**. **Notanya sengaja dibuat pendek dan tunggal isinya, dan itu keputusan yang diambil dari bukti**, bukan dari selera: `LAB-REQ-004` membundel tiga tanda tangan, dua penetapan, dan tiga pertanyaan klinis — ketiga tanda tangannya turun, **kelima butir lainnya tidak**, termasuk pertanyaan yang kini diajukan ulang ini. `LAB-REQ-009` meminta **satu hal** dan dijawab dalam hitungan jam sesudah menggantung delapan hari. Dua pertanyaan, satu perkara. **Satu hal saya tanyakan terbuka di dalam nota dan saya catat di sini supaya tidak terbaca sebagai kelalaian:** `Sp.Rad` adalah spesialis radiologi, sedangkan modul ini melayani tiga disiplin **laboratorium** — dan rumah sakit baru kemarin menetapkan tiga pemegang wewenang klinis per disiplin. Menetapkan siapa boleh menyatakan sebuah angka hasil benar adalah penilaian **kompetensi atas pekerjaan laboratorium**. Nota karena itu menanyakan instalasi mana yang dipimpin — Laboratorium, atau Penunjang Medis yang menaunginya, dan data induk sistem memang menempatkan Laboratorium di bawah `Penunjang Medis` sehingga keduanya lazim — serta apakah wewenang menetapkan ada pada kepala instalasi sendiri, pada ketiga pemegang wewenang klinis, atau berlapis. **Ketiganya saya nyatakan sama-sama sah**, dan pertanyaannya diajukan sebagai perkara batas wewenang, bukan perkara orang. **Nol keputusan dibuat, nol task, nol kontrak, nol kode** | `DRAFT` |
| 63 | 2026-09-18 | **Amendment pass putaran 6 — empat pertanyaan, empat jawaban, dan dua penilaian saya sendiri yang ternyata keliru.** `LAB-DEC-080` sampai `LAB-DEC-083`. **Yang terbuka:** `S4b` naik `READY_FOR_DOMAIN_DESIGN` menemani `S4c`, sehingga **dua** slice kini dapat dikirim ke arsitektur domain; `S4` turun menjadi **satu** penahan sesudah `LAB-P0-002` ditutup; dan dua slice **baru** lahir — `S4d` dan `S4e` validasi/rilis Mikrobiologi dan Patologi Anatomi — menutup lubang peta yang ditemukan kemarin. **Keputusan yang paling menentukan bentuk kode kelak adalah `LAB-DEC-080`:** validasi dan rilis dicatat sebagai **fakta**, bukan status — `ValidatedAt`, `ReleasedAt`, beserta pelakunya — dan `LabExaminationStatus` tidak disentuh sama sekali. Jalur yang ditolak disimpan alasannya supaya tidak diusulkan ulang: menambahkan `Validated`/`Released` ke enum itu **cacat**, sebab ia memuat `ChargeEligible` dan `Voided` yang bersumbu kelayakan tagih — pemeriksaan yang `ChargeEligible` lalu menjadi `Validated` kehilangan informasi tagihannya. Satu kolom, dua sumbu. **`LAB-DEC-081` mencabut penahan tunggal `S4b` tanpa satu pun keputusan klinis baru**, dan caranya membuat saya tidak nyaman: cukup dengan membaca yang sudah tertulis. Penanda `Definitif` memang sudah ditempatkan Rilis 2 sejak semula justru karena maknanya belum diputuskan — dan gerbang `r6` kemarin menandainya `BLOCKING` bagi `S4b`. **Itu penilaian saya yang terlalu keras, dan saya koreksi hari ini.** **Koreksi kedua lebih penting dan arahnya berlawanan:** gerbang `r6` menyatakan `LAB-P0-003` dapat ditutup pemilik modul sendirian. Wawancara membuktikan **tidak** — `LAB-REQ-004` bagian 4.5 memuat empat pertanyaan turunan `LAB-DEC-007` dan **tiga di antaranya klinis**. Saya membacanya dari judul, *"aturan pembatalan dan koreksi"*, lalu menyimpulkan ia sejenis `LAB-DEC-063`. Yang ditutup pemilik modul nyata tetapi sebagian — bentuk alasan dan bentuk versi — dan sisanya menjadi `DEC-LAB-014`. **`S6` karena itu tidak terbuka, dan urutan prioritas yang saya tulis kemarin terlalu optimistis.** **Penahan termahal seluruh modul kini `DEC-LAB-011`:** ia menahan `S4`, `S4d`, dan `S4e` sekaligus — **seluruh kemampuan merilis hasil, untuk ketiga disiplin** — dan pertanyaannya tetap yang paling sederhana dari semuanya: siapa yang berhak menunjuk seseorang sebagai pemegang kewenangan validasi. Sudah ditanyakan sejak 2026-09-09 lewat `LAB-REQ-004` bagian 5.1, dan tidak ikut dijawab ketika tanda tangannya turun. **Ini kedua kalinya modul ini berhenti bukan karena keberatan atas isinya, melainkan karena satu pertanyaan penunjukan tidak sampai ke orang yang tepat.** Gerbang dinilai ulang menjadi `LAB-RCG-001-r7`. **Nol task, nol kontrak, nol kode, nol migration** | `DRAFT` |
| 62 | 2026-09-17 | **Gerbang kelengkapan requirement dijalankan ulang (`LAB-RCG-001-r6`), dan hasilnya lebih baik daripada yang saya duga ketika menutup `LAB-SIGN-001` beberapa jam sebelumnya.** Satu slice **naik**, empat **menyempit**, nol mundur. `S4c` pengisian hasil Patologi Anatomi menjadi **`READY_FOR_DOMAIN_DESIGN`** dan menjadi satu-satunya slice yang dikirim ke `hospital-domain-architect` — seluruh dimensinya terisi, bentuk hasilnya sudah dikunci BR-23 (makroskopik, mikroskopik, kesimpulan, ketiganya wajib), dan tanda tangan `DR-LAB-003` kini ada. `S4` turun dari empat penahan menjadi dua, `S4b` dari tiga menjadi **satu**, `S5` dari empat menjadi tiga, `S6` dari tiga menjadi **satu**. **Dua penahan saya turunkan derajatnya, dan saya tulis alasannya supaya dapat dibantah:** `LAB-P0-005` integrasi alat menjadi `NON_BLOCKING_STANDARD` untuk Rilis 1 sebab `LAB-DEC-005` memang sudah memutuskannya — ditunda, dan struktur data Rilis 1 sengaja tidak menyediakan tempatnya — dan `S4a` sudah dibangun di atas keputusan itu. `LAB-P0-001` menjadi `NON_BLOCKING_STANDARD` bagi ketiga slice hasil sebab bagiannya yang menyangkut validasi dan rilis sudah dijawab `LAB-DEC-022` lalu disahkan `LAB-DEC-079`; memblokir `S4c` atas kewenangan **mengisi** hasil berarti menerapkan ukuran yang tidak pernah diterapkan pada `S4a`, dan ukuran yang hanya berlaku bagi sebagian orang bukan ukuran. **Temuan termahal hari ini bukan soal requirement, melainkan soal PETA, dan saya nyaris melewatkannya:** `S4b` dan `S4c` bernama *pengisian hasil*, dan **nol slice** memuat validasi serta rilis untuk Mikrobiologi dan Patologi Anatomi — `LAB-DEC-076` memecah `S4` hanya untuk Patologi Klinik. Selama `LAB-SIGN-001` menahan kelima slice sekaligus, celah itu **tidak punya cara terlihat**. Tanda tangan per disiplin membuatnya terlihat sekaligus mendesak: `DR-LAB-002` dan `DR-LAB-003` kini memegang wewenang klinis atas disiplin yang tidak punya tempat menjalankannya — kelas kesalahan yang sama dengan `BE-EXT-04`, satu sisi berdiri tanpa sisi lainnya, hanya terbalik arahnya. Dibuka `DEC-LAB-013`. **Dua decision ID lain, `DEC-LAB-011` dan `DEC-LAB-012`, bukan hal baru** — keduanya sudah tertulis pada `LAB-REQ-004` bagian 5 sejak 2026-09-09 dan tidak ikut dijawab; gerbang memberi keduanya identitas supaya tidak hilang untuk kedua kalinya, sebab mencatatnya sebagai prosa di dalam satu dokumen permintaan terbukti tidak cukup. **Dan satu pernyataan lama saya cabut:** revision 2 dokumen gerbang menulis *"`grill-me` tidak akan membuka apa-apa lagi tanpa kehadiran pihak-pihak itu"*. Hari ini tidak lagi begitu — `LAB-P0-002`, `LAB-OPEN-017`, dan `LAB-P0-003` dapat ditutup pemilik modul **sendiri**, dan ketiganya penahan **tunggal atau terakhir** bagi slice-nya. Wawancara pemilik kembali menjadi jalur termurah. **Nol task, nol kontrak, nol kode, nol entity diturunkan** — gerbang menilai, ia tidak merancang | `DRAFT` |
| 61 | 2026-09-17 | **`LAB-SIGN-001` ditutup. Penahan tertua modul ini — terbuka 2026-09-01, diajukan 2026-09-09, berumur 16 hari — dibayar pada hari yang sama dengan penetapan penanda tangannya.** `LAB-DEC-079`: `LAB-DEC-003`, `LAB-DEC-004`, dan `LAB-DEC-007` disetujui **apa adanya**, **masing-masing untuk disiplinnya sendiri**, oleh `DR-LAB-001` (Patologi Klinik), `DR-LAB-002` (Mikrobiologi Klinik), dan `DR-LAB-003` (Patologi Anatomi). **Kenapa ia bertahan 16 hari, dan ini pelajaran yang pantas dicatat sebagai pelajaran:** penahannya bukan keberatan atas isinya. Dokumennya **tidak punya alamat** — ditujukan kepada jabatan yang belum pernah diisi — dan begitu alamat itu ada, jawabannya datang dalam hitungan jam. **Yang saya kerjakan hari ini hanya pembukuan, dan saya menuliskannya terang supaya tidak terbaca sebagai lebih:** nol task dibuka, nol kontrak naik revisi, nol baris kode disentuh, nol migration. Yang berpindah adalah **izin merancang**. `S4`, `S4b`, `S4c`, `S5`, dan `S6` karena itu **tetap di luar scope roadmap ini** — memasukkannya menuntut arsitektur domain, amandemen kontrak, lalu penurunan task, dan nol di antara ketiganya sudah ada. **Manifest sengaja TIDAK saya geser**: memindahkan kelima slice ke `slices_in_scope` adalah keputusan rilis pemilik modul, bukan akibat otomatis tanda tangan klinis, dan `LAB-REQ-004` bagian 5 sudah menyatakan sejak 2026-09-09 bahwa tanda tangan membuat perancangannya boleh dimulai — bukan membuat fiturnya ada. **Gerbang kelengkapan requirement juga sengaja tidak saya sunting.** `02-requirement-completeness-assessment.md` masih menandai kelima slice `BUSINESS_DECISION_REQUIRED` dengan `LAB-SIGN-001` sebagai penahannya; dokumen itu menyatakan sendiri ia dipanggil ulang ketika penahannya dijawab, dan menyunting barisnya sepotong berarti mengaku sudah menjalankannya ulang padahal belum. `requirement_gate_readiness` pada manifest ditandai **basi**, bukan dinaikkan. **Satu open question lahir dari BENTUK tanda tangannya, bukan dari isinya**, dan ia yang paling pantas diperhatikan dari seluruh hari ini: `LAB-OPEN-034` — apakah kewenangan validasi dan rilis **melintasi disiplin**. Ia bukan sekadar hak akses. `LAB-DEC-022` menjamin dua pemegang kewenangan validasi tersedia **per shift**, dan jaminan itu menjadi **jauh lebih mahal** bila harus dipenuhi per disiplin alih-alih per laboratorium — satu ahli Patologi Anatomi yang cuti berarti `S4c` berhenti sepenuhnya. **Dan empat hal yang sudah diperingatkan `LAB-REQ-004` bagian 7.2 sejak 2026-09-09 terbukti tepat pada hari pertama**: bagian 5 dan 6 dokumen itu tidak ikut dijawab, sehingga `LAB-P0-004`, `LAB-OPEN-014`, `LAB-P0-001`, dan kedua penetapan (`LAB-DEC-022` butir 3, `LAB-DEC-023`) tetap terbuka | `DRAFT` |
| 60 | 2026-09-17 | **`LAB-REQ-009` dijawab, dan penahan tertua modul ini kehilangan penahannya sendiri — tetapi tidak kehilangan dirinya.** Manajemen rumah sakit menetapkan pemegang wewenang Clinical Governance lewat pemilik modul sebagai penerus; `LAB-DEC-078` membukukannya. **Nota itu meminta satu nama dan menerima tiga:** `DR-LAB-001` dr. Aditya Pranata, Sp.PK (Patologi Klinik), `DR-LAB-002` dr. Nabila Rahmawati, Sp.MK (Mikrobiologi Klinik), `DR-LAB-003` dr. Citra Maharani, Sp.PA (Patologi Anatomi) — menutup **persis** ketiga disiplin scope `LAB-DEC-025`. **Yang sebenarnya berpindah hari ini tepat satu baris, dan saya menuliskannya terang supaya tidak ada yang menyimpulkan lebih:** `LAB-REQ-004` punya alamat yang sah sesudah menggantung delapan hari — bukan karena isinya sulit, melainkan karena ia ditujukan kepada jabatan yang belum pernah diisi. **`LAB-SIGN-001` TIDAK tertutup.** `LAB-DEC-003`, `LAB-DEC-004`, dan `LAB-DEC-007` masih nol ditandatangani, dan `S4`, `S4b`, `S4c`, `S5`, `S6` tetap tertahan persis seperti kemarin; **nol task baru dibuka**, nol kontrak naik revisi, nol baris kode disentuh. **Penetapannya melahirkan satu pertanyaan yang sebelumnya tidak ada, dan itu temuan paling berharga hari ini:** bentuknya **perorangan per disiplin** — bentuk keempat, di luar ketiga yang ditawarkan `LAB-REQ-009` bagian 4 — sedangkan ketiga keputusan yang ditahan dirumuskan **lintas** disiplin. Siapa menandatangani apa karena itu belum tertentu, dan saya **tidak menebaknya**: pertanyaannya ditulis langsung ke dalam `LAB-REQ-004` bagian 9.2 sebagai dua pilihan bercentang — satu naskah bersama, atau tiga salinan per disiplin — beserta pernyataan asumsi yang akan dipakai bila tidak dicentang. **Tiga hal lain dicatat apa adanya, bukan dirapikan.** Empat ruas penetapan tidak disampaikan (siapa yang menetapkan, tanggal, masa berlaku, pengganti), sehingga yang tercatat adalah **nama**, belum sepenuhnya **penetapan** — cukup untuk melanjutkan, belum cukup sebagai jejak tata kelola. **Nol di antara ketiganya bergelar Profesor**, sehingga `LAB-OPEN-029` menyempit **dan justru menajam**: `RULE-017` terbukti menyebut penyetuju yang tidak punya padanan pada penetapan. Dan `LAB-GOV-SEAT` **menyempit, bukan tertutup** — kursinya ada sejak `LAB-DEC-077`, namanya ada sejak hari ini, **barisnya pada data induk belum**; penelusuran source dan seeder menemukan nol kemunculan ketiga nama maupun kodenya, basis data tidak diperiksa karena nol klien SQL tersedia, dan `MstDoctor` menuntut tujuh ruas wajib yang tidak satu pun disampaikan bersama nama — mencatatkannya pekerjaan `master-data`/HR dengan masukan yang belum ada. **Satu catatan pembukuan, dan ia tentang dokumen ini sendiri:** header menyebut roadmap revision `59` sedangkan baris riwayat terakhir `56`. Baris `57`, `58`, dan `59` **tidak pernah ditulis dan tidak saya susun ulang dari ingatan**; nomor berikutnya diambil `60` agar hitungannya tidak mundur, dan ketiadaannya dicatat di sini. Ruas `Manifest` dan `Masukan` pada header ikut disegarkan; ruas `Input hash` **sengaja dibiarkan** pada nilai 2026-09-02, sebab ia mencatat isi yang menjadi dasar penurunan dokumen ini, bukan isi hari ini. **DISUSUL PADA SESI YANG SAMA — lihat revision 61.** Pernyataan *"`LAB-SIGN-001` TIDAK tertutup"* di atas benar pada saat ditulis dan berumur beberapa saat: tanda tangannya menyusul. Baris ini **tidak** disunting supaya urutan kejadiannya tetap terbaca — dan supaya terlihat bahwa yang menahan 16 hari itu memang alamatnya, bukan isinya | `DRAFT` |
| 56 | 2026-09-17 | **Jabatan pemegang wewenang klinis laboratorium didirikan, dan pemeriksaannya mengoreksi dua hal yang saya catat sendiri sebelumnya.** Pemilik modul menginstruksikan penambahan data induk; `LAB-DEC-077` mendirikan `Dokter Penanggung Jawab Laboratorium` (`POS-PMJ-004`) di bawah `Penunjang Medis`, menurunkan `LAB-DEC-011` yang sudah menamainya. **`Komite Medis` sengaja tidak ikut dibuat** — ia lembaga, bukan jabatan seseorang. **Koreksi pertama:** catatan revisi 55 menulis *`MstDepartment` nol memuat unit Laboratorium* — benar harfiah, tetapi menyesatkan. Laboratorium **punya** rumah: departemen `Penunjang Medis`, dan jabatan `Analis Laboratorium` sudah ada dengan **2 orang** memegangnya. Yang hilang **satu hal**, bukan seluruhnya. **Koreksi kedua, dan ini yang paling penting:** menambahkan baris jabatan **TIDAK** membuka `LAB-SIGN-001`. Penelusuran source menemukan **nol kode** yang membaca konsep Clinical Governance — tanda tangan itu tindakan manusia atas sebuah dokumen, dan yang dibutuhkannya **sebuah nama**, bukan sebuah baris database. Jabatan tadi berguna untuk **mencatat** pemegangnya dan kelak untuk hak akses. **Jabatan validator dan pengisi untuk aturan empat mata SENGAJA belum dibuat:** `SysAccessPolicy` memang dikunci pada Department + Position sehingga keduanya akan dibutuhkan, tetapi **apa peran itu persis adalah yang justru diputuskan tanda tangannya** — membuatnya sekarang berarti menebak jawaban atas pertanyaan yang sedang diajukan. **Dua penahan baru dicatat.** `MST-POS-WRITE`: `MstPosition` dan `MstDepartment` **nol punya endpoint tulis** di seluruh backend, sehingga barisnya disisipkan lewat SQL langsung dan **tidak akan ada pada environment lain kecuali diseed ulang** — kelas yang sama dengan `LAB-COORD-006`. `LAB-GOV-SEAT`: jabatannya berdiri tetapi **pemegangnya nol**, dan itulah sisa `LAB-SIGN-001` yang sebenarnya | `DRAFT` |
| 55 | 2026-09-17 | **`LAB-REQ-004` diperbarui, dan yang dikoreksi adalah halaman yang justru paling mungkin dibaca: halaman pertamanya.** Dokumen itu ditulis 2026-09-09 dan menyebut yang tertahan sebagai *pengisian dan validasi hasil*, dengan keterangan *hasil masih dicatat di luar sistem seperti sekarang*. **Kedua kalimat itu menjadi keliru pada 2026-09-17**, ketika `S4a` selesai: hasil kini dicatat DI DALAM sistem, dan yang tertahan tinggal **validasi dan rilis**. Dokter yang membacanya apa adanya akan menyimpulkan nol bagian yang berjalan, padahal keadaannya justru sebaliknya — dan lebih mendesak. **Koreksinya membuat permintaannya lebih kuat, bukan lebih ringan:** sebelumnya tidak ada yang menumpuk karena hasil memang di luar sistem; sekarang **hasil mulai tersimpan tanpa satu pun jalan keluar yang sah**, dan jumlahnya bertambah setiap hari tanpa seorang pun yang berwenang menyatakannya benar. **Satu fakta baru ditambahkan pada kotak hambatan-sebelum-hambatan:** jabatan yang dimintakan penetapannya juga nol ada pada data induk sistem — `MstPosition` 31 jabatan tanpa Komite Medis maupun penanggung jawab laboratorium, `MstDepartment` nol memuat unit Laboratorium — sehingga bahkan bila seseorang ditunjuk hari ini, sistem belum punya tempat mencatat jabatannya, dan itu pekerjaan `master-data`. Dicatat supaya tidak ditemukan belakangan sebagai hambatan ketiga. Tabel bagian 7.1 diperbarui: `S4a` keluar dari daftar yang tertahan, **bukan karena dilewati melainkan karena terbukti tidak pernah diatur ketiga keputusan yang dimintakan tanda tangannya**. Baris `LAB-SIGN-001` pada manifest ikut diselaraskan. **Nol isi permintaan yang berubah** — ketiga keputusan yang dimintakan tanda tangannya tetap sama persis, dan penetapan penanda tangannya tetap di luar wewenang siapa pun di sini | `DRAFT` |
| 54 | 2026-09-17 | **`REC3-NEW-002` tertutup sepenuhnya lewat `r23`, dan cara ia ditutup lebih pantas dibaca daripada isinya.** Pilihan ketiga Kategori Periode — `Tanggal Pemeriksaan` — **ditolak `r18`** dengan alasan tertulis: `LabExamination` saat itu nol punya kolom waktu pemeriksaan, sehingga mencantumkannya berarti mendirikan pilihan yang tidak menuju ke mana-mana. **`r23` menambahkannya sesudah DUA syarat terpenuhi, bukan satu:** kolom `ExaminedAt` ada (`r21`), **dan ada yang mengisinya** — jalur tulis beserta layar pengisian hasil (`r21`/`r22`). Syarat kedua yang menentukan; kolom tanpa penulis adalah persis kesalahan `BE-EXT-04`, dan mencantumkan pilihannya saat kolomnya baru berdiri akan mengulanginya dari sisi yang berbeda. **Penjagaan `r18` bekerja sebagaimana dirancang:** uji unit dan pemeriksaan layar yang mengunci KETIADAAN pilihan ketiga adalah gerbangnya, dan keduanya diperbarui bersama amandemen ini — **perubahan uji itulah buktinya** bahwa prasyaratnya berubah, bukan dicabut diam-diam supaya kodenya lolos. Inilah gunanya menguji ketiadaan: penambahan kelak dipaksa melewati pemeriksaan sadar alih-alih menyelinap. **Penyaringnya dibuktikan menyaring, bukan sekadar menerima:** satu hasil diisi dengan waktu pemeriksaan 2026-09-10, lalu rentang setahun yang sama mengembalikan **1** baris pada `ExaminationDate` dan **4** pada `OrderDate` — bila keduanya menyaring pada waktu yang sama, angkanya akan sama, dan uji yang hanya memeriksa status berhasil beserta adanya isi nol akan menangkapnya. Tanggal 09-11 mengembalikan **0**, membuktikan pembedaan harinya bekerja. Nilai lama nol bergeser; `ExaminationDate` disisipkan sebagai `3`. 1079/1079 uji unit, 19/19 pemeriksaan layar, nol baris uji tertinggal | `DRAFT` |
| 53 | 2026-09-17 | **Utang yang ditandai sendiri pada revisi 52 ditutup pada hari yang sama: endpoint pengisian hasil kini punya layarnya.** `r22` menambahkan `GET /lab-examinations/{id}/result` beserta dua ruas waktu pada daftar kerja, dan `FE-LAB-23` mendirikan dialog pengisian pada layar daftar kerja. **Kenapa jalur baca dibutuhkan, dan kenapa layar tidak boleh menebaknya:** bentuk hasil — angka atau pilihan — ditentukan batas nilai yang berlaku bagi **pasien tertentu**, dibedakan jenis kelamin dan kelompok umur, sehingga nol dapat diturunkan dari jenis pemeriksaannya saja. Menebaknya berarti menampilkan kotak angka untuk pemeriksaan yang hanya menerima pilihan, dan petugas baru mengetahuinya sesudah `422` datang. **Jalur pemilihan batas dipakai bersama baca dan tulis** — dua salinan aturan yang sama pasti bercabang, dan cabangnya membuat layar menampilkan rujukan yang berbeda dari yang dipakai menilai hasilnya; dibuktikan pada Hemoglobin, yang dibaca **12,0–15,0** sama persis dengan yang dipakai jalur tulis. **Dua hal sengaja tidak dikirim, dan ketiadaannya diverifikasi nol kemunculan:** `criticalLow`, `criticalHigh`, dan `IsCritical` — batas kritis adalah `LAB-DEC-004` yang tertahan `LAB-SIGN-001`, dan mengirimkannya mengundang layar menandai nilai kritis, yaitu menjanjikan alur pelaporan yang belum diputuskan. **Dua ruas pada daftar kerja adalah WAKTU, bukan status**, dan ada uji unit yang menjaganya — tanpa keduanya analis akan mengetik ulang hasil yang sudah ada tanpa satu pun tanda. **Batas `S4a` ditulis DI LAYAR, bukan hanya di kode:** dialog memuat keterangan tetap bahwa menyimpan belum memvalidasi maupun merilis, dan ada pemeriksaan layar yang menjaga kalimat itu tetap ada. **Pemeriksaan layar yang paling menentukan:** bentuk `Choice` merender `select` dan **nol** merender kotak angka; bentuk `Numeric` sebaliknya — itulah yang membuktikan layar membaca bentuknya alih-alih menebaknya. Satu pemeriksaan memeriksa muatan kunci demi kunci dan membuktikan keempat ruas yang diturunkan backend **nol ikut terkirim**. 1078/1078 uji unit, 5 pemeriksaan layar, 14 regresi; nol baris uji tertinggal | `DRAFT` |
| 52 | 2026-09-17 | **Penahan tertua modul ini ditelusuri sampai akarnya, dan ternyata menahan jauh lebih sedikit daripada yang tercatat.** `LAB-SIGN-001` berdiri sejak 2026-09-01 dan terbaca menahan seluruh pengisian hasil. Penelusurannya menemukan ia menahan **tepat tiga keputusan** — `LAB-DEC-003` (empat mata pada validasi dan rilis), `LAB-DEC-004` (nilai kritis), `LAB-DEC-007` (koreksi setelah rilis) — dan ketiganya memikul caveat yang sama kata demi kata. **`LAB-DEC-005`, yang menetapkan hasil diketik manual oleh analis, berdiri TANPA caveat itu** — setara persis dengan `LAB-DEC-006` yang sudah dibangun penuh sebagai `LabValueBound`. Pemilik modul memecah slicenya lewat `LAB-DEC-076`: `S4a` pengisian **READY**, `S4` validasi dan rilis tetap tertahan. Nama `S4b` sengaja tidak dipakai karena sudah milik Mikrobiologi. **Disiplin yang paling menentukan bukan ditemukan melainkan DIPERTAHANKAN:** `LabExaminationStatus` sudah memuat komentar bahwa `Pending`/`InProcess`/`Completed`/`Validated`/`Released` sengaja ditahan, dan `BE-LAB-43` nol menambahkan status — *hasil sudah diisi* dibaca dari `ResultEnteredAt != null`, sebuah **fakta yang tercatat**, bukan **janji tentang apa berikutnya**. Tujuh kolom nullable, satu endpoint, satu migration; ketujuh baris lama utuh. **Batas yang berlaku disimpan sebagai penunjuk**, karena tanpa itu Kalium 3,4 yang hari ini di bawah normal dapat menjadi normal besok bila batasnya digeser — berlaku surut pada hasil yang sudah tercetak. **Satu pemeriksaan dirancang agar gagal bila implementasinya salah:** Hemoglobin punya dua batas — 13–17 laki-laki dan 12–15 perempuan — dan pasiennya perempuan; Hb 12,5 terbaca **normal**, membuktikan batas yang benar dipilih. Pemeriksaan yang hanya menguji *hasil tersimpan* tidak akan pernah menangkap kekeliruan itu, karena angkanya tetap tersimpan dan hanya artinya yang salah. **Peringatan `BE-EXT-05` diterapkan, dan di sini lebih berbahaya:** `ResultEnteredByUserId` nol ber-FK, sehingga `Guid.Empty` tidak akan ditolak database melainkan tersimpan diam-diam sebagai pelaku yang tidak pernah ada. 15 pemeriksaan, seluruhnya lulus; `ExaminationStatus` terbukti tidak bergeser; nol baris uji tertinggal. **`LAB-REQ-009` diperkuat:** biayanya kini dapat disebut lebih tepat — yang tertahan bukan lagi *hasil laboratorium* melainkan **kemampuan merilis hasil**, dan ia menjadi lebih mendesak karena hasil akan mulai menumpuk tanpa jalan keluar yang sah | `DRAFT` |
| 51 | 2026-09-17 | **`REC3-NEW-009` ditutup, dan temuannya adalah bahwa penahannya bukan teknis sama sekali.** Kelima butir `Laboratorium (4).md` yang tersisa diperiksa terhadap source: **empat benar-benar buntu** — nol kolom waktu pemeriksaan, nol gerbang pesan, nol pustaka PDF, nol entity persetujuan hasil, dan `Dokter Lantai` nol kemunculan. **Yang kelima datanya lengkap** — `MstPatient.BloodType`, `OrderNumber`, `orderedProcedures`, pustaka barcode, dan komponen cetak seluruhnya sudah berdiri — dan yang menahannya **`LAB-DEC-030`**, keputusan pemilik modul sendiri yang menempatkannya di Rilis 2 dengan alasan *"kemampuan cetak, tidak memblokir alur kerja"*. Alasan itu tidak lagi sebanding dengan biaya yang sudah mendekati nol, dan pemilik modul memindahkannya ke **Rilis 1** lewat `LAB-DEC-075`. **Temuan kedua menghemat separuh pekerjaannya:** Nota Lab ternyata **sudah berdiri** sebagai "Ringkasan Pesanan Laboratorium" milik `FE-LAB-17` — isinya persis yang artifact tuntut — sehingga membangun dokumen A4 kedua hanya akan menduplikasi yang sudah terbukti. Yang benar-benar baru **dua label**. **Barcode Label Lab dari nomor order, bukan barcode wadah**, dan itu keputusan: satu amplop memuat hasil satu nomor order sedangkan satu order dapat punya beberapa wadah. **Dua hal diuji sebagai keadaan yang TIDAK boleh terjadi**, dan keduanya gagal dengan cara yang sama — hasilnya terlihat seperti label yang sah sampai seseorang memakainya: nomor order kosong **tidak** menghasilkan barcode, karena batang yang tidak dapat dipindai terlihat sah sampai dicoba; dan golongan darah tak diketahui **tidak** tercetak kosong, karena bagian kosong pada label tube tidak dapat dibedakan dari cetakan yang gagal. **Kedua label terbukti nol memanggil rincian pesanan** — label tube 50×25 mm tidak boleh menunggu perjalanan jaringan demi dua baris teks yang sudah dipegang — sementara Nota tetap memanggilnya karena daftar terpesan memang hanya ada di detail. **Ukurannya TIDAK diadopsi sebagai aturan:** `LAB-OPEN-032` tetap terbuka, angkanya hidup sebagai satu konstanta berlabel belum dikonfirmasi, dan ada uji yang menjaga ia tetap satu tempat. 1067/1067 uji unit, 4 pemeriksaan layar, 10 regresi | `DRAFT` |
| 50 | 2026-09-17 | **`REC3-NEW-010` ditutup lewat `r19` — dan yang pantas dibaca adalah bahwa ia ternyata bukan pekerjaan yang dicatatkan.** Butir ini tercatat *"kewenangan UI, tidak menyentuh kontrak backend"*, dan **keterangan itu sempat diulang dalam rekap kepada pemilik modul**. Pemeriksaan source membantahnya: `LabMonitoringItemResponse` **nol** membawa gender, dan penelusuran seluruh DTO Laboratorium hanya menemukannya pada jalur pendaftaran — ikonnya **mustahil** dibangun dari sisi layar saja. Catatan lamanya keliru karena **mengandaikan datanya sudah sampai**; ini kelas kesalahan yang sama dengan `LAB-OPEN-025`/`026`, hanya arahnya terbalik: di sana penahan yang tidak ada disangka ada, di sini pekerjaan yang ada disangka tidak. `r19` menambahkan **satu ruas** `gender` bernama enum, mengikuti bentuk `orderStatus`/`encounterType`/`paymentType` pada DTO yang sama — nol pola baru, nol migration, nol permission. **Kediaman artifact tidak diisi sendiri:** ia menyebut dua warna, sementara enum punya empat nilai dan ruasnya nullable. Pemilik modul memutuskan ikon netral untuk sisanya, **berwarna sama tetapi berlabel berbeda** — `NotDisclosed` adalah pilihan **sadar** pasien, bukan pengisian yang kurang, dan menyatukan keduanya menjadi satu label akan membaca keputusan pasien sebagai data yang hilang. **Dua alternatif ditolak beserta alasannya:** "tanpa ikon" karena baris tanpa ikon tidak dapat dibedakan dari ikon yang gagal dimuat, dan "tanda tanya" karena ia membaca pilihan sadar sebagai kekurangan. **Warna sengaja bukan satu-satunya pembawa makna:** ikonnya `aria-hidden` dan artinya dibawa teks, karena justru pada nada netral warnanya **sama** untuk dua hal yang berbeda — katanya satu-satunya pembeda. Satu pemeriksaan layar membandingkan **warna terhitung** dan membuktikan ketiga nada berbeda satu sama lain sementara kedua keadaan netral sama; satu baris uji **sengaja nol memuat ruas `gender`** — bentuk muatan sebelum `r19` — dan terbukti tetap menghasilkan ikon, bukan galat. 1058/1058 uji unit, 4 pemeriksaan layar, 6 regresi; respons nyata dicocokkan silang ke database, 4 dari 4 cocok | `DRAFT` |
| 49 | 2026-09-17 | **Dua butir `Laboratorium (4).md` yang tidak bergantung pada hasil pemeriksaan akhirnya dibangun — dan pembuktiannya menemukan cacat yang jauh lebih besar daripada kedua butirnya.** `LAB-API-v1` `r18` disetujui pemilik modul beserta **dua keputusan bentuk**: penyaring NIK memakai **dua ruas terpisah dengan pilihan di layar**, dan Kategori Periode berjalan **dua pilihan sekarang**. `BE-LAB-39` dan `FE-LAB-20` melaksanakannya — nol migration, nol endpoint, nol permission. **Keputusan pertama menolak menebak format isian**, dan alasannya diuji: No. RM yang kebetulan 16 digit akan dikirim sebagai NIK dan daftarnya kosong tanpa satu pun sebab yang terbaca petugas. **Keputusan kedua menolak mendirikan pilihan ketiga** `Tanggal Pemeriksaan` — `LabExamination` punya 20 properti dan nol di antaranya waktu pemeriksaan — dan **ketiadaannya diuji**, baik pada unit maupun layar, sehingga menambahkannya tanpa kolomnya akan gagal lebih dulu. **`Search` sengaja tidak ikut diperlebar ke NIK**, karena pelebaran diam-diam sama merusaknya dengan pengetatan diam-diam; dibuktikan `?search=<NIK>` tetap nol baris. **`CollectedAt` kosong tidak pernah cocok, dan itu SENGAJA berbeda dari `r17`** yang justru memakai jatuh-tempo: di sana substitusinya terlihat pembaca lewat penanda `FE-LAB-12`, di sini hasilnya hanya daftar dan baris bersubstitusi tidak dapat dibedakan. **Cacat yang ditemukan pembuktiannya:** penyaring tanggal ketiga layar Pemeriksaan menjawab **`500`**, bukan daftar — nilai `YYYY-MM-DD` yang memang dikontrakkan dan memang dikirim layar terikat tanpa zona dan ditolak Npgsql. **Cabang `OrderDate` yang tidak disentuh `r18` gagal identik**, jadi ia temuan, bukan regresi. Diperbaiki dua lapis: penormalan ke WIB, dan akhir rentang dinaikkan ke penghabisan hari — tanpa yang kedua, pencarian **satu hari** yang justru menjadi alasan `LAB-DEC-071` ditulis mengembalikan nol baris. **Satu penjaga dicabut karena terbukti kode mati:** `Enum.IsDefined` untuk `VAL-76` tidak pernah tercapai — pengikatan query sudah menolak lebih dulu — dan penjaga yang tidak pernah tercapai terbaca seolah menjadi penegaknya. **Pasangan bukti yang menentukan:** rentang setahun yang sama mengembalikan **3** baris pada `SamplingDate` dan **4** pada `OrderDate`; yang hilang adalah pesanan yang nol punya wadah terambil. 20 pemeriksaan API, **1053/1053** uji unit, 4 pemeriksaan layar, 2 regresi | `DRAFT` |
| 48 | 2026-09-17 | **`FR-11.8` dikoreksi dari `BELUM DIMULAI` menjadi `SELESAI`, dan koreksinya sendiri yang pantas dibaca.** Barisnya berbunyi *"`AC-75`, `AC-76`, `AC-77` — belum dikerjakan"*, padahal **kedua task pelaksananya sudah `SELESAI`** dan laporannya menyatakan ketiga AC itu terpenuhi: `FE-LAB-11` membuktikan satu pasien rujukan luar diselesaikan dari identifikasi sampai penetapan kelayakan **tanpa berpindah menu**, dan `AC-76` dibuktikan **sebagai regresi** — 12 dari 12 pemeriksaan layar lama lulus dengan nol berkas lama disentuh; `FE-LAB-12` menambahkan sisi penelusurannya. Nol kode ditulis untuk koreksi ini; yang berubah hanya catatannya. **Ini kemunculan kelima pola yang sama dalam tiga hari**, sesudah `LAB-COORD-010`, `DATA-MST-MEASUREMENT`, serta `LAB-OPEN-025` dan `LAB-OPEN-026` — catatan tata kelola modul ini **tertinggal dari keadaan sebenarnya, dan selalu ke arah yang sama**: menyatakan ada pekerjaan atau penahan yang sesungguhnya sudah tidak ada. Empat kali sebelumnya itu menghentikan pekerjaan; kali ini ia hanya membuat modul terbaca lebih belum selesai daripada keadaannya. **Sesudah koreksi ini, satu-satunya baris `FR` yang belum `SELESAI` adalah `FR-11.9` dan `FR-11.10`** — keduanya `OPEN DECISION` menunggu `LAB-COORD-006` dan `LAB-COORD-007` pada `LAB-REQ-005`, milik modul lain, dan keduanya memang **nol punya task** | `DRAFT` |
| 47 | 2026-09-17 | **`FR-11.11` berpindah menjadi `SELESAI`, dan `AC-92` terpenuhi — tetapi yang pantas dibaca dari entri ini adalah bahwa kedua penahannya tidak pernah ada.** `BE-EXT-05` dikerjakan atas wewenang pemilik `registration-management` **Andry Zain**, pilihan **A** pada `LAB-REQ-012`. **Keduanya terbantah dengan cara yang persis sama: menyebut nama yang benar pada jalur yang salah.** `LAB-OPEN-025` menunjuk `EncounterIntakeService.RegisterAsync` yang memang menuntut `PatientEncounter:Create` — tetapi itu jalur **layar pendaftaran pasien lab**, bukan jalur kiosk. Kiosk menempuh `POST /patient-encounters/kiosk`, yang **sudah ada**, dijaga hanya `KioskReadPolicy`, **nol** memikul `[AccessPermission]`, dan sudah menerima `KioskScanSessionId`, membentuk nomor antrean, serta menandai sesi terpakai. **Pilihan A yang baru disetujui itu sudah berdiri di source sejak sebelum diusulkan.** `LAB-OPEN-026` menunjuk `IsAvailableForKiosk`, kolom yang **nol disebut** jalur pembentukan kunjungan mana pun; yang benar-benar dituntut adalah `IsAvailableForRegistration`, dan pada `SU-LAB-001` nilainya `true`. **Yang benar-benar dibangun karena itu hanya butir 4 `BR-46`** — penutupan otomatis akhir hari layanan. **Penyaringnya dibangun agar tidak mungkin melebar, bukan sekadar ditulis sempit:** sasarannya diambil dari penjawab unit, sehingga **nol penjawab terdaftar berarti nol kunjungan ditutup**. **Selektivitasnya diukur terhadap `QuilvianNewDevYoga` tanpa mengubah satu baris pun** — 0 sebagaimana ditulis, **14** tanpa klausa tujuan, **157** tanpa klausa kiosk yang **91** di antaranya `WaitingForNurse`, pasien poliklinik yang sedang menunggu dipanggil. **Empat cabang kemudian dibuktikan lewat aplikasi yang benar-benar menyala** — satu ditutup (`5 → 11`, antrean `4 → 8`), **tiga bertahan**: punya `LabOrder`, bertujuan poliklinik, sudah `CheckedInAt`. Ketiga cabang "harus tetap utuh" sengaja diuji, karena penutupan yang terlalu rakus tidak akan pernah tertangkap oleh uji yang hanya memeriksa bahwa sesuatu tertutup. Pagar data nyata tidak bergeser, idempotensi terbukti, nol baris uji tertinggal. **Dan uji itulah yang menemukan satu cacat, persis kelas yang paling berbahaya pada modul ini:** aktor sistem default `Guid.Empty` melanggar foreign key `NoShowByUserId` ke `AspNetUsers`, sehingga penutupan **gagal seluruhnya** — ter-rollback dengan benar, dicoba lagi, **nol yang tampak rusak dari luar**, dan penutupan otomatis tidak akan pernah terjadi satu malam pun kecuali sebagai baris log. Diperbaiki dengan menulis `null`, nilai yang memang diizinkan kolomnya dan memang artinya benar. **Pelajaran yang menguat untuk keempat kalinya dalam dua hari:** sesudah `LAB-COORD-010` dan `DATA-MST-MEASUREMENT` yang disangka sedang ditunggu padahal belum pernah diajukan, kini dua penahan yang menghentikan satu task selama dua hari ternyata **tidak ada** — catatan penahan pada modul ini tidak pernah diverifikasi ulang terhadap source sebelum dipakai menghentikan pekerjaan | `DRAFT` |
| 46 | 2026-09-17 | **`DATA-MST-MEASUREMENT` akhirnya diajukan — penahan kedua yang ternyata belum pernah ditanyakan kepada siapa pun.** Ia tercatat sejak 2026-09-15 dengan keterangan "Belum diajukan" dan menggantung sejak itu; `LAB-REQ-010` adalah permintaannya. **Ini pola yang sama dengan `LAB-COORD-010`**, yang juga disangka sedang menunggu jawaban padahal belum pernah ditanyakan, dan baru diajukan hari ini lewat `LAB-REQ-008`. Dua penahan, dua kali sebab yang sama. **Pembacaan ulang database memperbesar temuannya, bukan mengulanginya:** dari 17 baris ber-`IsForLaboratory`, **sembilan adalah duplikat dari empat simbol** — bukan "dua baris gram" seperti tercatat sebelumnya. `L` punya **tiga** baris termasuk satu salah eja `LITTER`. Akibatnya di layar: petugas melihat dua pilihan bertuliskan `g` yang tidak dapat dibedakan, dan laporan yang mengelompokkan menurut satuan akan memecah satu satuan menjadi dua baris. **Yang diminta tetap tiga baris saja** — `µL`, `blok`, `slide` — dengan `IsDecimalAllowed = false` untuk dua yang terakhir, karena "2,5 slide" tidak punya arti di meja kerja dan akan muncul pada laporan tanpa satu pun kesalahan yang terlihat. **Kode dan `MeasurementType`-nya sengaja tidak didikte:** seri `STN` tergenerasi, dan Laboratorium tidak tahu apakah golongan cacahan sudah ada | `DRAFT` |
| 45 | 2026-09-17 | **`FR-11.5` tertutup ujung ke ujung; `MVP-5a` tuntas.** `FE-LAB-12` menutup sisi layarnya pada hari yang sama ia sempat tertahan. **`AC-67` kini terpenuhi pada ketiga sisinya** — rekap sejak `BE-LAB-22`, daftar sejak `BE-LAB-38`, dan tampilan sejak hari ini, lengkap dengan **selisih** waktu pencatatan terhadap waktu kedatangan yang menjadi butir 5 `LAB-DEC-042`. **Satu hal ditegakkan di layar yang tidak dapat ditegakkan backend:** baris yang waktu tibanya tidak pernah dicatat **ditandai terang-terangan**, sehingga waktu pencatatan tidak terbaca sebagai waktu kedatangan. Backend memang mengembalikan keduanya; hanya layar yang dapat membedakannya bagi pembaca. **Ketiga task frontend `MVP-5a` kini selesai** — `FE-LAB-10`, `FE-LAB-11`, `FE-LAB-12` — dan yang tersisa pada gelombang ini bukan pekerjaan Laboratorium: `DATA-MST-MEASUREMENT` menunggu tiga baris satuan dari `master-data`, dan `LAB-REQ-005` menunggu jawaban tiga modul lain | `DRAFT` |
| 44 | 2026-09-17 | **`r16` dan `r17` disetujui lalu dilaksanakan pada hari yang sama; penahan `FE-LAB-12` terangkat.** `BE-LAB-37` membuka `orderNumber` pada kedua jalur yang benar-benar dipakai layar — dan menemukan bahwa **`VAL-59` sudah terimplementasi dengan benar sejak `BE-LAB-22`**; yang kurang hanya pembandingnya, yang diteruskan sebagai `collectedAt: null` secara harfiah. Aturannya ada, kodenya benar, datanya yang tidak pernah datang. **`AC-66` karena itu naik dari terpenuhi separuh menjadi terpenuhi penuh.** `BE-LAB-38` mendirikan `GET /lab-specimens`, dan butir yang menentukan **dibuktikan dua arah atas satu wadah yang sama**: tiba 15 Sep dan dicatat 17 Sep, ia **muncul** pada rentang 15 Sep dan **tidak muncul** pada rentang 17 Sep. Implementasi yang keliru memakai `CreateDateTime` memberi hasil terbalik persis; implementasi yang menyaring pada kedua kolom sekaligus akan lolos tanpa pemeriksaan kedua. **`AC-67` kini terpenuhi pada kedua sisinya** — rekap sejak `BE-LAB-22`, daftar sejak hari ini. **Satu tabrakan pembukuan ditemukan dan diselesaikan:** `r16` sempat punya dua isi dari dua tempat berbeda — usulan `orderNumber` dan ruas `collectedAt` dari `LAB-CONFLICT-006` yang **sudah diputuskan 2026-09-16 tetapi amandemennya tidak pernah ditulis**; laporan `BE-LAB-22` §6 menyebut sasaran "`r10`" yang ditulis ketika kontrak masih `r7`. Keduanya digabung menjadi satu amandemen. **Satu batas dilaporkan, bukan didiamkan:** `collectedAt` dipakai sebagai pembanding dan **belum disimpan** — menyimpannya ke `LabSpecimen.CollectedAt` akan ditimpa tindakan pengambilan, justru menghapus data yang hendak diselamatkan; tiga pilihan jalan keluarnya diserahkan kepada pemilik modul | `DRAFT` |
| 43 | 2026-09-17 | **`FE-LAB-12` ⛔ tertahan; `FR-11.5` tidak dapat ditutup sisi layarnya.** Daftar penerimaan lintas pesanan **nol punya jalur baca** — `GET /lab-specimens/summary` mengembalikan angka rekap tanpa baris, `GET /by-order/{id}` melayani satu pesanan, dan `LabMonitoringService` menyentuh wadah hanya untuk mencacah. **`AC-67` tetap terpenuhi pada sisi backend**, dan itu dicatat terpisah supaya tidak tertukar: `GetSummaryAsync` menyaring tepat pada `PhysicallyReceivedAt ?? CreateDateTime`, sehingga wadah yang tiba Senin malam dan diregistrasi Selasa pagi sudah terhitung pada hari Senin. **Yang hilang barisnya, bukan aturannya.** Usul `LAB-API-v1` `r17` ditulis lengkap pada `frontend-roadmap.md` bagian 6b dan menunggu persetujuan pemilik modul. **Kemunculan kelima pola yang sama pada modul ini**, dan kelimanya kini ditabelkan supaya terbaca sebagai pola: nilai tersimpan tanpa jalan keluar (`MVP-5d`), tabel ditulis tanpa pembaca (`MVP-5e`), kolom berdiri tanpa jalan keluar (`BE-LAB-36`), ruas dituntut backend tanpa penulis (`FE-LAB-19`), dan layar dituntut roadmap tanpa jalur baca (`FE-LAB-12`). **Keempat yang pertama ditemukan sesudah pekerjaannya dimulai atau selesai; yang kelima ditemukan sebelum satu baris pun ditulis** — penjaganya bekerja | `DRAFT` |
| 42 | 2026-09-17 | **`FE-LAB-19` menutup cacat berjalan yang dibuka revisi 41 — pada hari yang sama.** Ruas wadah `r7` kini punya penulis di frontend, dan layar wadah `FE-LAB-07` dapat kembali merencanakan wadah. `AC-58` bagian layar wadah karena itu tertutup pada **kedua** sisinya: data induknya sejak `BE-LAB-20`, penolakan teks bebas sejak `BE-LAB-21`, dan **pengiriman jenisnya sejak hari ini**. **Satu hal dicatat sebagai pelajaran, bukan sebagai keluhan:** cacat ini lolos dari build, lint, dan tinjauan kontrak sekaligus — dan **uji yang ada justru menguncinya**, menyatakan rencana wadah tanpa jenis specimen adalah sah. Kelas kesalahan yang sama sudah muncul tiga kali pada modul ini dengan bentuk terbalik (sesuatu yang **ditulis** tanpa pembaca); ini bentuk keempatnya — sesuatu yang **dituntut backend** tanpa penulis. Penjaganya sama: menelusuri setiap ruas sampai ke kedua ujungnya, bukan hanya ke ujung yang sedang dikerjakan | `DRAFT` |
| 41 | 2026-09-17 | **`BE-LAB-36` dan `FE-LAB-11` selesai; satu gap ditutup dan satu cacat berjalan ditemukan.** `LAB-DEC-072` menutup gap "kolom nomor order belum punya task" — perancangannya ditulis lebih dulu sebagaimana gap itu menuntut, lalu dilaksanakan; kelemahan pola acuannya **tidak ditiru**, dan dua temuan perancangan yang tidak terlihat dari keputusannya dicatat: alokasi wajib **berblok**, dan kunci advisory **nol menjaga** di luar transaksi eksplisit. `FE-LAB-11` mendirikan layar terbesar modul ini dan menjadi **konsumen pertama** `GET /lab-specimen-types/options` yang berdiri tanpa pembaca sejak `FE-LAB-10`. **Satu butir DoD dicabut sebelum dikerjakan** karena bertentangan dengan `LAB-DEC-050`, `r9`, dan arsitektur §10.5 sekaligus — membangunnya berarti membangun sesuatu yang pasti ditolak index unik `(SpecimenId, ProcedureId)` pada baris kedua. **⚠ Cacat berjalan ditemukan, dan ia bukan lahir dari task ini:** ruas wadah `r7` **nol punya penulis di frontend**, sehingga `VAL-51` menolak `422` setiap perencanaan wadah dari `FE-LAB-07` sejak migration `BE-LAB-21` diterapkan 2026-09-15. Traceability revisi 18 sudah meramalkannya; kini terbukti dari source. Perbaikannya kecil — ruas wilayah C `FE-LAB-11` tinggal dipasang — tetapi menuntut task tersendiri | `DRAFT` |
| 40 | 2026-09-16 | **Bukti pelaksanaan `FE-LAB-10`. Dua baris `FR` memperoleh bukti sisi layarnya, dan `AC-49` tertutup ujung ke ujung.** `FR-11.1` dan `FR-11.2` kini punya bukti pada **kedua sisinya**: data induknya sejak `BE-LAB-20` dan `BE-LAB-25`, layarnya sejak hari ini. **`AC-49` terpenuhi penuh** — keempat route fitur ini berada di `health-services/master-data/lab-specimen-types/`, dan nol berkas jatuh di folder `laboratory-management`; `LAB-FE-014` dijaga, bukan sekadar disebut. **`VAL-63` berpindah dari "terverifikasi source" menjadi terbukti runtime**, dan pembuktiannya dua arah: backend menolak `HTTP 422` dengan kalimat yang kemudian dibandingkan **kata demi kata** terhadap yang dipakai layar, lalu keadaan sesudahnya diperiksa dari koneksi baru dan **nol baris berubah**. Di layar, tombolnya mati beserta sebabnya, **dan baris biasa terbukti tetap hidup** — pembuktian terbalik yang tanpanya penjaga yang salah pasang akan tetap lolos. **Catatan `AC-60` pada bagian 4.1 terbukti benar dan diberi keterangan lanjutan:** daftar pantau mengembalikan `totalData 0` karena baris uji `T-60d` memang dihapus kembali, sehingga skenario *"cairan kista berjumlah tiga"* tidak dapat dijalankan pada task frontend. Itu **ketiadaan data, bukan ketiadaan layar**, dan bukan pula alasan membuat data uji pada basis data bersama. **Satu pola lama sengaja tidak diulang:** formulir ubah memuat barisnya lewat `GET /{id}`, bukan dari daftar yang kebetulan ada di memori, dan ketiadaan permintaan daftar itu **diukur dengan menghitung yang tiba** — inilah jalur yang `LAB-API-v1` `r6` lahir untuk mencegah terulangnya sesudah `FE-LAB-03`. Gelombang `MVP-5a` sisi frontend kini 1 dari 3; `FE-LAB-11` dan `FE-LAB-12` menyusul, keduanya masih bersinggungan dengan `LAB-COORD-006` dan `LAB-COORD-007` | `DRAFT` |
| 39 | 2026-09-16 | **Bukti putaran 3 masuk sebagai gap, bukan sebagai task.** Bagian `4.1c` ditambahkan: sepuluh keputusan `LAB-DEC-064`..`LAB-DEC-073` beserta `BR-50` berdiri **tanpa satu pun task, epic, maupun `FR`** — seluruhnya slice `S17`, yang berdiri di atas hasil pemeriksaan yang belum ada. Tiga gap turunannya ikut dicatat: kolom nomor order yang keputusannya sudah turun tetapi perancangannya belum, `Tanggal Pemeriksaan` yang nol punya sumber data, dan counter `Terkirim ke Pasien` yang belum punya bentuk simpan. Empat penahan baru masuk bagian 5: `LAB-COORD-011`, `LAB-OPEN-029`, `LAB-OPEN-030`, dan `LAB-OPEN-032`, seluruhnya diajukan lewat `LAB-REQ-007`. Masukan disegarkan ke decisions rev `35` dan manifest rev `37`, yang sebelumnya tertinggal di rev `32` dan `30` | `DRAFT` |
| 38 | 2026-09-16 | **`FR-11.12` menjadi `SELESAI`, dan dengan itu seluruh layar modul Laboratorium tuntas.** `FE-LAB-17` mendirikan ringkasan cetak pesanan, sehingga **`AC-95` terpenuhi penuh** — bagian "tampil pada ringkasan cetak" yang bertahan terbuka sejak 2026-09-16 pagi akhirnya tertutup, dan ketiga nama tanda tangan terbukti **benar-benar tercetak** pada pesanan yang sudah dikonfirmasi. **Butir yang paling menentukan dibuktikan terbalik:** `window.print` diganti pencatat, dan satu klik Cetak pada baris tercatat **nol** memanggilnya. **Baris ini pantas dibaca utuh sebagai satu perjalanan**, karena ia melewati **enam tahap dalam dua hari** dan setiap perpindahannya menuntut tindakan dari orang yang berbeda: `BE-LAB-30` mendirikan kolomnya; `BE-LAB-31` mengisinya lalu menemukan nilainya tidak punya jalan keluar; `r13`/`BE-LAB-33` membuka jalan itu tetapi ke DTO yang keliru; `r14`/`BE-LAB-34` mengoreksinya; `FE-LAB-15` menampilkannya pada daftar; dan `r15`/`BE-LAB-35` lalu `FE-LAB-17` menutup bagian cetaknya. **Empat dari enam tahap itu lahir dari celah yang tidak ditemukan build, uji, maupun tinjauan kontrak** — ketiganya hijau sepanjang waktu — melainkan dari membaca apa yang benar-benar dibutuhkan konsumennya. **Dua catatan proses dicatat pada laporannya, bukan dibulatkan menjadi lolos:** satu asersi sempat gagal dan yang salah adalah ujinya, tetapi penyebabnya diperiksa lebih dulu alih-alih langsung dianggap masalah harness; dan satu pemeriksaan ditambahkan menyusul sesudah ketahuan bahwa pemeriksaan tanda tangan semula memakai baris yang belum dikonfirmasi, sehingga hanya membuktikan bloknya ada dan bukan namanya tercetak — persis pelajaran `AC-83`. **Satu batas disebut apa adanya:** jalur daftar terpesan **terisi** belum pernah terlihat pada data sungguhan karena `LabOrderedProcedure` masih berisi 0 baris | `DRAFT` |
| 37 | 2026-09-16 | **Penahan yang dibuka revisi 36 ditutup pada hari yang sama, dan `FE-LAB-17` kini siap dikerjakan.** `r15` disetujui lalu dilaksanakan `BE-LAB-35` ✅: `orderedProcedures` berdiri pada `LabOrderDetailResponse` dan **terbukti dari database** lewat 12 pemeriksaan, 12 `PASS`. **Empat pemeriksaan sengaja menguji ketiadaan** — baris terhapus tidak ikut terbaca, pesanan lama mengembalikan array kosong dan bukan `null`, daftar terpesan tidak menimpa ruas wakil, dan DTO-nya nol memuat penunjuk. **Satu pemeriksaan dirancang agar gagal bila implementasinya salah:** nama snapshot dibuat berbeda dari nama katalog, sehingga proyeksi yang menoleh ke `MstProcedure` akan tertangkap. **Baris `FR-11.12` tetap `SEBAGIAN`** — `AC-95` bagian ringkasan cetak baru tertutup ketika layarnya berdiri — tetapi sebabnya kini **murni pekerjaan yang belum dikerjakan**, bukan lagi keputusan yang belum ada maupun kontrak yang belum membuka jalan. Ketiga sebab itu pernah berlaku berturut-turut pada baris ini dalam dua hari, dan pembedaannya ditulis setiap kali karena masing-masing menuntut tindakan dari orang yang berbeda. **Satu peringatan diteruskan ke `FE-LAB-17`, bukan disimpan di laporan backend saja:** `LabOrderedProcedure` berisi **0 baris** pada database, sehingga seluruh 5 pesanan nyata menempuh jalur array kosong — jalur sah yang wajib mencetak `procedureName` sebagai isi lengkap, dan justru jalur itulah yang paling mungkin terlihat saat verifikasi layar | `DRAFT` |
| 36 | 2026-09-16 | **Sebab tertahannya `FE-LAB-17` berpindah untuk ketiga kalinya, dan kali ini ia berpindah ke penahan yang belum pernah ada dalam catatan mana pun.** Dua penahan lamanya selesai: tanda tangan **ditutup** oleh `r13`/`r14`, dan ketiga keputusan pemilik **ditetapkan** — isi cetak ditunda sampai `r15` jalan, tombol **per baris** mencetak **satu pesanan**, blok tanda tangan **tiga kolom sejajar** dengan yang belum terisi tetap dicetak bergaris kosong. **Penahan yang tersisa ditemukan saat task itu hendak dimulai:** daftar pemeriksaan yang benar-benar dipesan **tidak dapat dibaca siapa pun di luar backend**. `LabOrderedProcedure` berdiri sejak `BE-LAB-26`, terisi sejak `BE-LAB-27`, dan menegakkan `VAL-68` serta `VAL-69` dengan benar — tetapi nol DTO dan nol endpoint mengembalikannya, sementara `LabOrder.ProcedureId` hanyalah **penunjuk wakil**. **Pola ini kini muncul untuk ketiga kalinya pada modul yang sama** — sesudah `LAB-CONFLICT-007` dan `r13` — dan bentuknya selalu identik: sesuatu yang tersimpan dengan benar tetapi tidak punya jalan keluar, tidak menghasilkan galat apa pun, dan baru terlihat ketika konsumennya dibangun. Yang menemukannya bukan lint, build, uji, maupun tinjauan kontrak, melainkan pertanyaan "apa yang akan tercetak pada dokumen ini". **Usul `LAB-API-v1` `r15` ditulis** — satu ruas `orderedProcedures` pada `LabOrderDetailResponse`, dibaca dari kolom **snapshot** supaya nama katalog yang kelak diganti tidak mengubah dokumen yang sudah dicetak, **nol penunjuk dikirim**, dan pesanan lama mengembalikan **array kosong** — beserta pelaksanaannya `BE-LAB-35` ⛔ pada gelombang `MVP-5e` yang baru. **Keduanya `usulan`, belum disetujui**, dan itu ditulis terang supaya tidak terbaca sebagai pekerjaan yang tinggal dijalankan. **Keputusan pemilik dicatat apa adanya:** `FE-LAB-17` **ditunda**, bukan diturunkan menjadi versi sebagian — konsisten dengan penolakan versi dua tanda tangan kosong pada revisi 12, dan atas alasan yang sama persis: dokumen resmi yang salah tidak menimbulkan galat, ia hanya dipercaya orang | `DRAFT` |
| 35 | 2026-09-16 | **Bukti pelaksanaan `FE-LAB-13` masuk ke baris `FR-11.11`. Gelombang `MVP-5b` tuntas, dan `T-93b` akhirnya punya bukti dari layar.** Baris itu **tetap `SEBAGIAN SELESAI`** — `AC-92` masih tertahan `LAB-OPEN-025` dan `LAB-OPEN-026` — tetapi sisi `AC-93` kini terbukti dari **kedua** sisi: sisi data sejak `BE-EXT-04`, dan sisi layar hari ini. **Yang pantas dibaca ulang adalah cara task ini menemukan letak pilihannya**, karena ketiga temuannya membalik dugaan yang wajar dan tidak satu pun terlihat dari kartu task. Dugaan yang wajar adalah memasang pertanyaan tujuan layanan pada langkah yang namanya paling cocok — `Layanan & Dokter`. Membaca source membatalkannya: sesi kiosk alur Pasien Lama dibentuk di langkah **pertama**, lima langkah sebelum itu, dan `BE-EXT-04b` sengaja tidak membangun jalur ubah, sehingga kedua ruas hanya dapat ditulis **sekali**. Temuan ketiga lebih menentukan lagi: pada alur Pasien Baru sesi **langsung dikonsumsi** kunjungan poliklinik lewat `PatientEncounterController.cs:686`, sedangkan panel `FE-LAB-14` menyaring justru yang **belum** terpakai — sesi Laboratorium dari alur itu hilang pada detik yang sama ia dibuat. **Akibatnya pada cakupan diputuskan pemilik modul, bukan disempitkan sepihak:** alur Pasien Baru dikeluarkan, alurnya berhenti sesudah identitas terbaca, dan cabang poliklinik tidak menulis apa pun. **Satu temuan menghindarkan `400` yang pasti terjadi:** `Program.cs` nol mendaftarkan `JsonStringEnumConverter`, sehingga enum pada body JSON hanya terbaca sebagai angka — `targetService` dikirim `2`, bukan `"Laboratory"` seperti penyaring **query** milik `FE-LAB-14`; dua bentuk yang keduanya benar, pada tempat yang berbeda. **Pelajaran yang menguat untuk kesekian kalinya:** celah yang menentukan tidak ditemukan oleh lint, build, maupun uji — ketiganya hijau sepanjang waktu — melainkan dengan membaca apa yang benar-benar dilakukan jalur konsumennya, persis seperti `r13` dan kedua cacat `FE-LAB-14`. **Satu batas ditulis terang, bukan dibulatkan menjadi selesai:** pasien Laboratorium belum memperoleh kunjungan maupun nomor antrean di kiosk — itu `BE-EXT-05` yang masih ⛔, dan `AC-45` melarang Laboratorium membentuknya | `DRAFT` |
| 34 | 2026-09-16 | **`FR-11.13` menjadi `SELESAI` — baris FR pertama gelombang `MVP-5c` yang tertutup penuh, backend dan layar.** `FE-LAB-16` mendirikan pop-up pembatalan beralasan beserta alert konfirmasi akhir, dan **menutup alert terbukti tidak mengirim permintaan apa pun** — diperiksa dengan mencatat setiap permintaan yang tiba dan memastikan jumlahnya tetap nol. `AC-96` dan `AC-97` keduanya **terpenuhi penuh**. **Perbandingannya dengan `FR-11.12` pantas dibaca berdampingan, karena keduanya selesai pada hari yang sama dengan hasil berbeda:** `FR-11.13` tertutup penuh karena kriterianya menagih **penolakan** dan **jejak audit** — keduanya dapat dibuktikan tanpa layar tambahan; sedangkan `FR-11.12` tetap `SEBAGIAN` karena `AC-95` menagih nilai yang tampil pada **ringkasan cetak**, dan fitur cetaknya belum pernah ada. Bentuk kriteria menentukan kapan ia dapat ditutup, dan itu terlihat jelas ketika keduanya dikerjakan berurutan | `DRAFT` |
| 33 | 2026-09-16 | **`AC-94` TERPENUHI PENUH — kriteria pertama gelombang `MVP-5c` yang benar-benar tertutup ujung ke ujung.** `FE-LAB-15` mendirikan kolom Konfirmasi pada ketiga menu pemeriksaan beserta pop-upnya, dan **konfirmasi kedua terbukti tidak mungkin dilakukan dari layar** — diperiksa dari DOM aplikasi yang benar-benar berjalan. Perjalanan kriteria ini pantas dibaca utuh, karena ia melewati **empat tahap dalam satu hari**: `BE-LAB-30` mendirikan kolomnya, `BE-LAB-31` mengisinya, `BE-LAB-33` dan `BE-LAB-34` membuka jalan keluarnya — yang kedua karena yang pertama salah sasaran DTO — lalu `FE-LAB-15` menampilkannya. **`AC-95` tetap `SEBAGIAN`, dan sisanya tunggal:** bagian "tampil pada **ringkasan cetak**" menunggu `FE-LAB-17`, yang tertahan bukan oleh kontrak melainkan oleh fitur cetak yang belum pernah ada dan tiga keputusan pemilik yang belum diambil | `DRAFT` |
| 32 | 2026-09-16 | **Bukti pelaksanaan `BE-LAB-33` masuk ke baris `FR-11.12`. Seluruh sisi backend baris itu kini selesai dan terbukti.** Kelima ruas `r13` terbaca dari database pada jalur daftar maupun detail; pesanan yang belum dikonfirmasi terbukti mengembalikan kelimanya `null`; dan nama konfirmator terbukti memakai jalur yang **sama** dengan `RequestedByName`. Baris `FR-11.12` **tetap `SEBAGIAN`**, tetapi alasannya berpindah untuk kedua kalinya dalam satu hari — dan ketiga tahapnya pantas dibaca berurutan: **(1)** keputusan yang belum ada — `r13` belum disetujui; **(2)** pelaksanaan yang belum dikerjakan — `BE-LAB-33` belum jalan; **(3)** sekarang, **layar yang belum dibuat** — `FE-LAB-15`, yang nol penahan. Ketiganya menuntut tindakan dari orang yang berbeda, dan itulah sebabnya pembedaannya ditulis, bukan dibulatkan menjadi "belum selesai" | `DRAFT` |
| 31 | 2026-09-16 | **Dua keputusan pemilik modul dicatat; `FR-11.12` berubah sebab tertahannya, bukan statusnya.** `LAB-API-v1` **`r13` disetujui** — lima ruas respons konfirmasi, seluruhnya aditif — dan diturunkan menjadi `BE-LAB-33` pada gelombang `MVP-5d` yang baru. Baris `FR-11.12` **tetap `SEBAGIAN`**, tetapi alasannya berpindah dari **keputusan yang belum ada** menjadi **pelaksanaan yang belum dikerjakan**. Perbedaan itu ditulis terang karena keduanya menuntut tindakan yang berbeda: yang pertama menunggu pemilik, yang kedua menunggu programmer. **Keputusan kedua** — label tombol alert konfirmasi akhir `FE-LAB-16` ditetapkan `Konfirmasi Pembatalan` — tidak menyentuh baris traceability mana pun, karena ia kewenangan UI murni tanpa kontrak; dicatat di sini supaya jejaknya tidak hanya ada pada roadmap frontend. **Satu hal yang pantas dibaca berdampingan dengan revisi 30:** celah `r13` ditemukan bukan oleh build, uji, maupun tinjauan kontrak, melainkan dengan membaca apa yang dibutuhkan layar konsumennya — pola yang sama dengan dua cacat `FE-LAB-14` yang hanya ketahuan ketika layarnya dijalankan | `DRAFT` |
| 30 | 2026-09-16 | **Bukti pelaksanaan `FE-LAB-14` masuk ke baris `FR-11.2b`.** Baris itu berpindah dari **`BELUM DIMULAI`** menjadi **`SEBAGIAN`**: backend gelombang `MVP-5b` sudah selesai sejak 2026-09-15, dan layarnya berdiri hari ini. **Yang pantas dicatat bukan fiturnya, melainkan cara ia diverifikasi.** Lint bersih, build produksi hijau, dan 932 dari 932 uji unit lolos — **dan dua cacat nyata tetap lolos dari ketiganya**, baru ketahuan ketika layarnya benar-benar dijalankan: menarik pasien dari kiosk tidak terlihat apa-apa, dan rincian pemecahan terhapus sendiri sepersekian detik sesudah tampil. Keduanya diperbaiki. Ini menguatkan aturan yang sudah tertulis pada governance frontend: lint, test, dan build yang `PASS` **bukan** bukti bagi kontrol interaktif. **Satu penahan teknis ditemukan sebelum satu baris pun ditulis dan menentukan apakah layarnya dapat dipakai sama sekali:** jalur baca sesi kiosk yang wajar dipilih — `GET /kiosk-scan-sessions/options` — dijaga `KioskReadPolicy` yang hanya mengakui SuperAdmin, Administrator, dan akun kiosk; memakainya berarti setiap petugas laboratorium menerima `403`. Yang dipakai `admin/options`, dijaga izin aplikasi biasa, dengan prasyarat konfigurasi yang dicatat terang. **Satu bagian verifikasi tidak selesai dan dilaporkan sebagai tidak selesai**, bukan dibulatkan menjadi lolos — pelajaran `AC-83` dipakai lagi di sini | `DRAFT` |
| 29 | 2026-09-16 | **Bukti pelaksanaan `BE-LAB-32` masuk ke baris `FR-11.13`. Seluruh backend gelombang `MVP-5c` selesai.** `FR-11.13` menjadi `SEBAGIAN` — backendnya selesai dan **terbukti penuh**, tinggal `FE-LAB-16`. **Perbedaannya dengan `FR-11.12` pantas dibaca berdampingan:** `AC-96` dan `AC-97` **terpenuhi penuh**, sedangkan `AC-94` dan `AC-95` hanya sebagian. Sebabnya bukan mutu pekerjaan melainkan bentuk kriterianya: kedua kriteria pembatalan menagih **penolakan** dan **jejak audit**, yang keduanya milik backend; sedangkan kedua kriteria konfirmasi menagih nilai yang **tampil pada daftar**, dan jalan keluarnya belum ada pada kontrak. **Akibat praktisnya berbeda pula:** `FE-LAB-16` **tidak tertahan** — ia hanya mengirim `cancelReason` dan membaca `orderStatus` yang sudah lama ada — sementara `FE-LAB-15` ⛔ tetap menunggu `r13` | `DRAFT` |
| 28 | 2026-09-16 | **Bukti pelaksanaan `BE-LAB-31` masuk ke baris `FR-11.12`, beserta satu penahan baru.** Backend gelombang ini selesai: endpoint konfirmasi berdiri sesuai `r12`, `VAL-70`..`VAL-73` keempatnya terbukti menolak sesuai matriks, dan `T-97c` terbukti dari database. Baris `FR-11.12` **tetap** berstatus `SEBAGIAN`, dan alasannya berubah — bukan lagi "endpoint belum ada", melainkan **celah kontrak**. `AC-94` bagian "terbaca pada daftar" dan `AC-95` bagian "tampil pada daftar serta ringkasan cetak" menuntut ruas respons yang `LAB-API-v1` `r12` **tidak pernah definisikan**: §7.1 hanya menulis badan permintaan. Akibatnya `confirmedByName`, `confirmedAt`, dan `examinerDoctorName` tidak dikembalikan endpoint mana pun, dan **`FE-LAB-15` ditandai ⛔ `TERTAHAN`** sebelum satu baris pun ditulis — nilainya sudah tersimpan di database sejak `BE-LAB-30`, yang hilang hanyalah jalan keluarnya. Usul `r13` berisi lima ruas ditulis lengkap pada laporan `BE-LAB-31` bagian 7.2. **Pembedaan ini ditulis tegas karena pelajaran `AC-83`:** menandai `FR-11.12` selesai hanya karena backendnya selesai akan menyembunyikan penahan yang baru terlihat ketika layarnya dibangun | `DRAFT` |
| 27 | 2026-09-16 | **Bukti pelaksanaan `BE-LAB-30` masuk ke baris `FR-11.12`.** Statusnya berpindah dari **`BELUM DIMULAI`** menjadi **`SEBAGIAN`**, dan pembedaannya sengaja ditulis tegas: **prasyarat strukturalnya selesai, kriterianya sendiri belum.** Yang berdiri: nilai `LabOrderStatus.Confirmed` ada, dan ketiga kolom `ConfirmedByUserId`, `ConfirmedAt`, serta `ExaminerDoctorId` diterapkan ke `QuilvianNewDevYoga` — ketiganya `nullable` tanpa nilai bawaan, foreign key `ON DELETE RESTRICT` ke `MstDoctor` beserta indexnya berdiri, jalur `Down` lalu `Up` dibuktikan, dan 5 pesanan lama terbukti utuh dengan ketiga kolom barunya `null`. Yang **belum**: `AC-94` dan `AC-95` keduanya menuntut **aksi** konfirmasi — endpointnya `BE-LAB-31`, layarnya `FE-LAB-15` — sehingga `T-94a`..`T-95b` dan `T-97c` seluruhnya masih `NOT RUN`. Pembedaan ini ditulis karena pelajaran `AC-83`: kriteria yang ditandai selesai tanpa cara menagihnya akan bertahan salah tanpa ada yang menyadarinya | `DRAFT` |
| 26 | 2026-09-15 | **Gelombang `MVP-5c` dipetakan.** Dua baris FR baru: `FR-11.12` konfirmasi pesanan — yang **menutup sebagian `LAB-P0-002`**, pertanyaan posisi `Confirmed` yang terbuka sejak 2026-09-01 — dan `FR-11.13` pembatalan wajib beralasan. Empat acceptance criteria `AC-94`..`AC-97` beserta sembilan baris uji `T-94a`..`T-97c` ditulis bersamaan, sehingga tidak ada AC yang lahir tanpa cara menagihnya; pelajaran `BE-LAB-29` dipakai, bukan sekadar dicatat. **Dua baris uji sengaja menguji ketiadaan perubahan:** `T-97c` menjaga jalur `Requested` → `Accepted` tanpa konfirmasi tetap berjalan, dan `T-97b` menjaga pembatalan yang sah tidak ikut tertutup `VAL-75`. **Satu koreksi atas catatan sesi ini sendiri:** `LAB-DEC-063` semula menuntut kolom alasan pembatalan pada `LabOrder`; pemeriksaan source saat menulis kontrak menemukan alasannya **sudah** tersimpan sebagai `ReasonNote` pada `LabTransitionHistory`, sehingga migration turun dari empat kolom menjadi tiga | `DRAFT` |
| 25 | 2026-09-15 | **`BE-EXT-05` dihentikan sebelum ditulis; `BE-EXT-04b` menutup satu penahannya.** Pemeriksaan pra-implementasi menemukan kedua kolom `BE-EXT-04` **tidak dapat diisi siapa pun** — jalur tulis sesi kiosk nol memuat keduanya, dan 16 dari 16 sesi membenarkannya dengan nilai `null`. `AC-92` karena itu **bukan menunggu kode**: pemicunya tidak akan pernah menyala. **Bahaya yang dihindari terukur:** penyaring penutupan otomatis yang aman cocok nol baris, sedangkan yang lebih longgar akan menyapu 15 kunjungan kiosk nyata — 91 di antaranya `WaitingForNurse`, pasien yang sedang menunggu dipanggil. `BE-EXT-04b` membuka jalur tulisnya dan membuktikannya dengan memanggil controller yang sebenarnya; **muatan lama terbukti tetap diterima**. **Satu butir DoD `BE-EXT-05` ternyata sudah benar tanpa kode:** Registrasi nol menerbitkan fakta kelayakan tagih, sehingga "biaya pendaftaran gugur" terpenuhi dengan sendirinya. Dua penahan tersisa dicatat bernomor — `LAB-OPEN-025` kewenangan prinsipal kiosk, `LAB-OPEN-026` unit lab belum tersedia untuk kiosk — dan satu ketentuan yang selama ini tidak bernilai ditetapkan: `LAB-DEC-059`, hari layanan berakhir 21:00 WIB sebagai konfigurasi, belum dikonfirmasi terhadap jam operasional resmi | `DRAFT` |
| 24 | 2026-09-15 | **`BE-LAB-28` selesai — `AC-91` ditutup.** Wadah kini hanya memuat pemeriksaan yang memang dipesan, dan setiap permintaan yang memperoleh wadahnya **tertaut ke baris pemeriksaan yang mengerjakannya**; yang belum berwadah terbaca menunggu, bukan hilang. `VAL-68` `422` dan `VAL-69` `409` keduanya **dijalankan terhadap `QuilvianNewDevYoga`**, pesannya dibandingkan kata demi kata terhadap matriks. **Klaim aditifnya diperiksa, bukan diandaikan:** pesanan lama tanpa baris terpesan menerima wadah seperti sebelumnya, dan **permintaan yang sama persis** ditolak pada pesanan yang punya baris terpesan — baris kontrol inilah yang membuat pembuktian ketiadaan perubahan bermakna. **Satu jebakan dihindari sebelum ditulis:** membatalkan wadah tidak membatalkan pemeriksaan di dalamnya, sehingga menegakkan `VAL-69` dari penanda `Fulfilled` akan mengunci permintaan yang masih sah; penjagaannya membaca keadaan sebenarnya dan pulih sendiri. **Dua hal dicatat terbuka:** pembatalan **pesanan** tidak menyentuh baris terpesan — menuntut keputusan yang belum diambil — dan permintaan yang sudah dibatalkan diperlakukan sebagai tidak ada pada daftar terpesan, penafsiran yang tidak disebut matriks. `AC-87` **tetap terbuka dan tetap bukan kekurangan kode**: ia menunggu bahan uji, dan katalog lab 10 dari 10 sudah tergolong disiplin | `DRAFT` |
| 23 | 2026-09-15 | **Selisih kontrak yang dibuka revision 22 ditutup pemilik modul: `clinicalNote` dicabut lewat `LAB-API-v1` `r11`.** Yang menentukan pilihannya bukan argumen, melainkan pemeriksaan: `FE-LAB-14` — satu-satunya layar yang memanggil `POST /lab-orders/by-examinations` — **tidak menyebut catatan klinis sama sekali**. Ruas itu tidak pernah punya peminta. Pencabutannya **nol dampak kode**: `ClinicalNote` nol kemunculan di seluruh modul Laboratorium, sehingga yang hilang adalah janji, bukan perilaku. `AC-87` **tetap terbuka dan tetap bukan kekurangan kode** — ia menunggu bahan uji, yaitu pemeriksaan yang belum tergolong disiplin, dan katalog saat ini 10 dari 10 sudah tergolong. **Kepala dokumen ini disegarkan dan selisihnya diakui:** manifest revision `26` menjadi `30`, Backend SHA `466a7127` menjadi `e2152709`, kontrak `r7`/`VAL r4`/`PERM rev 4` menjadi `r11`/`r5`/`rev 5`, Decisions rev `21` menjadi `30`. Nilai-nilai itu tertinggal sejak revision 15 sementara roadmap backend sudah disegarkan revision 33 — dua dokumen bersaudara menyebut dua keadaan berbeda selama delapan revisi | `DRAFT` |
| 22 | 2026-09-15 | **`BE-LAB-27` selesai — inti permintaan pemilik modul berdiri.** `AC-86` dan `AC-88` terpenuhi dan **terbukti dengan memanggil service yang sebenarnya** terhadap `QuilvianNewDevYoga`, bukan menelusuri source: empat pemeriksaan lintas tiga disiplin menghasilkan tiga pesanan, Hemoglobin dan Kalium yang sedisiplin berkumpul menjadi satu, dan cito melekat hanya pada pemeriksaan yang ditandai. `AC-87` terpenuhi pada source tetapi **belum dapat diuji karena tidak ada bahan ujinya** — 10 dari 10 pemeriksaan laboratorium sudah tergolong disiplin, dan membuat katalog belum tergolong hanya untuk diuji berarti menambah data induk palsu. Dicatat sebagai **menunggu bahan uji, bukan menunggu kode**. **Satu selisih kontrak dibuka:** ruas `clinicalNote` pada `r10` tidak dapat dibangun karena `LabOrder` tidak punya kolom catatan — selisih yang lahir dari kontrak yang ditulis sesi ini sendiri, dan perlu keputusan pemilik modul | `DRAFT` |
| 21 | 2026-09-15 | **Gelombang 1 `MVP-5b` selesai.** `BE-EXT-04` menutup `AC-93`: dua kolom nullable berdiri pada `TrxKioskScanSession` milik `registration-management` atas wewenang `LAB-REQ-006`, dan ke-16 sesi kiosk yang sudah tersimpan terbukti utuh dengan 15 di antaranya masih cocok ke pasien. **Separuh kartu tasknya ternyata tidak perlu dikerjakan** — jalur baca sesi yang belum diproses sudah ada, sehingga yang ditambahkan hanya satu penyaring tujuan; endpoint kedua sengaja tidak dibuat agar tidak lahir dua definisi "belum diproses". `FR-11.11` berpindah menjadi **`SEBAGIAN SELESAI`**; `AC-92` menunggu `BE-EXT-05`. **Celah proses dicatat untuk kedua kalinya:** `AC-92` dan `AC-93` tidak punya baris uji, persis seperti `AC-83` sehari sebelumnya. Polanya jelas — AC ditulis `grill-me` pada decision log, matriks uji diperbarui terpisah oleh `design-business-module`, dan tidak ada langkah yang memeriksa setiap AC baru memperoleh barisnya. Akibatnya sudah terbukti sekali lewat `AC-83`, yang bertahan dilanggar karena memang tidak ada cara memeriksanya | `DRAFT` |
| 20 | 2026-09-15 | **Bukti pelaksanaan dua task `MVP-5b`.** `BE-LAB-29` menutup `LAB-CONFLICT-007` — disiplin kini diturunkan dari katalog pada **satu-satunya** jalur tulis `LabOrder`, dan bukti pelanggarannya terbaca dari database: dua pesanan tanpa disiplin ternyata atas pemeriksaan yang **katalognya sudah tahu disiplinnya**. `BE-LAB-26` mendirikan `LabOrderedProcedure` yang memisahkan **apa yang diminta** dari **apa yang dikerjakan dari sebuah wadah**, diterapkan ke `QuilvianNewDevYoga` beserta pembuktian unique parsial, `Restrict` terisolasi, dan jalur `Down`. **Dua catatan proses yang pantas dibaca ulang kelak.** Pertama, `AC-83` bertahan dilanggar sejak 2026-09-14 karena ia **tidak punya satu pun baris uji** — bukan karena orang lalai memeriksanya; `T-83a`..`T-83d` ditambahkan menutupnya. Kedua, pembuktian `Restrict` gagal dua kali dengan cara yang **terlihat seperti lulus**, dan yang menyelamatkannya adalah membaca nama constraint serta menambahkan langkah kontrol. Keduanya menguatkan pelajaran yang sama dengan `LAB-CONFLICT-004`..`006`: klaim yang tidak punya cara diperiksa akan bertahan salah tanpa ada yang menyadarinya | `DRAFT` |
| 19 | 2026-09-15 | **Gelombang `MVP-5b` dipetakan.** Dua baris FR baru: `FR-11.2b` untuk pemesanan per disiplin — `BE-LAB-26` sampai `BE-LAB-29` dan `FE-LAB-14` — dan `FR-11.11` untuk pendaftaran lewat kiosk, yang dikerjakan `BE-EXT-04`, `BE-EXT-05`, dan `FE-LAB-13` di bawah `LAB-REQ-006`. Kontrak `LAB-API-v1` `r10`, `LAB-VAL-v1` `r5`, dan `LAB-PERM-v1` rev 5 disetujui pemilik modul, sehingga backend dan frontend boleh berjalan paralel. Baris `FR-11.11` mencatat fakta yang memperkecil pekerjaannya: **kiosk sudah ada dan dipakai** — 16 sesi nyata, 15 cocok ke pasien. **Baris ini ditulis menyusul**: revisi 19 sempat dinaikkan pada kepala dokumen tanpa entri riwayatnya, dan kelalaian itu ditemukan saat revisi 20 disusun | `DRAFT` |
| 18 | 2026-09-15 | **Penutupan gelombang `MVP-5a` sisi backend, ditulis `build-module-backend`.** `FR-11.2` menjadi **`SELESAI`**, dan dengan itu **keenam task backend gelombang ini selesai atau ditutup**. `AC-60` — acceptance criteria terakhir `LAB-DEC-040` yang masih terbuka — terpenuhi dan **terbukti terhadap database**, bukan ditelusuri pada source: tiga wadah `cairan kista` menjadi satu baris berjumlah tiga, sementara `Cairan Kista` tetap baris tersendiri sehingga keragaman ejaan yang menjadi alasan layar itu dibuat tetap terlihat. Pengelompokannya terbukti diterjemahkan menjadi satu `GROUP BY` penuh di PostgreSQL. **Nol tabel ringkasan dibuat**, sesuai butir DoD-nya. **`FE-LAB-10` tidak lagi tertahan** — kedua prasyaratnya selesai. **Yang tersisa pada gelombang ini bukan pekerjaan backend:** `LAB-CONFLICT-006` menunggu keputusan pemilik modul, `DATA-MST-MEASUREMENT` menunggu tiga baris satuan dari `master-data`, ketiga task frontend menunggu dikerjakan dengan `FE-LAB-11` berstatus mendesak karena layar wadah menjawab `422` sejak migration diterapkan, dan `LAB-REQ-005` menunggu jawaban tiga modul lain | `DRAFT` |
| 17 | 2026-09-15 | **Bukti eksekusi database dan satu koreksi asumsi, ditulis `build-module-backend`.** Kedua migration `MVP-5a` yang tertunda diterapkan ke `QuilvianNewDevYoga`; `DB-EXEC-BE-LAB-21` **ditutup pada hari yang sama ia dibuka**. `FR-11.1` dan `FR-11.3` menjadi **`SELESAI`**, `FR-11.4` menjadi **`SELESAI SEBAGIAN`**. `T-M4` terbukti `23503` — menutup butir yang digantung `BE-LAB-20` sejak 2026-09-14 — dan jalur `Down` lalu `Up` dibuktikan dengan verifikasi ulang yang tetap lulus. **Koreksi yang paling penting pada revisi ini bukan kemajuan, melainkan asumsi yang terbantah.** `DATA-MST-MEASUREMENT` sejak revision 14 mengandaikan `MstMeasurement` kosong dari satuan laboratorium; pembacaan database menunjukkan **17 baris ber-`IsForLaboratory` yang aktif sudah ada**, termasuk `mL` dan `gram`. Penahan itu karena itu **dipersempit dari lima baris menjadi tiga** — `µL`, `blok`, `slide` — `AC-62` tidak lagi tertahan sama sekali, dan `AC-64` tertahan separuh. Permintaan ke `master-data` perlu **ditulis ulang**, bukan diteruskan apa adanya: kode `ML`/`UL`/`G`/`BLOK`/`SLIDE` yang diminta blueprint tidak akan pernah ada karena `MstMeasurement` memakai seri tergenerasi `STN`. **Satu gap baru dicatat:** penanda `IsForLaboratory` jauh lebih longgar daripada yang diandaikan rancangan — galon dan meter kubik ikut membawanya — dan `VAL-57` **sengaja tidak dipersempit** untuk menutupinya. Pelajarannya sejajar dengan `LAB-CONFLICT-004` sampai `006`: yang di sana adalah acceptance criteria yang tidak pernah diadu dengan model data, dan yang di sini adalah **penahan yang tidak pernah diadu dengan isi database** | `DRAFT` |
| 16 | 2026-09-15 | **Pembaruan bukti pelaksanaan `BE-LAB-22`, ditulis `build-module-backend`.** `FR-11.5` berpindah menjadi **`SELESAI SEBAGIAN`**. Dua acceptance criteria terbukti **tanpa menunggu apa pun**, dan keduanya dibuktikan dengan pencarian yang tidak menemukan apa-apa: `AC-65` lewat nol ruas permintaan bernama `ReceivedAt` pada seluruh DTO Laboratorium, dan `AC-17` lewat nol rujukan `PhysicallyReceivedAt` pada ketiga berkas perhitungan cito beserta nol diff pada ketiganya. `AC-67` terpenuhi pada source. **Satu penahan baru dibuka sebagai `LAB-CONFLICT-006`:** `VAL-59` terbukti **tidak dapat dilaksanakan** pada kontrak `r7` — pembandingnya, waktu pengambilan yang dinyatakan petugas, tidak ada pada satu pun DTO permintaan, dan `CollectedAt` yang tersimpan adalah cap waktu server yang pada jalur rujukan luar justru lebih akhir daripada waktu kedatangan. Menegakkannya pada tindakan pengambilan akan menolak skenario contoh `BR-37` sendiri. **Ini temuan ketiga dengan pola yang sama** setelah `LAB-CONFLICT-004` dan `LAB-CONFLICT-005`, dan ketiganya menguatkan catatan pada bagian 5: acceptance criteria baru dari artifact lapangan wajib diadu dengan ERD dan alur yang sudah berjalan **sebelum** disetujui, bukan sesudah implementasinya dicoba. Gap eksekusi migration diperluas mencakup kedua migration yang kini menunggu pada tabel yang sama | `DRAFT` |
| 15 | 2026-09-15 | **Pembaruan bukti pelaksanaan `BE-LAB-21`, ditulis `build-module-backend`.** Tiga baris FR berpindah status. `FR-11.1` menjadi **`SELESAI SECARA KODE`**: `AC-58` kini terpenuhi di kedua sisinya — data induknya oleh `BE-LAB-20` dan penolakan teks bebas saat mencatat wadah oleh `BE-LAB-21`, sehingga kata "sebagian" dicabut. `FR-11.3` dan `FR-11.4` berpindah dari `BELUM DIMULAI`. **`AC-63` adalah satu-satunya AC gelombang ini yang terbukti penuh tanpa menunggu apa pun**, karena bentuknya ketiadaan aturan: penelusuran seluruh rujukan `VolumeAmount` menemukan nol pembandingan terhadap batas. Dua gap dicatat pada 4.1b, dan keduanya **bukan gap desain maupun gap kode**: pembuktian runtime menunggu wewenang eksekusi migration, dan verifikasi volume menunggu `DATA-MST-MEASUREMENT`. Penahan data itu **diperkuat, bukan sekadar diulang** — sekarang kodenya berdiri, akibatnya menjadi nyata: selama kelima baris satuan kosong, setiap satuan yang dikirim ditolak `VAL-57` | `DRAFT` |
| 14 | 2026-09-14 | **`EPIC-LAB-11` dipetakan** — sepuluh FR, delapan di antaranya bertask dan dua berstatus `OPEN DECISION` tanpa ID task sama sekali. `FR-11.7` bukan `BELUM DIMULAI` melainkan `SEBAGIAN SUDAH ADA`: `VAL-18` sudah menegakkan penguncian pada jalur tambah sejak sebelum `LAB-DEC-039` ditulis. Dua gap baru dicatat pada 4.1b: ketiadaan grafik urutan dependency pada gelombang lama, dan verifikasi volume yang tertahan lima baris `MstMeasurement`. Penahan `DATA-MST-MEASUREMENT` ditambahkan sebagai **penahan jenis ketiga** — menahan pembuktian, bukan kode maupun approval — mengikuti pola `FE-LAB-05` yang selesai 2026-09-07 tetapi verifikasinya menunggu sampai hari ini | `DRAFT` |
| 1 | 2026-09-02 | Traceability pertama. 45 FR, 28 AC, 10 epic, dan 10 slice dipetakan ke 18 task backend dan 9 task frontend. Dua coverage gap dalam scope ditemukan dan dicatat | `DRAFT` |
| 2 | 2026-09-02 | Kedua coverage gap ditutup: `AC-11` alur pemesanan lintas unit dan `AC-19` batas reagen ditambahkan ke matriks uji. Cakupan acceptance criteria naik menjadi 30 dari 30. Utang pembukuan riwayat revisi decisions ikut ditutup | `DRAFT` |
| 3 | 2026-09-02 | `input_hashes` dihitung ulang sebagai sha256 penuh setelah konvensinya ditemukan dari pharmacy dan billing-kasir dan diverifikasi. `LAB-OPEN-020` ditetapkan menjadi wewenang Andry Zain. Seluruh utang pembukuan tertutup | `DRAFT` |
| 4 | 2026-09-02 | Audit cakupan endpoint ditambahkan sebagai dimensi ketiga di samping FR dan AC. Empat endpoint Lab Examination ternyata tanpa pemilik task; `BE-LAB-16` ditambahkan pada roadmap backend. Total task backend menjadi 19 | `DRAFT` |
| 5 | 2026-09-02 | Audit cakupan diperluas ke aturan validasi, entity, kewenangan, dan integrasi. Seluruhnya berpemilik. Temuan terpenting: `VAL-09`, aturan empat mata pada tingkat wadah, semula tidak dikutip task mana pun — kini dibebankan ke `BE-LAB-12`. Tujuh dimensi cakupan kini terperiksa | `DRAFT` |
| 29 | 2026-09-07 | **Pembaruan bukti pelaksanaan frontend, ditulis `build-module-frontend`.** `FR-10.2` kini punya bukti pada **kedua sisinya** setelah `FE-LAB-09` selesai, dan dengan itu **seluruh sembilan task frontend Laboratorium selesai**. `AC-41` ditegakkan secara struktural di layar: ketiga disiplin menunjuk objek definisi penyaring yang sama, dan ujinya memeriksa identitas objek — bukan kesamaan isi — sehingga percabangan pertama langsung tertangkap. Keputusan `LAB-DEC-025` dijaga dua arah: penyaringnya tidak punya ruas disiplin, dan parameter permintaannya tidak pernah membawanya. Satu batas dicatat: verifikasi manual seluruh task frontend yang tertunda — `FE-LAB-05`, `FE-LAB-07`, `FE-LAB-08`, `FE-LAB-09` — belum dijalankan, menunggu backend dijalankan dan data induk perujuk diisi | `DRAFT` |
| 28 | 2026-09-07 | **Pembaruan bukti pelaksanaan frontend, ditulis `build-module-frontend`.** `FR-07.3` berpindah dari `Direncanakan` menjadi **`SELESAI`** lewat `FE-LAB-08`, sehingga `FR-04.1` sampai `FR-04.3` kini punya bukti pada kedua sisinya. Invariant `LAB-FE-006` ditegakkan dua lapis di layar, dan ujinya menelusuri seluruh pilihan pengurutan yang ditawarkan — bukan satu contoh yang dipilih tangan. Ancaman terhadap invariant itu datang dari tempat yang tidak terduga: `DataTable` mengurutkan ulang datanya sendiri secara bawaan, yang akan menaikkan pemeriksaan biasa di atas cito. `VAL-39` dijaga uji tersendiri. Satu batas dicatat: verifikasi manual delapan skenario belum dijalankan, menunggu backend dan data pekerjaan yang memadai | `DRAFT` |
| 27 | 2026-09-07 | **Pembaruan bukti pelaksanaan frontend, ditulis `build-module-frontend`.** `FR-07.2` berpindah dari `Direncanakan` menjadi **selesai untuk source dan uji** lewat `FE-LAB-07`. Kedua invariant keselamatan `LAB-FE-009` dan `LAB-FE-010` kini punya bukti: yang pertama berlapis tiga — pada data lewat `resolveSpecimenActions`, pada kartu wadah sebagai daftar terbuka, dan sekali lagi di dalam dialog penolakan; yang kedua muncul sebagai pesan utama dialog, sebelum tombol konfirmasi. `VAL-13` dijaga uji yang memeriksa katalog aksi tidak memuat satu pun aksi berlingkup pemeriksaan. Baris `FR-02.1`, `FR-02.2`, `FR-02.3`, dan `FR-02.5` sisi backendnya sudah selesai sejak `BE-LAB-12` dan `BE-LAB-16`; sisi layarnya kini ikut berdiri. **Satu kerusakan di luar Laboratorium ditemukan dan diperbaiki pada sesi yang sama:** merge `c71c02a07` membawa lima kelompok deklarasi kembar `billing-management` yang membuat `npm run build` gagal — nol error menunjuk ke `laboratory-management`. Salah satunya `addCase` kembar yang membuat Redux Toolkit melempar saat store dibentuk, sehingga seluruh aplikasi tidak dapat dijalankan. Atas instruksi eksplisit pemilik repository, salinan keduanya dibuang dan kedua gerbang kembali lolos. Yang tersisa hanya verifikasi manual delapan skenario, menunggu backend dijalankan | `DRAFT` |
| 26 | 2026-09-07 | **Pembaruan bukti pelaksanaan frontend, ditulis `build-module-frontend`.** Kelima baris `FR-08.1` sampai `FR-08.5` kini **`SELESAI`** seluruhnya: sisi backend terbukti lewat `BE-LAB-08`, sisi layar lewat `FE-LAB-05`. `AC-50` ditegakkan **secara struktural** pada layar — formulirnya tidak punya satu pun kotak isian nama perujuk, dan uji memeriksa muatan yang dikirim juga tidak punya ruas namanya. Satu penahan yang tidak terduga ditutup pada sesi yang sama: `MstReferralInstitution` dan `MstReferralDoctor` ternyata tidak punya endpoint sama sekali, sehingga daftar perujuk tidak punya sumber; dua endpoint bacanya dibangun, dan butir Verifikasi `BE-EXT-02` *"kedua data induk dapat dipilih dari daftar"* yang selama ini tidak pernah terpenuhi kini terpenuhi. Satu batas dicatat: verifikasi manual delapan skenario **belum dijalankan**, menunggu data induk perujuk diisi | `DRAFT` |
| 25 | 2026-09-07 | **Pembaruan bukti pelaksanaan, ditulis `build-module-backend`.** Kelima baris `FR-08.1` sampai `FR-08.5` berpindah dari `Direncanakan`/`Menunggu` menjadi **backend selesai**, dengan `FR-08.4` **selesai seluruhnya** karena tidak punya pasangan frontend. Penahan yang selama ini melintang — pelaksana `INT-05` yang belum berpemilik — dicabut `BE-LAB-08` dengan membangunnya di sisi Registrasi. `AC-44`, `AC-45`, `AC-46`, dan `AC-50` seluruhnya **terbukti**; `AC-45` dibuktikan dua kali dengan cara yang berbeda. Dengan ini **`FE-LAB-05` tidak lagi terblokir** — ketiga endpoint yang dibutuhkannya tersedia dan terdokumentasi Swagger | `DRAFT` |
| 24 | 2026-09-04 | **Pembaruan bukti pelaksanaan frontend, ditulis `build-module-frontend`.** `FE-LAB-06` selesai: layar pesanan, formulir pemesanan, dan layar detail berdiri, dengan penanda cito dan duplo melekat pada baris pemeriksaan. `AC-40` dijaga uji unit lewat pemeriksaan bahwa daftar pesanan tidak punya kolom kesegeraan. Dua hal dicatat: **`FE-LAB-05` ditandai `BLOCKED`** karena `BE-LAB-08` belum ada sama sekali pada source backend, dan dependency itu diwaive pemilik modul agar `FE-LAB-06` dapat berjalan; serta satu batas kontrak — `LabOrderDetailResponse` tidak membawa `requestedByUserId`, sehingga layar belum dapat menyembunyikan tombol cito dari dokter yang bukan pemesan. Ini temuan ketiga dari pola yang sama setelah `VAL-33` dan `LabRejectionReason : SystemFlag` | `DRAFT` |
| 23 | 2026-09-04 | **Pembaruan bukti pelaksanaan frontend, ditulis `build-module-frontend`.** `FE-LAB-04` selesai, sehingga keempat baris `FR-09.1` sampai `FR-09.4` kini punya bukti dari kedua sisi. Menu tarif berdiri baca saja — tanpa kolom aksi, tanpa tombol ubah, dan tanpa satu pun fungsi maupun thunk tulis — sehingga `AC-48` dijaga sejak di layar, bukan hanya ditolak backend. Komponen pemilih katalog berdiri beserta perhitungan `AC-43`-nya, dan sengaja belum dipasang karena konsumennya `FE-LAB-06`. Dengan ini **gelombang `MVP-0` frontend selesai seluruhnya** | `DRAFT` |
| 22 | 2026-09-04 | **Pembaruan bukti pelaksanaan frontend, ditulis `build-module-frontend`.** `FE-LAB-03` selesai: `FR-07.5` berpindah dari `Direncanakan` menjadi **`SELESAI`**. Tiga layar alasan penolakan berdiri, dan `LAB-FE-012` ditegakkan empat lapis dengan empat uji unit menjaganya. Dua batas dibuka: grup endpoint ini tidak punya `GET /{id}` sehingga fitur ini tidak dapat punya halaman detail dan formulir ubahnya memuat baris dari daftar; dan frontend belum menerima daftar permission per pengguna, sehingga penyembunyian aksi Setel Penanda Sistem hanya dapat sedekat peran, bukan sedekat `LabRejectionReason : SystemFlag`. Keduanya menuntut pekerjaan backend | `DRAFT` |
| 21 | 2026-09-04 | **Pembaruan bukti pelaksanaan frontend, ditulis `build-module-frontend`.** `FE-LAB-02` selesai: `FR-07.4` berpindah dari `Direncanakan` menjadi **`SELESAI`**. Enam layar batas nilai berdiri, dan invariant `LAB-FE-011` ditegakkan tiga lapis dengan dua di antaranya dijaga uji unit. Tiga temuan dibuka: `AC-34` belum memuat pelaku karena `LabValueBoundHistoryResponse` hanya membawa penunjuk pengguna tanpa nama sehingga tidak boleh ditampilkan; status sebelas endpoint Lab Value Bound dan Lab Critical Bound Approval pada `contracts/api-contract.md` masih tertulis `Rencana (belum tersedia)` padahal seluruhnya sudah ada sejak `BE-LAB-04` dan `BE-LAB-05`; dan peran pemegang `LabCriticalBound : Approve` yang sudah tercatat pada bagian 4.3 kini benar-benar menahan pemakaian layar pengajuan | `DRAFT` |
| 20 | 2026-09-04 | **Pembaruan bukti pelaksanaan frontend, ditulis `build-module-frontend`.** `FE-LAB-01` selesai: kerangka tujuh lapis modul `laboratory-management` berdiri di frontend beserta kontrak penanganan state yang dipakai seluruh layar berikutnya. `CAP-22` dan `CAP-23` berpindah dari sekadar `Ready to reuse` menjadi **benar-benar terpakai**, dengan buktinya. Task ini tidak memindahkan satu pun baris FR karena memang tidak menjawab requirement bisnis; ia menopang sembilan task frontend sesudahnya. Satu catatan lingkungan dibuka: script `npm run test:unit` memakai pola glob yang tidak dikembangkan test runner Node 20 yang terpasang, sehingga suite hanya berjalan lewat bentuk perintah `--test tests/unit/` | `DRAFT` |
| 19 | 2026-09-04 | **Pembaruan bukti pelaksanaan, ditulis `build-module-backend`.** Kelima baris `FR-09.1` sampai `FR-09.5` berpindah menjadi **`SELESAI`** oleh `BE-LAB-07`. `AC-43` terbukti tanpa satu baris tagihan pun terbentuk, `AC-47` dan `AC-48` terbukti lewat ketiadaan — nol entity tarif dan nol jalur ubah — dan `AC-51` terbukti lewat `VAL-46`. Satu batas dicatat: penegakan `INV-22` menuntut disiplin pesanan **dan** disiplin katalog sama-sama diketahui, sehingga cakupannya masih bergantung pada pengisian nilai disiplin yang menunggu penggolongan dari pihak klinis | `DRAFT` |
| 18 | 2026-09-04 | **Dependency eksternal dikerjakan, ditulis `build-module-backend`.** `BE-EXT-01`, `BE-EXT-02`, dan `BE-EXT-03` tidak lagi menahan siapa pun: kolom `MstProcedure.LabDiscipline`, dua data induk perujuk, dan dua penunjuk perujuk pada `TrxPatientEncounter` semuanya sudah ada dan sudah diterapkan ke dev pemilik. Lima baris `FR-08.2`, `FR-08.3`, `FR-08.4`, `FR-09.1`, dan `FR-09.5` berpindah dari `Menunggu eksternal` menjadi menunggu task Laboratorium sendiri — kecuali `FR-08.4`, yang idempotensinya masih menuntut endpoint `INT-05` milik `registration-management`. Bentuk teknis `INT-05` ditulis pada `contracts/integration-contract.md` bagian 2b | `DRAFT` |
| 17 | 2026-09-04 | **Pembaruan bukti pelaksanaan, ditulis `build-module-backend`.** `FR-10.1` dan `FR-10.2` berpindah menjadi **`SELESAI`** oleh `BE-LAB-15`. `AC-42` terbukti bersama `AC-19`: penelusuran seluruh tipe, anggota, entity, dan route Laboratorium tidak menemukan satu pun yang melayani Bank Darah maupun stok, pembelian, dan pemakaian reagen | `DRAFT` |
| 16 | 2026-09-04 | **Pembaruan bukti pelaksanaan, ditulis `build-module-backend`.** Keempat baris `FR-04.1` .. `FR-04.4` berpindah menjadi **`SELESAI`** oleh `BE-LAB-14`. `FR-01.4` ikut naik dari `SELESAI SEBAGIAN` menjadi **`SELESAI`**: kolom `CitoTurnaroundMinutes` yang disediakan `BE-LAB-02` kini benar-benar dipakai menghitung keterlambatan, sehingga `AC-17` yang selama ini menunggu akhirnya terbukti | `DRAFT` |
| 15 | 2026-09-04 | **Pembaruan bukti pelaksanaan, ditulis `build-module-backend`.** `FR-01.1`, `FR-01.2`, dan `FR-01.3` berpindah menjadi **`SELESAI`** oleh `BE-LAB-10`: kesegeraan dan penanda duplo melekat pada pemeriksaan, `VAL-03` menjawab `403` dan `VAL-04` menjawab `409`, dan setiap penandaan meninggalkan satu baris riwayat berlingkup `LabExamination`. `AC-40` dijaga dua arah — penanda duplo hanya mengenai baris yang ditandai, dan ketiadaan endpoint kesegeraan pada grup `Lab Order` ikut diuji | `DRAFT` |
| 14 | 2026-09-04 | **Pembaruan bukti pelaksanaan, ditulis `build-module-backend`.** `FR-02.4` dan `FR-02.6` berpindah menjadi **`SELESAI SEBAGIAN`** oleh `BE-LAB-11`: keenam kolom salinan tarif lepas dari `LabSpecimen` dan utuh pada `LabExamination`, dan migration `SplitLabSpecimenIntoExamination` berikut skrip maju dan mundurnya sudah ada. Migration dijalankan dua arah terhadap `QuilvianNewDevYoga` — maju, mundur, lalu maju lagi — sehingga keduanya menjadi **`SELESAI`**. `LAB-OPEN-012` tetap terbuka untuk produksi, tetapi prasyaratnya kini ditegakkan migration itu sendiri, dan penjaganya lolos dua kali pada dev pemilik sehingga jawaban `0` terverifikasi ulang oleh mesin. Empat baris `FR-05.1` .. `FR-05.4` yang masih tertulis `Direncanakan` diperbarui mengikuti `BE-LAB-13` yang sudah selesai, termasuk `AC-13` yang terbukti 2026-09-04 | `DRAFT` |
| 13 | 2026-09-03 | **Pembaruan bukti pelaksanaan, ditulis `build-module-backend`.** `FR-02.2`, `FR-02.3`, dan `FR-02.5` diperbarui oleh `BE-LAB-12`: `AC-36` terbukti — menolak wadah menggugurkan seluruh pemeriksaan yang ditopangnya — dan `VAL-13` ditegakkan secara struktural. `VAL-05` sampai `VAL-15` masing-masing punya ujinya, termasuk `VAL-09` empat mata yang ditulis sebagai kode di dalam service. `FR-02.4` dan `FR-02.6` keluar dari `BLOCKED` setelah `LAB-OPEN-012` dijawab dengan angka `0`. `AC-37` baru terbukti pada tingkat status; penerbitan fakta per pemeriksaan tetap milik `BE-LAB-13` | `DRAFT` |
| 12 | 2026-09-03 | **Pembaruan bukti pelaksanaan, ditulis `build-module-backend`.** `FR-02.1` naik dari `SELESAI SEBAGIAN` menjadi **`SELESAI`**: `BE-LAB-16` melengkapi entity dari `BE-LAB-09` dengan empat endpoint, sehingga `AC-35` kini terbukti ujung-ke-ujung — dua pemeriksaan ditambahkan lewat `POST /by-order` lalu terbaca lewat `GET /by-specimen` dengan satu barcode yang sama. `VAL-17` .. `VAL-20` masing-masing punya ujinya, dan batas terpenting task itu terbukti: membatalkan satu pemeriksaan tidak mengubah pemeriksaan lain maupun status wadahnya | `DRAFT` |
| 11 | 2026-09-03 | **Pembaruan bukti pelaksanaan, ditulis `build-module-backend`.** `FR-02.1` berpindah dari `Direncanakan` menjadi **`SELESAI SEBAGIAN`**: entity `LabExamination` tuntas pada `BE-LAB-09` berikut pembuatan dan eksekusi migration dua arah, sementara endpoint beserta penolakannya menunggu `BE-LAB-16`. `AC-35` dan `AC-40` terbukti pada tingkat struktur. Satu risiko dicatat: keenam kolom yang harus pindah dari `TrxLabSpecimen` belum dipindahkan karena `BE-LAB-11` masih `BLOCKED` oleh `LAB-OPEN-012`, sehingga salinan tarif untuk sementara ada di dua tempat. Satu pertentangan dokumen dicatat: kamus data bagian 4 menuntut kolom baru pada `TrxLabTransitionHistory`, sedangkan roadmap bagian 8.3 menyatakan tabel itu tanpa pekerjaan struktur | `DRAFT` |
| 10 | 2026-09-03 | **Pembaruan bukti pelaksanaan, ditulis `build-module-backend`.** Ketiga baris `EPIC-LAB-06` berpindah dari `Direncanakan` menjadi **`SELESAI`**: `BE-LAB-06` tuntas pada tingkat source dan test, tanpa menyentuh schema sehingga tanpa migration. `AC-26` terbukti seluruh jalur — menambah, menonaktifkan, penolakan `VAL-37` dua arah, dan penyetelan sah oleh administrator sistem. `FR-06.3` terbukti lewat `LabRejectionReasonSeeder` yang mengisi sepuluh alasan baseline tanpa menimpa keputusan pengguna. Satu risiko organisasi dicatat pada `FR-06.2`: pemegang `LabRejectionReason : SystemFlag` belum ditetapkan, sehingga alasan baru selalu bernilai "bukan kesalahan internal" dan pengambilan ulangnya dapat ditagihkan kepada pasien sampai peran itu ada | `DRAFT` |
| 9 | 2026-09-02 | **Pembaruan bukti pelaksanaan, ditulis `build-module-backend`.** `FR-10.3` berpindah dari `Direncanakan` menjadi **`SELESAI`**: `BE-LAB-01` tuntas pada tingkat source, test, pembuatan migration, dan eksekusi migration ke `QuilvianNewDevYoga` beserta pembuktian jalur `Down`. `AC-11` terbukti lewat tiga skenario kunjungan; `AC-41` terbukti separuh karena daftar pantau per disiplin adalah cakupan `BE-LAB-15`. Satu temuan lintas modul dibuka sebagai `LAB-REQ-003` — penyimpangan status `FINAL`/`CLOSED` pada Billing yang mematikan koreksi AR. Catatan pembukuan: revision 6 sampai 8 tidak pernah tercatat pada tabel ini walaupun kepala dokumen sudah menyebut revision 8 — selisih itu peninggalan sebelum task ini dan menjadi utang pemilik blueprint | `DRAFT` |

---

## Traceability gelombang `MVP-7` — `S4b` (2026-09-21)

| Field | Nilai |
|---|---|
| Masukan | decisions **rev 50**; capability map **rev 4** |
| Kontrak | `LAB-API-v1` `r26`, `LAB-VAL-v1` `r9`, `LAB-PERM-v1` rev 8 — ketiganya `approved` 2026-09-21 |
| Source | backend `981e002c`, frontend `ebef7ebe5` |

### Keputusan → task → kontrak → AC

| Keputusan | Task backend | Task frontend | Kontrak | AC |
|---|---|---|---|---|
| `LAB-DEC-095` hasil per pemeriksaan | — (bentuknya sudah benar sejak `r24`) | `FE-LAB-30` | `r24` 19.2 | `AC-156` |
| `LAB-DEC-096` waktu turunan | `BE-LAB-58` ✅ | `FE-LAB-33` | `r26` 21.3 | `AC-157` ✅ |
| `LAB-DEC-097` Final bukan rilis | `BE-LAB-53`, `BE-LAB-54` | `FE-LAB-33` | `r26` 21.2 | `AC-158`, `AC-159` |
| `LAB-DEC-098` Spesifik Specimen | `BE-LAB-55` | `FE-LAB-32` | `r26` 21.5 | `AC-160`, `AC-161` |
| `LAB-DEC-099` isi awal disaring | `BE-LAB-55` (tabel saja) | — | `r26` 21.5 | `AC-161` |
| `LAB-DEC-100` satuan volume | `BE-LAB-55` (seed `MstMeasurement`) | `FE-LAB-32` | — | `AC-162` |
| `LAB-DEC-101` MIC dan zona opsional | — (sudah benar sejak `r24`) | `FE-LAB-31` | `r24` 19.2 | `AC-163` |
| `LAB-DEC-102` nol subbakteri | — (sudah benar sejak `r24`) | `FE-LAB-31` | `r24` 19.2 | — |
| `LAB-DEC-103` aturan kritis | `BE-LAB-56`, `BE-LAB-58` | `FE-LAB-31` | `r26` 21.6 | `AC-166`, `AC-167` |
| `LAB-DEC-104` wajib bersyarat | `BE-LAB-54`, `BE-LAB-56` | `FE-LAB-31` | `LAB-VAL-v1` `r9` | `AC-163`, `AC-164`, `AC-165` |
| `LAB-DEC-105` Analis turunan | `BE-LAB-58` ✅ | `FE-LAB-33` | `r26` 21.3 | `AC-168` ✅ |
| `LAB-DEC-106` `Definitif` fakta | `BE-LAB-53`, `BE-LAB-54` | `FE-LAB-33` | `r26` 21.2 | `AC-169` |
| `LAB-DEC-107` koreksi specimen | `BE-LAB-57` ✅ | `FE-LAB-32` | `r27` 21.4 | `AC-170` ✅ |
| `LAB-DEC-109` nol HL7 | — (ketiadaan) | `FE-LAB-33` (ketiadaan) | `r26` 21.8 | `AC-172` |
| `LAB-DEC-111` dokter konfirmator | `BE-LAB-59` ✅ | `FE-LAB-33` | `r26` 21.7 | `AC-173` ✅, `AC-174` ✅; `AC-171` milik layar |
| `LAB-DEC-112` jejak ruas | `BE-LAB-57` ✅ | `FE-LAB-32` | `r27` 21.4 | `AC-175` ✅ |
| `LAB-DEC-113` status temuan | `BE-LAB-53` | `FE-LAB-31` | `r24` 19.2 | `AC-176` |

### AC → task yang membuktikannya

| AC | Task pembukti | Lapis |
|---|---|---|
| `AC-156` | `FE-LAB-30` | Integrasi + UI |
| `AC-157` | `BE-LAB-58`, `FE-LAB-33` | Kontrak + UI |
| `AC-158`, `AC-159` | `BE-LAB-54`, `FE-LAB-33` | Integrasi |
| `AC-160`, `AC-161` | `BE-LAB-55`, `FE-LAB-32` | Integrasi |
| `AC-162` | `FE-LAB-32` | Unit + UI |
| `AC-163`, `AC-164`, `AC-165` | `FE-LAB-31` | Integrasi |
| `AC-166`, `AC-167` | `BE-LAB-56`, `FE-LAB-31` | Integrasi + UI |
| `AC-168` | `BE-LAB-58`, `FE-LAB-33` | Kontrak + UI |
| `AC-169` | `BE-LAB-54`, `FE-LAB-33` | Integrasi + UI |
| `AC-170` | `BE-LAB-57`, `FE-LAB-32` | Integrasi |
| `AC-171`, `AC-173`, `AC-174` | `BE-LAB-59`, `FE-LAB-33` | Integrasi |
| `AC-172` | `FE-LAB-33` | Kontrak + UI |
| `AC-175` | `BE-LAB-57` | Integrasi |
| `AC-176` | `BE-LAB-53`, `FE-LAB-31` | Kontrak + UI |

### Keputusan yang TIDAK melahirkan task, dan alasannya

| Keputusan | Kenapa nol task |
|---|---|
| `LAB-DEC-095`, `101`, `102` | Bentuknya sudah benar sejak rancangan 2026-09-18. Putaran 9 **menegakkan**, bukan mengubah |
| `LAB-DEC-108` | Butir 2 digantikan `LAB-DEC-111`; butir 1 (DPJP) sudah tercakup `BE-LAB-59` |
| `LAB-DEC-110` | Bentuk cetak menunggu `LAB-OPEN-039` |

### Penahan yang melekat pada gelombang ini

| Penahan | Menahan apa | Tidak menahan apa |
|---|---|---|
| `DEC-LAB-011` | Seluruh `S4d` — nol task validasi/rilis | Seluruh `S4b` |
| `LAB-OPEN-040` | Pengisian `LabSpecimenDetailType` | `BE-LAB-55` membangun tabelnya |
| `LAB-OPEN-041` | Pengisian `LabMicrobiologyCriticalRule` | `BE-LAB-56` membangun tabel dan evaluatornya |
| `LAB-OPEN-039` | Bentuk akhir cetak | Seluruh pengisian hasil |
| `LAB-COORD-011/012/013` | Pengiriman, HL7, cetak dwibahasa | Seluruh `S4b` |
| `LAB-COORD-014` | Ketepatan daftar dokter jaga | `BE-LAB-59` — jalur jatuhnya dibangun lebih dulu |
| `LAB-OPEN-042` | Aturan isolat pada temuan `Negatif` | Nol — aturan itu tidak dibangun |
| `LAB-RDY-C04` | — | Verifikasi wajib pemeriksaan database sungguhan |

### Catatan penomoran AC

Penomoran `AC-156`..`AC-176` melompat dari `AC-99`. Roadmap sudah terlanjur mengalokasikan
`AC-100`..`AC-155` secara lokal tanpa mendaftarkannya di tabel acceptance criteria — `BE-LAB-44`
misalnya mencatat `AC-98`..`AC-100` **terbukti pada database**. Penomoran gelombang ini digeser
`+56` supaya tidak menimpa nomor yang sudah dipakai pekerjaan selesai. **Nol nomor roadmap lama
diubah.**

---

## Traceability gelombang `MVP-7b` — `S4b` sesudah bukti cetak (2026-09-21)

| Field | Nilai |
|---|---|
| Masukan | decisions **rev 52**; `LAB-EVD-005`, `LAB-EVD-006` |
| Kontrak | `LAB-API-v1` `r27`, `LAB-VAL-v1` `r10`, `LAB-PERM-v1` rev 9 — `approved` 2026-09-21 |

### Keputusan → task → AC

| Keputusan | Backend | Frontend | AC |
|---|---|---|---|
| `LAB-DEC-114` kualifikasi hasil | `BE-LAB-53` *(cakupan bertambah)* | `FE-LAB-33` | `AC-177` |
| `LAB-DEC-115` satuan MIC | `BE-LAB-61` | `FE-LAB-31` | `AC-178` |
| `LAB-DEC-116`+`124` dua penanda | `BE-LAB-53` *(cakupan bertambah)* | `FE-LAB-31`, `FE-LAB-33` | `AC-179`, `AC-188` |
| `LAB-DEC-117` nomor cetak | `BE-LAB-63` ✅ | — | `AC-180` ✅ |
| `LAB-DEC-118` tanggal cetak | `BE-LAB-58` *(dikoreksi 2026-09-22 — ia ruas TURUNAN pada jalur baca `r27` 22.3, bukan milik `BE-LAB-63`)* | — | `AC-181` ✅ |
| `LAB-DEC-119`+`127` pengaturan disiplin | `BE-LAB-63` ✅ | `FE-LAB-34` ⚠ | `AC-182` ✅ |
| `LAB-DEC-120` petugas otorisasi | `BE-LAB-58` *(dikoreksi 2026-09-22)* — **dan nilainya tetap kosong sampai `S4d` dibuka `DEC-LAB-011`** | `FE-LAB-33` | `AC-183` ⚠ **terbuka** — sisi positifnya nol dapat diuji sampai `S4d` dibuka |
| `LAB-DEC-121` permintaan klinis | — *(dokumen)* | — | `AC-184` |
| `LAB-DEC-122` breakpoint + UG | `BE-LAB-60` ✅ + **`BE-LAB-64`** ✅ *(permukaan baseline)* | `FE-LAB-34` ⚠ | `AC-185` |
| `LAB-DEC-123` interpretasi terhitung | `BE-LAB-61` | `FE-LAB-31` | `AC-186`, `AC-187` |
| `LAB-DEC-125` profil katalog | `BE-LAB-62` ✅ + **`BE-LAB-64`** ✅ *(permukaan baseline)* | `FE-LAB-34` ⚠ | `AC-189` |
| `LAB-DEC-126` isolat tanpa uji | `BE-LAB-61` | `FE-LAB-31` | `AC-190` |
| `LAB-DEC-128` zona nol | `BE-LAB-61` | `FE-LAB-31` | `AC-191` |

### Task yang berubah cakupan, bukan bertambah

| Task | Semula | Sesudah |
|---|---|---|
| `BE-LAB-53` | 6 kolom + 1 enum | **9 kolom + 4 enum** |
| `FE-LAB-31` | Satu bentuk tabel antibiogram | **Dua bentuk**, kolom hasil terhitung |
| `FE-LAB-33` | Tombol aksi dan Diagnostic Report | **+ tiga pemilih baru** |

### Penahan gelombang ini

| Penahan | Menahan | Tidak menahan |
|---|---|---|
| `DEC-LAB-011` | Seluruh `S4d`; ruas `Petugas Otorisasi` **tampil kosong** | Pembangunan ruasnya |
| `LAB-OPEN-039` | Empat bentuk cetak yang belum terlihat | Seluruh task di atas |
| `LAB-OPEN-041` sekerabat | **Pengisian** breakpoint | `BE-LAB-60` membangun tabelnya |
| Daftar panel antibiotik resmi | Pengisian `DiscContentUg` | `BE-LAB-60` membangun kolomnya |

### Catatan penomoran

Gelombang ini memakai `AC-177`..`AC-191`, melanjutkan `AC-156`..`AC-176` milik `MVP-7`.
Penjelasan lompatan dari `AC-99` ada pada traceability `MVP-7` dan tabel AC decision log.
