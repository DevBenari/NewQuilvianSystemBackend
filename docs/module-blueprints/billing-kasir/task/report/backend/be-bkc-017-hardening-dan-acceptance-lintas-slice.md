# BE-BKC-017 — Hardening dan Acceptance Lintas-Slice (slice: menutup `BIL-AT-011` dan `BIL-AT-018`)

- TASK ID: `BE-BKC-017`
- TASK TYPE: Backend hardening — penambahan test regresi untuk menutup gap acceptance yang sudah tercatat, bukan fitur/endpoint baru
- COMPLEXITY: LIGHT (dua test baru, tidak ada perubahan kontrak API/model/DTO)
- CLASSIFICATION SCORE: NOT APPLICABLE (test-only slice, tidak melalui klasifikasi task fitur)
- MODEL: Claude Sonnet 5
- TASK MODE: `MODULE BLUEPRINT` (mengikuti blueprint `BIL-CASH-001` revisi `0.4`, roadmap backend `BE-BKC-017`)
- WRITE TARGET: `NewQuilvianSystemBackend` — hanya `QuilvianSystemBackend.Tests/BillingManagement/` dan dokumen evidence/laporan modul `billing-kasir`. Tidak ada migration/model snapshot yang dibuat atau diubah (wewenang tulis eksplisit dibatasi pengguna pada task ini)
- FILES INSPECTED: `docs/module-blueprints/billing-kasir/{MODULE-STATUS.md, roadmap/backend-roadmap.md, roadmap/requirement-traceability.md, testing/acceptance-test-matrix.md, evidence/06-be-bkc-017-acceptance-evidence-matrix.md}`; `Areas/HealthServices/BillingManagement/Billing/Services/{BillingCalculationService.cs, BillingFinalizationService.cs}`; `Areas/HealthServices/BillingManagement/Billing/Models/{MstAdministrationFeePolicy.cs, BilFinalizationRecord.cs}`; `QuilvianSystemBackend.Tests/BillingManagement/{BillingCalculationServiceTests.cs, BillingDiscountServiceTests.cs, BillingFinalizationServiceTests.cs}`
- FILES CHANGED:
  - `QuilvianSystemBackend.Tests/BillingManagement/BillingCalculationServiceTests.cs` — tambah `AdministrationFeeCoverableFlagGatesWhetherInsurerCanCoverIt`; tambah parameter opsional `coverable` (default `false`, tidak mengubah pemanggil lama) pada helper `AdministrationPolicy(...)`
  - `QuilvianSystemBackend.Tests/BillingManagement/BillingFinalizationServiceTests.cs` — tambah `DamaDepartureExceptionWithDebtorEvidenceIsAllowedAndInvoiceIsNotMarkedPaid`
  - `docs/module-blueprints/billing-kasir/evidence/06-be-bkc-017-acceptance-evidence-matrix.md` — perbarui status `BIL-AT-011` (`Partial` → `Covered (catatan)`) dan `BIL-AT-018` (`Partial` → `Covered`), ringkasan jumlah per status, daftar gap terbuka, dan angka hasil `dotnet test`
- IMPLEMENTATION: Trace repo (roadmap + evidence matrix `06-be-bkc-017-acceptance-evidence-matrix.md` yang sudah ada dari pengerjaan sebelumnya) menunjukkan 24 acceptance ID `BE-BKC-017` sudah 20 `Covered`/`Covered (catatan)`, 3 `Partial`, 1 `Tidak ditemukan`. Dari 3 `Partial`, dua di antaranya (`BIL-AT-011`, `BIL-AT-018`) murni butuh SATU test tambahan masing-masing untuk jadi `Covered` — tidak butuh perubahan source produksi, tidak butuh migration, tidak butuh akses database relasional nyata. Task ketiga yang `Partial` (`BIL-AT-020`, concurrency lock Postgres nyata) dan satu yang `Tidak ditemukan` (`BIL-AT-023`, terikat `BKC-BLK-INT-001`) sengaja TIDAK dikerjakan pada slice ini — keduanya butuh lingkungan/keputusan yang bukan wewenang task ini (lihat *Known Issues*).

  1. `BIL-AT-011` ("Coba diskon admin; insurer cover flag `true`" → "Diskon ditolak; coverage mengikuti policy"): separuh kriteria (diskon admin fee ditolak) sudah dibuktikan `BillingDiscountServiceTests.AdministrationFeeCategoryCannotBeDiscounted`. Separuh yang belum ada bukti eksplisit adalah flag `MstAdministrationFeePolicy.Coverable` benar-benar menjadi gerbang policy pada coverage waterfall (`BillingCalculationService.BuildCoverageComponents`/`ApplyCoverageWaterfall`, `coverableAmount = components.Where(x => x.Coverable).Sum(...)`). Test baru membuktikan kontras langsung: dengan `Coverable=true`, penjamin BOLEH menanggung penuh item + admin fee (`Primary=120.000` dari eligible `120.000`, `PatientAmount=0`); dengan `Coverable=false`, permintaan coverage yang SAMA (`Primary=120.000`) ditolak `BillingCalculationValidationException` ("melebihi biaya yang memenuhi syarat") karena admin fee tidak ikut `coverableAmount` (hanya `100.000` dari item).
  2. `BIL-AT-018` ("Death partial family payment dan DAMA unpaid" → "Departure boleh; AR ke family/lawful debtor; bukan PAID"): jalur Death+debtor sukses dan jalur DAMA-tanpa-debtor (ditolak) sudah ada test. Yang belum ada adalah jalur DAMA DENGAN bukti debtor (sukses) — test baru membuktikan `FinalizeAsync` dengan `DepartureReason=DAMA` + `DebtorIdentity`/`DebtorRelationship` terisi menghasilkan `IsDepartureException=true`, `InvoiceStatus=Final` (BUKAN status "PAID"), `OutstandingAtFinalization` tetap tercatat (`250.000`, tidak dianggap lunas), dan `BilFinalizationRecord.DepartureReason="DAMA"` + `DebtorRelationship` tersimpan sebagai bukti siapa yang menanggung.
- BLUEPRINT STATUS/EVIDENCE: Blueprint `BIL-CASH-001` revisi `0.4`, status `approved`; roadmap `BE-BKC-017` acceptance `24 acceptance IDs memiliki bukti`. Setelah slice ini: **22/24** `Covered`/`Covered (catatan)`, 1 `Partial` (`BIL-AT-020`, butuh Postgres nyata), 1 `Tidak ditemukan` (`BIL-AT-023`, blocked `BKC-BLK-INT-001`). Detail lengkap per ID ada di `evidence/06-be-bkc-017-acceptance-evidence-matrix.md` (diperbarui task ini).
- API CONTRACT IMPACT: NONE — tidak ada endpoint/DTO/response shape yang berubah.
- DATABASE IMPACT: NONE — tidak ada model, migration, atau schema yang disentuh pada slice ini. Test memakai `IsolatedBillingDbContextFactory` (EF Core InMemory), bukan Postgres nyata.
- SECURITY IMPACT: NONE — tidak ada perubahan authorization/authentication/privacy. Kedua test baru murni domain-level (kalkulasi finansial dan finalisasi invoice).
- VISUAL REFERENCE: NOT REQUIRED (backend, test-only)
- VALIDATION:
  - `dotnet build` → **0 Error** (199 warning, seluruhnya pre-existing di file/area lain, tidak terkait perubahan task ini)
  - `dotnet test QuilvianSystemBackend.Tests --filter "FullyQualifiedName~BillingManagement" --no-build` → **190/190 pass** (naik dari 188 sebelum slice ini; 2 test baru keduanya lulus pada percobaan pertama)
- WARNINGS: NONE dari perubahan task ini.
- KNOWN ISSUES (sengaja tidak dikerjakan slice ini, tercatat di evidence matrix):
  1. `BIL-AT-020` — korektnya *hasil* optimistic concurrency (satu sukses, satu `409`) sudah terbukti sekuensial lewat EF InMemory; penguncian Postgres nyata (`pg_advisory_xact_lock`) tidak pernah tereksekusi test manapun di suite ini karena `IsRelational() == false` pada harness test. Menutup ini butuh test terhadap Postgres lokal nyata, di luar scope test-only slice ini.
  2. `BIL-AT-023` — terikat penuh `BKC-BLK-INT-001` (belum ada consumer AR/AP eksternal nyata untuk disimulasikan downtime-nya). Tidak bisa ditutup dari sisi kode Billing sendiri.
  3. Sanitized Swagger examples (bagian scope `BE-BKC-017` di luar 24 acceptance ID) — belum dikerjakan sama sekali, tidak disentuh slice ini.
  4. **Temuan terpisah, di luar scope task ini tapi relevan**: migration `Migrations/20260828045534_AddBillingLatestChanges.cs` (dibuat dan sudah di-commit pengguna sendiri di luar sesi ini, commit `7c2ed4e "fix migration"`) masih menambahkan kolom+index `KwitansiNumber` pada tabel `BilInvoice` (`AddColumn`/`CreateIndex` baris 16-30), padahal model `BilInvoice.cs` saat ini TIDAK lagi punya properti `KwitansiNumber` (dipindah ke `BilTender` pada task backend sebelumnya, `BKC-DEC-057`). Migration yang sama JUGA sudah benar menambahkan `KwitansiNumber` ke `BilTender`. Kemungkinan penyebab: migration ini digenerate saat migration lama `20260827065538_AddKwitansiNumberToBilInvoice.cs` (yang sengaja tidak disentuh sesuai instruksi task sebelumnya) masih ada di folder `Migrations/`, sehingga riwayat migration ikut membawa penambahan kolom `BilInvoice.KwitansiNumber` yang seharusnya sudah tidak berlaku. Bila migration ini dieksekusi apa adanya, tabel `BilInvoice` akan punya kolom+index `KwitansiNumber` yang tidak pernah dibaca/ditulis kode manapun (schema drift, bukan bug fungsional, tapi sebaiknya dibersihkan sebelum `dotnet ef database update`). Tidak diperbaiki oleh laporan ini karena wewenang tulis migration/snapshot eksplisit dipegang pengguna sendiri pada task ini.
- STALE EVIDENCE / BLOCKED PHASES: NOT APPLICABLE
- MANUAL TEST: NOT APPLICABLE — slice ini murni domain/unit test baru (EF InMemory), tidak ada perilaku UI/endpoint baru untuk diklik-coba.
- INCIDENTAL CHANGES: NONE — parameter opsional baru pada helper `AdministrationPolicy(...)` mempertahankan default lama (`coverable = false`) sehingga seluruh pemanggil test existing tidak berubah perilakunya.
- INTERRUPTIONS: NONE dari sisi eksekusi task ini. Catatan investigasi: di tengah pengerjaan, `git status` sempat terlihat "kehilangan" perubahan task backend SEBELUMNYA (Kwitansi per-tender, `BKC-DEC-057`/`058`) karena working tree sudah tercampur dengan dua commit baru (`1c9e056 cek`, `7c2ed4e fix migration`) yang dibuat pengguna sendiri (git identity `yasmina04 <englishrocker07@gmail.com>`) di luar sesi ini sambil sesi berjalan. Diverifikasi lewat `git show`/`grep` bahwa seluruh perubahan `BKC-DEC-057`/`058` (marker `BKC-DEC-057`, method `AllocateKwitansiNumberAsync` terpanggil di `AddTenderAsync`, `KwitansiNumberResponse`/`GetOrAllocateKwitansiNumber` sudah terhapus) tetap utuh — ikut ter-commit oleh pengguna, bukan hilang. Tidak ada pekerjaan yang dipulihkan karena tidak ada yang hilang.
- GIT STATUS (akhir task): `On branch Yasmina, up to date with 'origin/Yasmina'`. 3 file modified (belum staged/commit): `QuilvianSystemBackend.Tests/BillingManagement/BillingCalculationServiceTests.cs`, `QuilvianSystemBackend.Tests/BillingManagement/BillingFinalizationServiceTests.cs`, `docs/module-blueprints/billing-kasir/evidence/06-be-bkc-017-acceptance-evidence-matrix.md`. Ada `stash@{0}` ("On QuilvianIntegrationBackend...") milik branch lain, TIDAK disentuh/di-pop task ini karena bukan bagian scope tulis task ini.
- NEXT RECOMMENDED STEP:
  1. Pengguna: tinjau temuan migration `KwitansiNumber` pada `BilInvoice` di atas (Known Issues #4) sebelum menjalankan `dotnet ef database update` — kemungkinan perlu menghapus blok `AddColumn`/`CreateIndex`/`DropIndex`/`DropColumn` terkait `BilInvoice.KwitansiNumber` dari `20260828045534_AddBillingLatestChanges.cs` secara manual, atau regenerate migration setelah memastikan tidak ada migration lain yang membawa riwayat kolom itu.
  2. Bila kapasitas tersedia: tutup `BIL-AT-020` dengan test terhadap Postgres lokal nyata (bukan EF InMemory) untuk membuktikan `pg_advisory_xact_lock` benar-benar mencegah lost update di bawah concurrency paralel — butuh database lokal terpisah dari eksekusi migration bersama.
  3. `BIL-AT-023` tetap terbuka sampai `BKC-BLK-INT-001` (consumer AR/AP eksternal) terselesaikan — bukan pekerjaan builder backend.
  4. Sanitized Swagger examples (bagian scope `BE-BKC-017` yang belum tersentuh) bisa jadi task terpisah bila diprioritaskan.
