# Conditional ERD Documentation

Create ERD or data-dictionary documentation only when data ownership, relationships, or persistence changes are material. Identify each concept as `Existing`, `Extend`, `New`, or `Adapter/View`; reference the owning module and do not imply migration authority.
