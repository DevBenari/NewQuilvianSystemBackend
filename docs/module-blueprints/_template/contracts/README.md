# Conditional Contracts

Create a versioned contract artifact only for a material API, integration, state, validation, permission, or data contract. Use `<PREFIX>-CON-###`; link its decision, requirement, compatibility impact, owner, approval, source SHA, and acceptance evidence.
