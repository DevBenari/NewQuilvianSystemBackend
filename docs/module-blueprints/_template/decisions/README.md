# Conditional Decisions

Create decision logs only when decisions, approvals, conflicts, or open questions materially affect the module. Use stable IDs `<PREFIX>-DEC-###`; retain superseded decisions as history and never reuse numbers.
