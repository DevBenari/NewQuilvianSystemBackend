namespace QuilvianSystemBackend.Enum
{
    public enum ContractHistoryStatus
    {
        Draft = 0,
        Active = 1,
        Expired = 2,
        Terminated = 3,
        Cancelled = 4,
        Superseded = 5
    }
}
